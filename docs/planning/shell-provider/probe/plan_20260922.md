# Implementation plan: probe

> Aspect of `shell-provider` · Date 2026-09-22 · Strict TDD. The G5 re-anchor (if any) lands
> in this aspect's REFACTOR, the intent-layer precedent.

## Goal

`PROBE-SHELL` — the composed default's facts driven inside the zero-network interposer
(`commands=0`, `spawnsSubprocess=false`) + the seeded arm round trip + guard-the-guard
readers + the intent-catalog-never-names-shell fact.

## Prerequisites

- `wiring` shipped (the composition the probe observes).

## Files to create/modify

- `Sources/VoccaNetworkProbe/ShellDrive.swift` — the probe drive (temp-dir registry +
  enablement, real `/bin/echo` round trip, dry-run row, fact carrier)
- `Sources/VoccaNetworkProbe/VoccaNetworkProbe.swift` — `PROBE-SHELL` line + module
  coverage list
- `Tests/HarnessTests/ZeroNetworkTests.swift` — the verbatim post-condition assertions +
  guard-the-guard readers
- `Scripts/test-with-floor.sh` — floor raise (growth commit)

## Implementation steps

1. **RED** — the `ZeroNetworkTests` additions: `PROBE-SHELL` post-condition lines
   (`commands=0`, `spawnsSubprocess=false`, round-trip `binding=matched`, decisions,
   ordinals), the guard-the-guard readers refusing a weakened constant (planted-line
   control), and a resolver-catalog fact asserting no `dev.vocca.shell` row is ever
   composed into the intent wiring.
2. **GREEN** — `ShellDrive` + the probe wiring; the interposer launches the probe binary
   with the shim loaded (`DYLD_INSERT_LIBRARIES`, `VOCCA_NETWORK_INTERPOSER_LOG`).
3. **REFACTOR** — if `AppBootstrap` grew this slice, **re-anchor the G5 pin deliberately**
   (computed, never edited-to-match); dictation digests asserted unchanged; the D2 limit
   recorded in the probe's doc (an *enabled* shell child is not observable — the probe
   proves the default cannot spawn, never that an enabled command cannot egress).
4. Raise the floor in the same commit as the tests.

## Validation

- `Scripts/test-with-floor.sh` green with the raised floor.
- `PROBE-SHELL` runs inside the zero-network interposer (exit 0, interposer loaded, probe
  completed) — the `runProbe(mode:)` shape.
- Guard-the-guard fails loudly on a planted weakening.

## Commit messages

- `probe: RED — the PROBE-SHELL post-conditions and the guard-the-guard pair`
- `probe: GREEN — the composed shell drive inside the interposer`
- `probe: REFACTOR — the deliberate G5 re-anchor and the D2 limit record`
- `probe: record — the floor ratchet N -> N+…`