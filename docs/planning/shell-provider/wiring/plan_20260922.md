# Implementation plan: wiring

> Aspect of `shell-provider` · Date 2026-09-22 · Strict TDD. **Step 1 is a fact-finding
> check** (PRD open question 1), decided here before any composition work.

## Goal

Additive composition: ShellProvider reachable from the existing arm surface through the
existing card/executor/audit spine, composed default unwired (nothing configured, nothing
spawns), `spawnsSubprocess` declared truthfully, the D2 copy on the surface, intent catalog
untouched.

## Prerequisites

- `command-registry`, `execution`, `provider` shipped; lint green at the raised floor.
- Step 1 answered: does the generic arm surface (enablement rows → `ActionWiring` arm) render
  a `dev.vocca.shell` tool already, or is a small shell leg needed?

## Files to create/modify

- `Sources/VoccaBootstrap/ShellWiring.swift` — the additive recipe (probe-safe, the
  `ActionWiring.swift` / `ContextWiring.swift` shape)
- `Sources/VoccaBootstrap/AppBootstrap.swift` — additive composition (only if the recipe
  must be invoked from `configure`; **G5 re-anchor risk**)
- Possibly a small arm-surface leg in `VoccaUI`/`VoccaBootstrap` if step 1 answers "not
  generic"
- `Tests/HarnessTests/ShellWiringTests.swift` — the composed acceptance
- `Scripts/test-with-floor.sh` — floor raise (growth commit)

## Implementation steps

1. **Fact-finding** — read how the Actions tab renders enablement rows and how
   `ActionWiring.arm` builds the tool list. Record the answer (generic vs leg) in this
   plan; if generic, skip the leg; if not, the leg is a small RED→GREEN unit on its own.
2. **RED** — `ShellWiringTests` (over a seeded temp-dir registry + enablement + the real
   engine, benign `/bin/echo`):
   - armed read-only command → card shows the argv-derived sentence → confirm → execute →
     audit reconstruct (ordinals, decisions, binding=match);
   - decline records the refused decision;
   - dry-run records but never invokes (the `failsTheTestIfInvoked` mode);
   - a disabled command is declined before any `describe`;
   - `commands=0` / `spawnsSubprocess=false` on the composed default.
3. **GREEN** — `ShellWiring` recipe + composition.
4. **REFACTOR** — the D2 copy on the surface (exact-in-spirit of the server-author copy);
   if `AppBootstrap` grew, **re-anchor the G5 pin deliberately** (computed, never
   edited-to-match) in this aspect's REFACTOR commit; the dictation digests must be
   unchanged.
5. Raise the floor in the same commit as the tests.

## Validation

- `Scripts/test-with-floor.sh` green with the raised floor.
- The dictation digest pin (`SessionMachine.swift`, `DictationPipeline.swift`) unchanged.
- The arm round trip is exercised on the real spine (not a stub), the 
  `ActionWiringHarness` precedent.

## Commit messages

- `wiring: RED — the composed shell arm round trip`
- `wiring: GREEN — the additive ShellWiring recipe`
- `wiring: REFACTOR — the D2 copy and the deliberate G5 re-anchor`
- `wiring: record — the floor ratchet N -> N+…`