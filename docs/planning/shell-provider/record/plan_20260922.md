# Implementation plan: record

> Aspect of `shell-provider` · Date 2026-09-22 · Last aspect, written from the record of
> what actually landed (the intent-layer record precedent).

## Goal

The unit's honesty obligations: STATUS head entry + CLAUDE.md sync + CAPABILITY_ROADMAP
amendment + ARCHITECTURE row + SMOKE 151-153 written and runnable + floor ratchet in the
growth commit + planning artifacts committed.

## Prerequisites

- The code aspects shipped: `command-registry`, `execution`, `provider`, `wiring`,
  `probe` — each green at its raised floor.

## Files to create/modify

- `docs/STATUS.md` — head entry (newest first): per-aspect what shipped, the decisions
  (destructive-by-default, argv-derived sentence, arm-surface-only, self-contained registry,
  the transport-permit widening with its D2 answer), the honest claims (R8 amplified, N2,
  D2), the no-gate-passes posture, the floor.
- `CLAUDE.md` — the top status paragraph in sync with the tree at the tip.
- `docs/technical/CAPABILITY_ROADMAP.md` — the C13 amendment: ShellProvider ships; what
  remains (coding-agent handoff, reply rendering, S1, time-boxed trust, the intent-seam
  shell leg).
- `docs/technical/ARCHITECTURE.md` — the shell row (provider, engine, config file, the
  widening).
- `docs/SMOKE_CHECKLIST.md` — steps 151-153, each under rule 1 (verify the state was
  entered), a pass/failure line, marked recorded-never-gated.
- The planning artifacts themselves (card, understanding, PRD, specs, plans) — committed
  with the unit.

## Implementation steps

1. Read each aspect's actual record (commits, floor deltas, any plan deviations) — the
   record is written from what landed, never from the plan.
2. Write the STATUS head entry + the CLAUDE.md paragraph + the CAPABILITY_ROADMAP amendment
   + the ARCHITECTURE row + SMOKE 151-153.
3. Verify the floor ratchet landed in the suite's growth commit (one commit, not two).
4. Commit the record and the planning artifacts together.

## Validation

- STATUS head entry names every decision in the PRD and the honest claim for each inherited
  caveat.
- CLAUDE.md's top paragraph describes the tree at the tip.
- SMOKE 151-153 runnable, each with a rule-1 precondition and a pass/failure line, all
  marked recorded-never-gated.
- No rate quoted; no gate claim made.

## Commit messages

- `record: the shell-provider unit record and the docs sync`
- `record: SMOKE 151-153 and the floor ratchet`