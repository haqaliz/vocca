# Plan: record — 2026-09-16

> Aspect of `dual-mode` (C11, the unit's LAST aspect — it owns the records, not the code).
> Requirements: R11 (`prd.md:184-187`, the SMOKE rows), G8 (`prd.md:85-88`, verifiable on the
> founder's machine), O1 (`prd.md:243`, the G5 pin re-anchor — the *record* of its rationale,
> the commit itself is `converse-wiring`'s), O7 (`prd.md:250`, the five-cue prose drift), the
> aspect boundary (`prd.md:276`), the SMOKE technical consideration (`prd.md:236-237`), and the
> card's binding honesty obligations (`_card/issue.md:42-56`, `_card/understanding.md:87-112`).
> Consumed from the sibling plans: `mode-machine`, `reply-seam`, `converse-hotkey`,
> `converse-wiring`, `widget-converse`, and `per-mode-cleanup` — their SMOKE-visible surfaces,
> seam names, probe filters and hand-offs are cited below and must be consumed, never
> re-derived. Suite command: `./Scripts/test-with-floor.sh` — **full runs only; this aspect adds
> no tests and no filtered runs exist**. Floor: whatever `Scripts/test-with-floor.sh:1763` holds
> at record time (started **2160**; every earlier aspect ratcheted it) — **verified, never
> edited, never blindly ratcheted**. Docs-only: there is no TDD RED/GREEN; the discipline is the
> doc-sync rule — *describe the state of the tree this file ships in* (`CLAUDE.md:147-148`).

## What this aspect ships

1. **SMOKE steps 134-138** — section 20 in `docs/SMOKE_CHECKLIST.md`, the five dual-mode rows
   appended after step 133 (`:2862`), each in the house row shape, each written **runnable,
   executed-later**: 134 the full spoken exchange (≥5 turns including a barge-in, keyboard
   untouched — the P3 gate's leg, with the stand-in reply generator's honesty stated plainly),
   135 mode clarity / zero mis-injections across daily use, 136 the converse chord rebind,
   137 the menu-bar toggle, 138 the converse-never-injects check.
2. **The PRODUCT_SPEC five-cue prose correction** — `docs/product/PRODUCT_SPEC.md:188`,
   "four simultaneous differences" → five cues (O7), in its own auditable commit.
3. **The G5 pin re-anchor record** — the re-anchor *commit* lands in `converse-wiring`; this
   aspect records the rationale and the before/after digests in the STATUS entry.
4. **The doc sync** — STATUS head entry, CLAUDE.md status paragraph, ARCHITECTURE.md seam
   amendments (the house pattern: capability up in the commit that lands it, STATUS append,
   CLAUDE.md status paragraph, ARCHITECTURE seam amendments).
5. **The floor verification** — never a blind ratchet: the record runs the floor script, reads
   the printed numbers, records `Floor <N> (executed <E>)`, and surfaces — never edits — a
   mismatch.
6. **The honesty block** — no gate passes; numbers recorded never gated; the dictation path
   byte-for-byte untouched (digests); AppBootstrap re-anchored with its new digest recorded;
   the stand-in reply is not an agent; zero network.

## Setup

- Worktree: `/Users/aliz/dev/at/vocca/.claude/worktrees/feat-dual-mode`, branch
  `feat/dual-mode/aliz` (verify the actual branch name with `git branch --show-current`).
- **Prerequisite — the whole unit has landed on this branch**: `mode-machine`, `reply-seam`,
  `converse-hotkey`, `converse-wiring`, `widget-converse`, and `per-mode-cleanup` (in the PRD's
  sequencing, `prd.md:278-281`; `record` last). **In particular the `converse-wiring` plan must
  have landed** — its re-anchor commit names the new AppBootstrap digest, its `PROBE-CONVERSE`
  (or `PROBE-TURN` extension) names the probe leg, and its env-gated suite names the filters
  this aspect's SMOKE rows cite. If any sibling plan has not landed, STOP and surface (Edge
  case 1); never invent a suite name, a filter, or a digest.
- **First action of the executing agent: read the six landed sibling plans' Testing strategies
  + Agent notes and their execution records (commits), then re-read this plan's Vetted facts.**
  The record is written from what **shipped**, not from what the PRD promised. Every seam name,
  filter, env var, chord and printed row tag in the SMOKE rows below is taken **verbatim from
  the landed plans** — the drafts here name the source of each; the executor fills the exact
  wording from the landed plan.
- No `swift package resolve` needed. The only commands this aspect runs are the full floor
  script (twice: once in Phase 3 to read the numbers the entries record, once in Phase 4 to
  verify) and git read commands.
- The uncommitted planning docs on the branch (`_card/issue.md`, `_card/understanding.md`,
  `docs/planning/dual-mode/prd.md`, the six landed sibling plans, and this plan) ride in this
  aspect's first commit together with the SMOKE section (the house precedent: planning docs
  land with the record).

## Vetted facts (recorded — do not re-research)

1. **The SMOKE step-entry shape** (steps 129/130, `docs/SMOKE_CHECKLIST.md:2722-2766`, and the
   C10 rows 131-133 at `:2782-2862`): numbered `N. **Title.**` + *Gesture:* (the exact command)
   + *Verify the state was entered:* (rule 1 — a skipped test ran nothing; the skip message is
   the tell-tale) + *Pass:* (the recorded row verbatim with the `recorded-never-gated` token)
   + *Failure:* + optional *Void — not fail — if:*. The two preamble rules
   (`SMOKE_CHECKLIST.md:14-37`) bind every row: rule 1 (verify the state was entered; void,
   never fail) and rule 2 (a pass criterion looser than the failure it guards accepts the
   failure). Section headers are numbered (`## 19. The turn-taking loop — \`turn-taking-barge-in\``
   at `:2770`); the new section is **`## 20. Dual mode — \`dual-mode\``**, inserted after step
   133 (`:2862`) and its `---` (`:2864`), before "When this file is wrong" (`:2866`). Steps
   continue **134, 135, 136, 137, 138** — no gap.
2. **The STATUS head-entry shape** (the turn-taking entry, `docs/STATUS.md:13-118`): bold-led
   summary → corrections/decisions by name → "What shipped, per aspect" with commit hashes and
   floor transitions → "Measured (recorded, never gated)" with the verbatim rows → "The honesty
   block" as closing bullets → the floor as `Floor <N> (executed <E>)`. Newest entry first
   (insert after the `---` at `:11`, before the turn-taking entry at `:13`).
3. **The CLAUDE.md status-paragraph shape** (`CLAUDE.md:10-24`, the turn-taking paragraph):
   capability, what shipped per aspect (compressed), the measured numbers with "recorded, never
   gated", the posture sentence, and `Test floor: <N>.` One paragraph, self-dated, inserted as
   the newest unit above the turn-taking paragraph (between the header blockquote's blank `>`
   at `:9` and `:10`); the header's date `Status (2026-09-15)` (`:5`) updated to the commit's
   date (2026-09-16).
4. **The P3 gate and the numbers the SMOKE rows carry** (from `ROADMAP.md:207-219`; the gate
   belongs to P3, uncleared):
   - The gate's first leg (`ROADMAP.md:215-217`): "A full spoken exchange (≥5 turns, including
     at least one barge-in) completes with the keyboard untouched".
   - The gate's third leg (`ROADMAP.md:218`): "Zero mode-confusion mis-injections across a week
     of daily use".
   - Each row is **recorded, never gated** — an over-budget or short-window observation is
     recorded verbatim, never a pass (the 130 wording precedent: "an over-budget number is
     recorded verbatim, never gated").
5. **The stand-in reply generator's honesty (founder-ratified, binding)** — from R7
   (`prd.md:165-167`): the shipped default is honest about being a stand-in; it must not
   pretend to be an agent; C13's real agent slots in behind the same seam. The record's line,
   stated plainly everywhere the exchange is mentioned: **the machinery exchange proves the
   loop; the P3 gate's conversational leg stays formally unmet until C13's real agent.**
6. **The five-cue prose drift (O7)** — `docs/product/PRODUCT_SPEC.md:188` says "**four**
   simultaneous differences" over the five-row table (`:190-196`: hotkey, shape, color, label,
   sound); the correction is "four" → "five cues". `:203` calls color "the *third* cue" — that
   is a table position (color is the third row), not a count, and G3 (`prd.md:58-59`) quotes it;
   it stays as-is with this reading recorded. G3's own "five simultaneous cues" is the target
   wording.
7. **The G5 digest pin and the re-anchor contract** — `Tests/HarnessTests/TurnTakingComposedAcceptanceTests.swift:311-347`:
   three pinned files: `Sources/VoccaCore/SessionMachine.swift`
   `1baeb2de2c45149746468bfef49862a08279008d3d2f305be892122d5727537e`,
   `Sources/VoccaCore/DictationPipeline.swift`
   `ce70ca10c15914d6960f07e53da8571a5fa9ec1fb58b8f0051ef051f16c07a84`,
   `Sources/VoccaBootstrap/AppBootstrap.swift`
   `03b624df624af005f4e550afff372b1afb82cf340794aa96750de4910044a225` (the **pre-C11** digest).
   The pin's own contract (`:340-344`): a deliberate edit recomputes the digest and edits the
   pin **in review**; "it must never be edited to match a moved tree." C11's wiring is additive
   converse wiring in `AppBootstrap` (the one pinned file that legitimately changes), so the
   re-anchor commit lands in `converse-wiring` (first, before any `AppBootstrap` edit,
   `prd.md:280-281`), and the first two files stay byte-for-byte untouched. **This aspect
   records the rationale and the new AppBootstrap digest in the STATUS entry — the digest
   taken verbatim from the landed re-anchor commit, never invented (Edge case 6).**
8. **The floor** (`Scripts/test-with-floor.sh:1738-1794`): `MINIMUM_EXECUTED_TESTS=2160` at
   `:1763` as of the PRD; each sibling aspect ratchets it in the same commit as the tests it
   counts; env-gated suites' visible skips count as executed. At record time the floor is
   whatever the last sibling aspect (`per-mode-cleanup`) ratcheted to. The record **never edits
   the script**; it runs it, reads the executed count `E` and the floor `N`, records
   `Floor N (executed E)`, and surfaces — never edits — a mismatch.
9. **The real-run command precedent** (SMOKE 129/130, `:2722-2766`): env-gated filtered runs
   (`VOCCA_RUN_REAL_*` vars) run **bare** — never through the floor script (the 131 wording,
   `:2794-2795`); row format follows the house tag=value shape
   (`VAD-CLASSIFY-LATENCY 0.2ms chunks=… recorded-never-gated`, `TURN-HALT <ms>ms
   recorded-never-gated`). The dual-mode rows are **app-surface rows**, not suite rows: the
   gesture is running the built bundle and using the real surface; suite-driven sub-observations
   (probe rows) cite the landed plans' filters verbatim.
10. **The unit's honesty obligations** (binding, `_card/issue.md:42-56`): no P2/P3 gate passes;
    the realtime conversation is executed by nothing in CI; `ParakeetEOU` stays PENDING and is
    consumed as-is; the zero-network invariant and transcript-never-lost invariant are
    load-bearing; hold-to-talk (dictate) stays whole; the dictation path byte-for-byte
    untouched (G5 pin); the ledger stays dictation-only (converse sessions not folded — a
    recorded out-of-scope, `prd.md:257-258`); per-mode ASR engine selection deferred (N1,
    `prd.md:200-204`).

---

## Phase 1 — SMOKE steps 134-138 (the rows, written runnable)

**Goal.** The five dual-mode rows appended to `docs/SMOKE_CHECKLIST.md` as a new section 20
after step 133 (`:2862`) and the section's `---` (`:2864`), before "When this file is wrong"
(`:2866`), each in the step 129/130 + 131-133 shape with the **exact gesture commands cited
from the landed sibling plans** and the state-entered checks per rule 1. The rows are written
**runnable, executed-later**: they state the observation, the gesture, the budget, and the
recorded-never-gated contract; whether the founder has run them by merge time is the Edge case
2 question, not a Phase 1 concern. The uncommitted planning docs ride in this commit.

**Section header + intro** (house shape, after the `---` at `:2864`):

> `## 20. Dual mode — \`dual-mode\``
>
> Nothing in this section runs in CI. The mode machine and its prohibition are proven headlessly
> (the acceptance asserts **no `TextInjector` call is ever made from the converse path** —
> enforced by type/assertion, not discipline — and a mode-transition test asserts full state
> reset with no carryover of buffer, transcript, or target); the realtime conversation itself is
> executed by nothing in CI (the tap-adapter precedent). These five rows are the founder's
> machine producing the first real observations of the CONVERSING surface and the wired loop —
> recorded, never gated, each under **rule 1**: the state must actually have been entered before
> a row means anything. **The reply source is the shipped deterministic stand-in (see step
> 134): the machinery exchange proves the loop; the P3 gate's conversational leg stays formally
> unmet until C13's real agent** — nothing below may be read as a gate pass.

**134. The first full spoken exchange — ≥5 turns with a barge-in, keyboard untouched (C11, recorded — never gated).**

- *Gesture:* run the built bundle (Accessibility grant in place — the tap prerequisite named in
  `SMOKE_CHECKLIST.md` §1), press the converse chord (shipped `⌥⇧Space`, `PRODUCT_SPEC.md:192`),
  and complete a full spoken exchange: **≥5 turns** (speak → the loop commits → ASR → cleanup
  (`.conversing`) → the stand-in reply generator → spoken reply through the shipped renderer
  and playback — per the landed `converse-wiring` plan's composition) with **at least one
  barge-in** (interrupt a reply mid-playback; it must halt ≤200 ms and the interrupting words
  must be preserved, not dropped), then end the session with the shipped stop surface (the
  converse chord again, Esc, or the menu-bar toggle — the landed `widget-converse` plan's
  recorded decision). **The keyboard is untouched throughout** — no typing, no mouse, no
  trackpad. Watch the unified log (`log stream --predicate 'subsystem == "dev.vocca.Vocca"'`)
  for the loop's state transitions. The reply source is the shipped deterministic stand-in
  (`reply-seam` plan's default implementation, named there) — it is **not an agent**: it does
  not understand, remember, or act; it answers with the recorded stand-in behavior so the loop
  is exercisable end to end.
- *Verify the state was entered:* the CONVERSING surface actually showed (the five cues: the
  notched pill, the distinct hue, the `◈` label, the lower tick — and no target app name), **≥5
  turns** actually exchanged, the barge-in actually interrupted **real playback** (a rendered
  reply was sounding and stopped), and no keystroke or click was used (rule 1 — an exchange
  driven by any key press proves nothing).
- *Pass:* the exchange recorded verbatim with the never-gated note: **≥5 turns including ≥1
  barge-in, keyboard untouched** (`ROADMAP.md:215-217`) — with the honesty line attached: the
  replies came from the deterministic stand-in, never an agent; the machinery exchange proves
  the loop; **the P3 gate's conversational leg stays formally unmet until C13's real agent
  slots into the `ReplyGenerator` seam**.
- *Failure:* fewer than 5 turns, no barge-in (or a barge-in without real playback sounding), any
  keyboard/mouse use, the CONVERSING surface not visibly entered, or the row recorded as a gate
  pass (it is recorded-never-gated, and the stand-in honesty line is part of the record).

**135. Mode clarity across daily use — zero mis-injections (C11, recorded — never gated).**

- *Gesture:* use the app in the daily mix — dictate into real fields (`⌥Space` → text lands in
  the focused field) and converse (`⌥⇧Space` → nothing typed anywhere) — across the observed
  window, and watch for the mode-confusion failure: **text intended for the agent landing in an
  app field, or dictate text spoken to the agent**. The gate's own duration is a **week of daily
  use** (`ROADMAP.md:218`); the row records the actual elapsed days and session counts of the
  observed window — a shorter window is a recorded observation, never the gate.
- *Verify the state was entered:* real dictations delivered (≥1 per day of the observed window
  — a window with no dictation proves nothing about mis-injection), real converse sessions ran,
  and both chords were used.
- *Pass:* the row recorded verbatim with the never-gated note: **0 mis-injections** across the
  observed window (days and session counts named). The gate's "zero mode-confusion
  mis-injections across a week of daily use" (`ROADMAP.md:218`) stays the gate's; a shorter or
  sparser window is stated as such, never claimed as the gate leg.
- *Void — not fail — if:* no real dictation occurred in the window, or the window contained no
  converse session (rule 1 — the state was not entered).
- *Failure:* ≥1 mis-injection — the catastrophic mode-confusion failure (`ROADMAP.md:194`): the
  number is recorded verbatim and surfaced to the integrator loudly (Edge case 4); it is a
  product bug, never silently absorbed.

**136. The converse chord rebind (C11, recorded — never gated).**

- *Gesture:* Settings → General → rebind the converse chord through the landed
  `converse-hotkey` plan's two-chord rebind flow (verbatim): rebind `⌥⇧Space` to a new chord,
  verify the **new** chord starts and stops converse and the old chord no longer does; test the
  collision warning (rebind the converse chord onto the dictate chord — the rebind is refused
  with the warning, `PRODUCT_SPEC.md:252`); verify a rebind is **refused while any session is in
  flight** (R3); then relaunch the app and verify the rebind **persisted**.
- *Verify the state was entered:* the rebind actually persisted across relaunch (persistence is
  the claim), the collision warning actually surfaced when tested, the in-flight refusal was
  actually exercised during a live session, and the dictate chord's behavior was untouched
  throughout.
- *Pass:* the row recorded verbatim with the never-gated note: the rebind persisted across
  relaunch, the old chord inert, the dictate chord unchanged (equality-match semantics — neither
  chord can end the other's session), the collision warning surfaced, the in-flight rebind
  refused.
- *Failure:* a rebind that works only until relaunch, a rebind that silently breaks or aliases
  the other chord, a missing collision warning, or a rebind applied mid-session.

**137. The menu-bar mode toggle (C11, recorded — never gated).**

- *Gesture:* open the menu bar (`PRODUCT_SPEC.md:361` — current state + the mode toggle, from
  the landed `widget-converse` plan's surface): verify the menu bar **shows the current state**
  while a session is active; toggle to converse and complete a turn; toggle back and dictate;
  verify the toggle's behavior during a live session per the landed decision (explicit
  switching only — never inferred — and at most one capture active: a toggle that would start a
  second capture is refused or stops the session first).
- *Verify the state was entered:* the menu bar item existed and showed state; toggling actually
  switched modes (the CONVERSING surface appeared/disappeared); a converse session was actually
  stoppable from the menu (R4's explicit stop affordance).
- *Pass:* the row recorded verbatim with the never-gated note: the toggle switches modes
  explicitly, the current state is visible in the menu bar, and no toggle ever produced two
  live captures or an inferred switch.
- *Failure:* a toggle that starts a second capture, a state not visible in the menu bar, an
  inferred (non-explicit) switch, or a converse session not stoppable from the menu.

**138. The converse-never-injects check (C11, recorded — never gated).**

- *Gesture:* put the cursor in a real editable field in a real app (Notes, Slack, an editor),
  type a known marker, then run a **converse** session (≥1 committed turn — speak, hear the
  reply, end); after the session, verify the focused field contains **exactly the marker and
  nothing else** — no transcript text was typed, pasted, or AX-inserted; and verify the widget
  showed **no target app name** in converse (the absence of `→ AppName` is itself a mode
  signal, `PRODUCT_SPEC.md:198-201`).
- *Verify the state was entered:* the CONVERSING surface was really entered (listening/speaking
  states seen), the session committed ≥1 turn (ASR ran and produced a transcript — a silent
  session proves nothing), and the focused field was a real editable field with the marker
  before the session.
- *Pass:* the row recorded verbatim with the never-gated note: **0 bytes entered into the
  focused field** across the converse session, and no target app name shown — the structural
  prohibition (no `TextInjector` call from the converse path, enforced by type/assertion and
  asserted in CI) observed on the real surface.
- *Failure:* any text landing in the field, the marker disturbed, or the widget showing a target
  app name in converse (any of these is a prohibition violation — recorded verbatim and
  surfaced, Edge case 4).

**Validation.**
1. Read the five rows back against the landed sibling plans: every *Gesture:* names the landed
   plan's own surface (the rebind flow, the toggle, the stop affordance, the stand-in's name,
   the probe filter where a suite is cited), every *Pass:* carries the `recorded-never-gated`
   token and its numbers, every row has *Verify the state was entered:*, and steps 135 has its
   *Void* clause. The stand-in honesty line is present in 134 and in the section intro.
2. The steps' numbering continues 131-133 → 134-138 with no gap; the section is numbered 20
   after section 19.
3. `git diff --stat` shows exactly `docs/SMOKE_CHECKLIST.md` (plus the already-dirty planning
   docs) — no `Sources/`, no `Tests/`, no `Scripts/`.

**Commit message.** `record: SMOKE steps 134-138 — the dual-mode real surface rows (the spoken exchange, mode clarity, the chord rebind, the menu-bar toggle, the never-injects check)`

---

## Phase 2 — the PRODUCT_SPEC five-cue prose correction (O7)

**Goal.** The prose drift named by O7 corrected in its own auditable commit: the defense's
count says four over a five-row table. The fix is the count; the table is the spec.

**Files.** `docs/product/PRODUCT_SPEC.md` — one edit at `:188`:

- `The defense is redundancy — **four** simultaneous differences, so no single missed cue causes confusion.`
  → `The defense is redundancy — **five** simultaneous cues, so no single missed cue causes confusion.`

**Recorded decisions (no further edits):**
- `:203`'s "color is the *third* cue" stays as-is: color is the third **row** of the five-cue
  table (`:190-196`) — a position, not a count — and G3 (`prd.md:58-59`) quotes it. The fix
  that removes the drift is the count at `:188`; nothing else in §5 contradicts five cues.
- If the landed `widget-converse` plan's copy pins surfaced any further §5 wording conflict,
  record that here rather than silently fixing it (the aspect's boundary is the O7 correction).

**Validation.** `git diff --stat` shows exactly `docs/product/PRODUCT_SPEC.md`; the edit is the
single count word and its noun ("differences" → "cues"), nothing else in §5 touched.

**Commit message.** `record: correct the PRODUCT_SPEC five-cue prose — the defense is five simultaneous cues, not four differences (O7)`

---

## Phase 3 — the STATUS head entry (with the pin re-anchor record), the CLAUDE.md status paragraph

**Goal.** The unit's record in the two always-read files, in the turn-taking pattern, with
**real numbers** — the floor script is run first, and the entries carry its printed numbers,
never placeholders.

**Files.**
- `docs/STATUS.md` — a head entry inserted after the `---` (`:11`), before the turn-taking
  entry (`:13`). Shape (the turn-taking pattern, `:13-118`):
  - **Bold-led summary** — e.g. `**The \`dual-mode\` unit shipped 2026-09-16 — C11's CONVERSING
    surface is real: the mode machine, the wired loop, the honest reply stand-in; the P3 gate's
    conversational leg stays formally unmet until C13's agent.**` `feat/dual-mode/aliz`.
  - **What shipped, per aspect** — one sentence per aspect, written from the **landed commits**
    (hashes + floor transitions taken from the landed `git log` — never invented): `mode-machine`
    (the explicit state machine, chord-keyed routing, no implicit switching, the structural
    injection prohibition + the reset acceptance), `reply-seam` (the `ReplyGenerator` protocol +
    the two deterministic local implementations, honest copy), `converse-hotkey` (the second
    chord persisted + the two-chord rebind surface + the collision warning), `converse-wiring`
    (C10's recipe executed in VoccaBootstrap — additive —, `PROBE-CONVERSE`, and the **G5 pin
    re-anchor commit**), `widget-converse` (the CONVERSING state, the five cues, the
    never-a-target rule, the menu-bar toggle), `per-mode-cleanup` (`CleanupContext.mode`
    consumed, the persisted per-mode selection), and this `record` aspect. Where a sibling plan
    promised a hand-off, the entry records what **actually** happened.
  - **The pin re-anchor record** — its own short paragraph: `AppBootstrap.swift` is in the G5
    pin (`TurnTakingComposedAcceptanceTests.swift:311-347`); C11's wiring is additive converse
    wiring in exactly that file, so the pin was **deliberately re-anchored in a reviewed
    commit** (`converse-wiring`'s re-anchor hash, cited) — per the pin's own contract
    ("a deliberate edit recomputes the digest and edits the pin in review; it must never be
    edited to match a moved tree"). The dictation files' digests are unchanged:
    `SessionMachine.swift 1baeb2de2c45149746468bfef49862a08279008d3d2f305be892122d5727537e`,
    `DictationPipeline.swift ce70ca10c15914d6960f07e53da8571a5fa9ec1fb58b8f0051ef051f16c07a84`;
    `AppBootstrap.swift` re-anchored from `03b624df…` to the **new digest taken verbatim from
    the landed re-anchor commit** (never invented).
  - **The five-cue correction recorded** — one line: `PRODUCT_SPEC.md:188`'s "four differences"
    corrected to five cues (O7), with the `:203` positional reading recorded.
  - **Measured, recorded never gated** — the SMOKE 134-138 rows' state: written and runnable;
    if the founder ran any of them before merge, the verbatim rows (with the stand-in honesty
    line on 134); if not, the entry says exactly that. The unit's headless/CI facts are recorded
    as CI facts with margins, never gate passes (the prohibition acceptance, the reset test,
    the probe leg's numbers).
  - **The honesty block** — the closing bullets: no P2/P3 gate passes (the P3 gate's
    spoken-exchange leg is SMOKE 134, recorded never gated, and **the conversational leg stays
    formally unmet until C13's real agent — the shipped reply is an honest stand-in, never an
    agent**); numbers recorded never gated; the dictation path byte-for-byte untouched (the two
    digests above) with AppBootstrap's re-anchor recorded; converse never injects (structural,
    asserted); zero network (`PROBE-CONVERSE` inside the interposer, no URL in the converse
    path); the ledger stays dictation-only; interim states recorded, not papered over
    (`ParakeetEOU` PENDING consumed as-is; per-mode ASR engine selection deferred, N1).
  - **The floor line** — `Floor <N> (executed <E>)` from the Phase 3 floor run.
- `CLAUDE.md` — one new status paragraph, inserted as the newest unit above the turn-taking
  paragraph (between the header blockquote's blank `>` at `:9` and `:10`), and the header's
  date `Status (2026-09-15)` (`:5`) updated to the commit's date (2026-09-16). The paragraph
  carries the same substance in the turn-taking wording pattern: the unit's capability; what
  shipped per aspect (compressed); the five-cue correction and the pin re-anchor in one line
  each; the SMOKE rows' state (or their measured numbers) with the stand-in honesty line; the
  posture sentence — "**No gate passes; the P3 gate's conversational leg stays formally unmet
  until C13's real agent** (the shipped reply generator is an honest stand-in)" — and
  `Test floor: **<N>**.` Self-dated like every paragraph; history below it untouched.

**Validation.**
1. **Run `./Scripts/test-with-floor.sh` first** — full run, green; record the printed executed
   count `E` and the floor constant `N` at `Scripts/test-with-floor.sh:1763`. Both entries are
   written with those numbers.
2. Read both entries against Vetted fact 10's checklist: every honesty obligation has a line;
   no sentence claims a gate pass, a conversational agent, or a number CI did not produce; the
   AppBootstrap digest in the pin-re-anchor paragraph matches the landed re-anchor commit
   verbatim.
3. `git diff --stat` — exactly `docs/STATUS.md`, `CLAUDE.md`, and the already-dirty planning
   docs.

**Commit message.** `record: the C11 unit record — STATUS head entry (the pin re-anchor rationale, the five-cue correction, the SMOKE rows' state) and the CLAUDE.md status paragraph`

---

## Phase 4 — the ARCHITECTURE.md sync, the floor verification, the honesty-block check

**Goal.** `docs/technical/ARCHITECTURE.md` describes the tree this unit's close ships in: the
C11 seam amendments, the loop's "wired by nothing" state resolved, `CleanupContext.mode`
consumed; the floor verified (not ratcheted); the honesty block confirmed complete.

**Files.** `docs/technical/ARCHITECTURE.md` — the named amendments, each verified against the
tree **and the landed sibling plans** before writing (seam names verbatim from the plans;
never claimed if not shipped):

1. **The seam table** (`:262-274`):
   - New row "Reply generation" — `ReplyGenerator` with the two landed deterministic
     implementations (names verbatim from the `reply-seam` plan), the seam documented as C13's
     slot for the real agent.
   - New row (or §12 annotation) "Mode (dictate/converse)" — the `SessionMode` state machine:
     chord-keyed, no implicit switching, the injection path reachable only from the dictate
     state (type/assertion-enforced), the machine's shipped type name verbatim from the
     `mode-machine` plan.
   - The Cleanup row's annotation: per-mode provider selection now real (consumed at last).
2. **The §12 Voice loop section** (`:568-592`): an annotation recording the C11 wiring — the
   C10 recipe executed in `VoccaBootstrap` (additive): capture-stream driver over
   `StreamingCapture`, the loop composed with the fallback VAD/detector and the shipped reply
   stand-in, utterance → ASR → cleanup (`.conversing`) → reply → render → playback with
   `reportPlayback*`, the barge-in path applied, `onStateChange` wired to the widget projection;
   the `PROBE-CONVERSE` leg drives the converse default work inside the zero-network interposer.
   The C10 annotations above are untouched — this amendment is additive, below them.
3. **The §11 Cleanup pipeline section** (`:542-564`): `CleanupContext.mode` consumed — the
   declared-not-read state resolved by the per-mode provider selection (the resolver + the
   persisted keys, names verbatim from the `per-mode-cleanup` plan); the timeout-yields-raw
   policy unchanged.
4. **The §4/§7 SessionMachine annotations** (`:235-237`, `:391-393`): `SessionMode` is now the
   explicit mode machine (the `CleanupContext.mode` field and the orchestrator's "Owns
   SessionMode" get their resolution lines); the dictation/converse wiring selection is
   chord-keyed at session start.
5. **The §3 directory layout line** (`:101`, `Session/`): annotated per the shipped truth — the
   mode machine's actual location (per the landed `mode-machine` plan), never a claimed one.
6. **The §14 harness table** (`:688-697`): a new row naming the mode-machine acceptance (no
   `TextInjector` from the converse path; the mode-transition reset) and the `PROBE-CONVERSE`
   drive — what runs in CI, what is founder-run (SMOKE 134-138), recorded never gated.
7. **Any §16 open question the unit closes** — verify at execution time; the converse
   transcript surface (`PRODUCT_SPEC.md:379`) stays C13's; the echo question (`:727`) stays
   C10's; the "wired by nothing" state is resolved by amendment 2. Do not close anything else.

**Floor verification (no edits).**
1. `./Scripts/test-with-floor.sh` from the worktree root — green; the executed count `E` and
   floor `N` print. Verify `N == E` (the record adds no tests, so the floor and the executed
   count are the same number the last sibling aspect ratcheted to).
2. **If `N != E`**: do NOT edit `Scripts/test-with-floor.sh` — the doc-sync rule names the
   truth, not the chase; surface the discrepancy to the integrator and record both numbers
   honestly (Edge case 5).

**Honesty-block check.** Re-read the Phase 3 entry against Vetted fact 10 and the SMOKE rows
against Vetted fact 4: every obligation has a line; every row carries the
`recorded-never-gated` token; the stand-in honesty line is present; nothing in the tree's
record claims a gate pass. The `git diff --stat` of the whole aspect (its four commits) lists
no `Sources/`, no `Tests/`, no `Scripts/` change — the unit's code shipped in the sibling
aspects' commits; this aspect's diff is docs only.

**Commit message.** `record: sync ARCHITECTURE.md to the shipped C11 seams and the converse wiring, and verify the floor`

---

## Commit sequence for this aspect

| # | Files | Message |
|---|-------|---------|
| 1 | `docs/SMOKE_CHECKLIST.md` (+ the uncommitted planning docs riding: `_card/issue.md`, `_card/understanding.md`, `docs/planning/dual-mode/`, including this plan) | `record: SMOKE steps 134-138 — the dual-mode real surface rows (the spoken exchange, mode clarity, the chord rebind, the menu-bar toggle, the never-injects check)` |
| 2 | `docs/product/PRODUCT_SPEC.md` | `record: correct the PRODUCT_SPEC five-cue prose — the defense is five simultaneous cues, not four differences (O7)` |
| 3 | `docs/STATUS.md`, `CLAUDE.md` | `record: the C11 unit record — STATUS head entry (the pin re-anchor rationale, the five-cue correction, the SMOKE rows' state) and the CLAUDE.md status paragraph` |
| 4 | `docs/technical/ARCHITECTURE.md` | `record: sync ARCHITECTURE.md to the shipped C11 seams and the converse wiring, and verify the floor` |

No `Sources/`, no `Tests/`, no `Scripts/`, no `Package.swift` in any commit.

## File-by-file build order

| File | Edit | Where it lands | Exports |
|---|---|---|---|
| `docs/SMOKE_CHECKLIST.md` | Phase 1: the section 20 header + intro + steps 134-138, each with *Gesture / Verify the state was entered / Pass / Failure* (+ *Void* for 135) | after step 133 (`:2862`) and its `---` (`:2864`), before "When this file is wrong" (`:2866`) | the five recorded-never-gated rows |
| `docs/product/PRODUCT_SPEC.md` | Phase 2: `:188` "four simultaneous differences" → "five simultaneous cues" | §5's opening paragraph | the O7 correction |
| `docs/STATUS.md` | Phase 3: the head entry — bold-led summary, per-aspect shipped (hashes/floors from the landed log), the pin re-anchor record, the five-cue correction line, measured rows, honesty block, floor line | after the `---` (`:11`), before the turn-taking entry (`:13`) — newest first | the unit record |
| `CLAUDE.md` | Phase 3: the new status paragraph + the header date (`:5` `2026-09-15` → 2026-09-16) | between the header blockquote's blank `>` (`:9`) and the turn-taking paragraph (`:10`) | the always-loaded unit summary |
| `docs/technical/ARCHITECTURE.md` | Phase 4: seam-table rows (`:262-274`), §12 wiring annotation (`:568-592`), §11 mode consumption (`:542-564`), §4/§7 lines (`:235-237`, `:391-393`), §3 layout (`:101`), §14 harness row (`:688-697`) | the sections named | the authoritative-tree sync |
| (untouched, by design) | `Scripts/test-with-floor.sh`, `Package.swift`, every `Sources/` file, every `Tests/` file | — | — |

## Testing strategy

- **This aspect adds no tests.** There is no RED/GREEN and no filtered run; the validation is
  the doc-consistency read and the floor script:
  - `./Scripts/test-with-floor.sh` — twice (Phase 3 to read the numbers the entries record,
    Phase 4 to verify `N == E`). The record does not change the executed count, so a green run
    at the unchanged floor is the whole test.
  - The doc-sync read: STATUS head, CLAUDE.md paragraph and ARCHITECTURE's named sections all
    describe the same tree (the same floor number, the same seam names, the same interim
    states) — the "describe the state of the tree this file ships in" rule.
  - The SMOKE rows' internal consistency: the numbers match ROADMAP/PRD (≥5 turns, ≥1 barge-in,
    keyboard untouched, 0 mis-injections, the week of daily use), the stand-in honesty line is
    present, and the gestures match the landed plans' wording.
- **What CI structurally cannot cover here** is stated, not hidden: the rows themselves (real
  conversation, real app surface, daily use) are founder-run by definition — that is why they
  are SMOKE rows with the recorded-never-gated token, and why the state-entered checks are the
  tell-tales.

## Environment & privacy

- **Docs-only: no code, no network, no model bytes, no runtime of any kind in this aspect.**
  The only executed command is the floor script (a local `swift test` run).
- The SMOKE rows **never claim egress**: every gesture runs the local app with the default
  zero-network configuration (the converse default work is fallback VAD/detector + the minimal
  reply generator — no model artifact, no SDK, no URL, `prd.md:80-84`); model artifacts, where
  any are involved, provision through the C2 store, never inside `configure`.
- The rows carry no transcript text and no audio: they record numbers, outcomes and counts,
  matching the usage-ledger byte-level pin's spirit (`docs/STATUS.md`, daily-use-ledger entry).

## Edge cases

1. **A sibling plan has not landed at record time** — STOP and surface; this aspect's rows
   cannot cite surfaces, filters or digests it would have to invent. (Should not occur: `record`
   runs last by sequencing, `prd.md:278-281`.)
2. **The founder has not run the steps by merge time** — the rows are written runnable,
   executed-later; the STATUS/CLAUDE entries say exactly that ("SMOKE 134-138 written; the
   executed rows land when the founder runs them"). Precedent: steps 131-133 were unexecuted at
   the C10 unit close. **This aspect's merge does NOT depend on the steps having been
   executed** — recommended, and the integrator may decide otherwise (Checkpoint 2). A
   non-executed step is never recorded as a pass, never as a fail — it is an unrun step, stated
   as such.
3. **A voided SMOKE run** — rule 1: a run whose state was not entered (no dictation in the
   window for 135, no real playback for 134's barge-in, keyboard used) is **void, not fail**:
   it is never recorded as a number, and the STATUS entry says the row awaits a valid run.
4. **An observed failure in the product** (a mis-injection in 135, bytes in the field in 138, a
   barge-in over budget in 134) — the row is **recorded verbatim** with the never-gated token,
   and the finding is surfaced to the integrator loudly (it is a product bug, not a record
   failure — but mode-confusion mis-injection is the catastrophic failure `ROADMAP.md:194`, and
   silence would be the real error). Never "fix" the product from the record aspect.
5. **Floor mismatch at verification** (`N != E` at Phase 4) — record both numbers honestly in
   the STATUS entry, surface to the integrator, and never edit the script. The doc-sync rule is
   about describing the tree, not correcting it.
6. **The pin re-anchor state is wrong at record time** — the landed `converse-wiring` re-anchor
   commit is absent, or its new AppBootstrap digest contradicts the pinned tree: STOP and
   surface. Never invent a digest; the record cites the pin as it landed.
7. **A sibling hand-off that did not happen** — e.g. `converse-wiring` did not update
   `CAPABILITY_ROADMAP.md`'s C11 row to shipped (the kokoro-binding sync-commit precedent,
   `71a4ee6`; C10's own close, `d7241ca`): flag it to the integrator in Checkpoint 3 — the
   record can carry it in Phase 4 if the integrator wants the roadmap row closed with the unit.

## Agent notes

- **Checkpoint 1 (after Phase 1):** the five SMOKE rows read back against the landed sibling
  plans — the integrator reviews that every *Gesture:* is the landed wording (rebind flow,
  toggle, stop affordance, stand-in name, probe filters), every number is the PRD/ROADMAP
  number, and the stand-in honesty line is present in 134 and the section intro. Surface the
  row drafts before Phase 2.
- **Checkpoint 2 (the founder check):** the SMOKE steps are ready to run on the founder's
  machine. The recommended merge posture: **this aspect merges without requiring the runs**
  (Edge case 2) — the rows exist as runnable, executed-later steps; the founder runs them after
  the unit merges, and the executed rows land as follow-up edits to the SMOKE section and
  STATUS entry. If the integrator prefers the rows executed before merge, the founder runs them
  during this aspect — recorded as the unit's final state either way.
- **Checkpoint 3 (after Phase 4):** the ARCHITECTURE diff reviewed (seam-table rows, §12 wiring
  annotation, §11 mode consumption, §14 harness row), the floor verified (`N == E`, or
  surfaced), the pin-re-anchor paragraph's digest cross-checked against the landed re-anchor
  commit, and the final `git log` shows this aspect's four commits on top of the six sibling
  aspects'.
- **Merge order:** `record` is the unit's last aspect; after its merge the unit closes — the
  branch merges to `master`, the unit's record is complete, and the P2/P3 gates stay uncleared
  with the posture named.
- **STOP and surface** (never improvise): any sibling plan absent (Edge case 1); any
  ARCHITECTURE fact that contradicts what shipped beyond the recorded amendments; any
  instruction to edit `Scripts/test-with-floor.sh`; any instruction to "fix" a sibling aspect's
  state from the record aspect; any temptation to invent the AppBootstrap digest (Edge case 6).
- **Do NOT touch** (by design): every `Sources/` file, every `Tests/` file, `Package.swift`,
  `Scripts/test-with-floor.sh` (the floor line included), the dictation path, and the
  already-shipped planning docs' history — the record appends; it does not rewrite.

## Checkpoints (for the human integrator)

1. **After Phase 1** — review the five SMOKE rows (134-138) against the six landed sibling
   plans: gesture wording verbatim, numbers exact, the stand-in honesty line present, step 135
   carries its *Void* clause. Approve before Phase 2.
2. **The founder check** — the rows are ready to run on your machine; the recommended posture
   is that this aspect merges without the runs (Edge case 2, the C10 precedent). If you want
   the rows executed before merge, run them during this aspect.
3. **After Phase 3** — read the STATUS head entry and the CLAUDE.md paragraph: the per-aspect
   hashes/floors match the landed log; the pin-re-anchor paragraph quotes the landed re-anchor
   commit's new AppBootstrap digest; the five-cue correction line is present; the honesty
   block's "conversational leg formally unmet until C13" line is present.
4. **After Phase 4** — review the ARCHITECTURE diff (the seam-table rows, the §12 wiring
   annotation, the §11 mode consumption, the §14 harness row); confirm the floor verification
   printed `N == E` (or that the mismatch was surfaced, never edited); confirm the final `git
   log` shows the four record commits on top of the six siblings; decide whether the
   CAPABILITY_ROADMAP C11 row close rides here (Edge case 7) or lands as its own follow-up.
5. **At unit close** — no gate passes; the P3 gate's conversational leg is formally unmet until
   C13's real agent; the dictation path is byte-for-byte untouched with the digests recorded.