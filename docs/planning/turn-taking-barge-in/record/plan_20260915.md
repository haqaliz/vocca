# Plan: record — 2026-09-15

> Aspect of `turn-taking-barge-in` (C10, the unit's LAST aspect — it owns the records, not the
> code). Requirements: PRD G4 (`prd.md:59-64`, turn commitment ≥95% with the 5× weighting),
> G6 (`prd.md:70-75`, zero-egress / no model bytes in the probe), O4 (`prd.md:218`, realtime
> coexistence), the SMOKE technical consideration (`prd.md:207-209`), the aspect boundary
> (`prd.md:243`), and the card's binding honesty obligations (`_card/issue.md:42-56`,
> `_card/understanding.md:108-122`). Consumed from the sibling plans: `sdk-vetting`,
> `voice-detection`, `streaming-capture`, `playback-ducking`, and `barge-in-loop` — their SMOKE
> claims, doc-sync notes and hand-offs are cited below and must be consumed, never re-derived.
> Suite command: `./Scripts/test-with-floor.sh` — **full runs only; this aspect adds no tests
> and no filtered runs exist**. Floor: whatever `Scripts/test-with-floor.sh:1639` holds at
> record time (started **1978**; every earlier aspect ratcheted it) — **verified, never
> edited**. Docs-only: there is no TDD RED/GREEN; the discipline is the doc-sync rule — *describe
> the state of the tree this file ships in* (`CLAUDE.md:147-148`).

## Setup

- Worktree: `/Users/aliz/dev/at/vocca/.claude/worktrees/feat-turn-taking-barge-in`, branch
  `feat/turn-taking-barge-in/aliz`.
- **Prerequisite — the whole unit has landed on this branch**: `sdk-vetting`,
  `voice-detection`, `sdk-adapters`, `streaming-capture`, `playback-ducking`, and
  `barge-in-loop` (in the PRD's sequencing, `prd.md:245-247`). **In particular the
  `barge-in-loop` plan (`docs/planning/turn-taking-barge-in/barge-in-loop/plan_20260915.md`)
  must have landed** — its env-gated composed suite names the exact filters, gate variables,
  corpus name and printed row formats this aspect's SMOKE rows cite. If it has not landed,
  STOP and surface (Edge case 4); never invent a suite name.
- **First action of the executing agent: read the landed `barge-in-loop` plan's Testing
  strategy + Agent notes** and its execution record (commits), then re-read the four sibling
  plans' hand-offs named in Vetted facts below. The record is written from what **shipped**,
  not from what the PRD promised.
- No `swift package resolve` needed. The only command this aspect runs is the full floor
  script (twice: once in Phase 2 to read the numbers the entries record, once in Phase 3 to
  verify). `swift test` alone is never run — the floor script's executed-count arithmetic is
  the whole point.
- The uncommitted planning docs on the branch (`_card/issue.md`, `_card/understanding.md`,
  `prd.md`, the five landed aspect plans) ride in this aspect's first commit together with the
  SMOKE section (the house precedent: planning docs land with the record).

## Vetted facts (recorded — do not re-research)

1. **The SMOKE step-entry shape** (steps 129/130, `docs/SMOKE_CHECKLIST.md:2722-2766`, the
   C9 TTFA rows): numbered `N. **Title.**` + *Gesture:* (the exact command) + *Verify the
   state was entered:* (rule 1 — a skipped test ran nothing; the skip message is the
   tell-tale) + *Pass:* (the recorded row verbatim with the `recorded-never-gated` token,
   baseline + budget where applicable) + *Failure:* + optional *Where no sharp criterion
   exists:* / *Void — not fail — if:*. The two preamble rules (`SMOKE_CHECKLIST.md:14-37`)
   bind every row: rule 1 (verify the state was entered; void, never fail) and rule 2 (a pass
   criterion looser than the failure it guards accepts the failure).
2. **The STATUS head-entry shape** (the kokoro-binding entry, `docs/STATUS.md:13-103`):
   bold-led summary → the vetting corrections by name → "What shipped, per aspect" → the
   measured numbers with the **verbatim** row → the honesty block as closing bullets → the
   floor as `Floor <N> (executed <E>)`. Newest entry first (insert after the `---` at
   `:11`).
3. **The CLAUDE.md status-paragraph shape** (`CLAUDE.md:10-30`, the kokoro-binding
   paragraph): capability, what shipped per implementation, the measured numbers with
   "recorded, never gated", the posture sentence ("No gate passes; no user-visible surface
   ships in this unit"), and `Test floor: <N>.` One paragraph, self-dated, inserted as the
   newest unit above the kokoro-binding paragraph.
4. **The numbers the SMOKE rows carry** (from the PRD/ROADMAP/ARCHITECTURE; budgets belong
   to the uncleared P3 gate):
   - **131** — ≥95% correct turn commitment on the founder-recorded conversational set,
     false cutoffs weighted **5× worse** than late commits (`ROADMAP.md:211`,
     `ARCHITECTURE.md:575`). The harness scores in CI over the scripted corpus; the real set
     is the founder's recording (interview decision, `_card/understanding.md:92`).
   - **132** — barge-in halt **≤200 ms** on real playback (`ROADMAP.md:210`), with the
     composed path: real VAD/EOU **or the honest fallback**, real Kokoro/System playback
     (PRD G2 `prd.md:46-52`; `ARCHITECTURE.md:571`'s decomposition: VAD frame → cancel ≤50 ms
     → duck → stop). First real execution of the output path (`playback-ducking` plan's
     honest-CI statement).
   - **133** — echo rejection on speakers: **0 instances of Vocca transcribing its own
     output** (`ROADMAP.md:212`), with the `ARCHITECTURE.md:727` caveat recorded (some
     hardware may need more than reference cancellation). Never headphones; the step's
     void clause guards it.
   - Each is **recorded, never gated** — an over-budget number is recorded verbatim, never a
     pass (the 130 wording precedent: "an over-budget number is recorded verbatim, never
     gated").
5. **The real-run command precedent** (SMOKE 129/130): `VOCCA_RUN_REAL_SPEECH=1 swift test
   --filter <Suite>` env-gated filtered runs; model directories from the provision scripts
   (`Scripts/provision-kokoro-fixtures.sh`; C10's `Scripts/provision-vad-fixtures.sh` +
   `Sources/VoccaASR/Models/Manifests/silero-vad.json` from `sdk-vetting`). The C10 composed
   real suite is the `barge-in-loop` aspect's env-gated two-variable gate — **the exact
   filter name, env var names, corpus name and printed row tags are taken from the landed
   `barge-in-loop` plan, verbatim**. The row format follows the house tag=value shape
   (`KOKORO-TTFA 232.5ms fixture=… baseline=… budget=… recorded-never-gated`).
6. **The seam-table corrections** (Phase 2 research finding, confirmed against
   `docs/technical/ARCHITECTURE.md` on this tree):
   - Row "Capture" (`:256`) names `AudioCapture` / `PushToTalkCapture, StreamingCapture (C10)`
     — stale C1-era names. The shipped family: `SessionAudioSource` +
     `MicrophoneSource` (dictation) and the C10 `ContinuousAudioSource` + `StreamingCapture`
     (`streaming-capture` plan's seam-shape decision, recorded verbatim there so this aspect
     does not re-derive it).
   - Row "TTS" (`:261`) names `KokoroSynthesizer` — stale; the shipped class is
     `KokoroEngine` (`VoccaSpeech/Kokoro/KokoroEngine.swift`).
   - Rows "VAD" (`:262`) / "Turn detection" (`:263`) — verify the shipped adapter names from
     the landed `sdk-adapters` work (the EOU-shape correction from `sdk-vetting`: the EOU is
     ASR-integrated, not a free-standing call — if the shipped `TurnDetector` implementation's
     name differs from `ParakeetEOU`, correct the row to the shipped name).
7. **The reservations that become real** (`ARCHITECTURE.md:107-108`): `VoccaAudio/Playback/`
   gains its first file (`SystemPlayback.swift`, `playback-ducking`); `VoccaAudio/VAD/` —
   record the truth of the tree: if the VAD/EOU adapters landed in `VoccaASR` (the H8b
   confinement), the `:108` placement comment is corrected to the shipped truth, never
   claimed as real if it stayed a reservation.
8. **The two-engine caution** (`ARCHITECTURE.md:344`): "an app switching between output-only
   and input-output configurations may want **two engine instances**" — C10 resolves it: the
   playback path ships a **separate `AVAudioEngine`** (`SystemPlaybackOutput`, `playback-ducking`
   Vetted fact 6 / O4); the naive reuse of the capture engine was not taken. The `:344` note
   gets the C10 resolution line.
9. **The floor** (`Scripts/test-with-floor.sh:1639-1666`): `MINIMUM_EXECUTED_TESTS` ratchets
   per aspect with the tests it counts; env-gated suites' visible skips count as executed. At
   record time floor == the last aspect's observed total. The record **never edits the
   script**; it runs it, reads the executed count `E` and the floor `N`, records
   `Floor N (executed E)`, and surfaces — never edits — a mismatch.
10. **The unit's honesty obligations** (binding, `_card/issue.md:42-56`): no P2/P3 gate
    passes; no user-visible surface ships; numbers recorded never gated; the dictation path
    untouched (G5 pin from `streaming-capture` Phase 3); zero network; a seam with one
    implementation is an interim state recorded, never papered over (`PlaybackEngine` one
    implementation, `ContinuousAudioSource` one conformance — both recorded).

---

## Phase 1 — SMOKE steps 131-133 (the rows, written runnable)

**Goal.** The three C10 rows appended to `docs/SMOKE_CHECKLIST.md` as a new section 19 after
step 130 and before "When this file is wrong" (`:2770`), each in the step 129/130 shape with
the **exact gesture commands cited from the landed sibling plans** and the state-entered
checks per rule 1. The rows are written **runnable, executed-later**: they state the
measurement, the command, the budget, and the recorded-never-gated contract; whether the
founder has run them by merge time is the Edge case 3 question, not a Phase 1 concern.

**Files.** `docs/SMOKE_CHECKLIST.md` — insert between step 130 (`:2766`) and the `---` /
"When this file is wrong" (`:2768-2770`):

- Section header in the house shape: `## 19. The turn-taking loop — \`turn-taking-barge-in\``
  with a short intro: the loop ships as machinery (no user-visible surface — the CONVERSING
  state is C11's), the realtime conversation is executed by nothing in CI (the tap-adapter
  precedent), and these three rows are the founder's machine producing the first real
  observations, recorded never gated, each under rule 1.
- **131. The first real conversational set — turn commitment on the founder-recorded set (C10, recorded — never gated).**
  - *Gesture:* the env-gated harness over the founder-recorded set with human-labelled turn
    boundaries — `VOCCA_RUN_REAL_TURN=1 <model-dir vars from the barge-in-loop plan's Testing
    strategy> swift test --filter <the composed suite filter, verbatim from the landed
    barge-in-loop plan>` — the VAD/EOU artifacts provisioned by
    `Scripts/provision-vad-fixtures.sh` (the `sdk-vetting` aspect's script), the set name as
    the barge-in-loop plan names it.
  - *Verify the state was entered:* the env-gated rows did **not** print their skip message
    (the skip is the tell-tale — a skipped test ran nothing), the scored commitments came
    from the **founder-recorded set**, not the scripted CI corpus (the row names the set),
    and the weight is the 5× false-cutoff weight.
  - *Pass:* the harness's row recorded verbatim with the never-gated note — ≥95% correct
    turn commitment, false cutoffs weighted 5× worse than late commits
    (`ROADMAP.md:211`, `ARCHITECTURE.md:575`). Below 95% is **recorded verbatim, never a
    pass** — the row wording must say so.
  - *Failure:* a skipped run recorded as a number, a number recorded as a gate pass, the row
    missing the never-gated note, or a score computed over the wrong set.
- **132. The first real barge-in halt on real playback (C10, recorded — never gated).**
  - *Gesture:* the barge-in-loop plan's env-gated composed suite over the composed real path
    — real VAD/EOU **or the honest fallback** (recorded which), real Kokoro/System playback
    (recorded which renderer) — scripted user-speech injection during real playback; command
    verbatim from the landed plan. First real execution of the output path (`playback-ducking`
    plan's hand-off).
  - *Verify the state was entered:* no skip message; the halt row shows a measured number,
    not a placeholder; the renderer actually rendered (the row names it); capture ran
    continuously (interrupting audio preserved).
  - *Pass:* the measured row recorded verbatim with the never-gated note — halt **≤200 ms**
    (`ROADMAP.md:210`) over the composed path (VAD frame ~30 ms → cancel ≤50 ms → duck →
    stop, `ARCHITECTURE.md:571`). An over-budget number is **recorded verbatim, never a
    pass**.
  - *Failure:* a skipped run recorded as a number, a number recorded as a gate pass, the row
    missing the never-gated note, or a halt measured without real playback.
- **133. The first echo rejection on speakers (C10, recorded — never gated).**
  - *Gesture:* run the loop (the env-gated composed route) with playback to the machine's
    **speakers** and the mic live; verify **0 instances of Vocca transcribing its own
    output** (`ROADMAP.md:212`); the caveat that some hardware may need more than reference
    cancellation (`ARCHITECTURE.md:727`) is recorded beside the row.
  - *Verify the state was entered:* the playback actually sounded through the speakers (not
    headphones, not muted), the capture was live, and the count covers a real run.
  - *Pass:* the row recorded verbatim with the never-gated note: 0 instances on speakers,
    the `:727` caveat attached.
  - *Void — not fail — if:* the run was headphone-only, muted, or the state was otherwise not
    entered (rule 1 — a voided run is never recorded as a number).
  - *Failure:* ≥1 instance of Vocca transcribing its own output (the zero-tolerance metric).

**Validation.**
1. Read the three rows back against the landed plans: every *Gesture:* command is the landed
   plan's own wording (filter, env vars, provision script), every *Pass:* carries the
   `recorded-never-gated` token and its budget, every row has *Verify the state was
   entered:*, and step 133 has its *Void* clause.
2. The steps' numbering continues 129/130 → 131/132/133 with no gap.
3. `git diff --stat` shows exactly `docs/SMOKE_CHECKLIST.md` (plus the already-dirty planning
   docs) — no `Sources/`, no `Tests/`, no `Scripts/`.

**Commit message.** `record: SMOKE steps 131-133 — the turn-taking loop's real conversational set, barge-in halt, and echo rejection rows`

---

## Phase 2 — the STATUS head entry and the CLAUDE.md status paragraph

**Goal.** The unit's record in the two always-read files, in the kokoro-binding pattern, with
**real numbers** — the floor script is run first, and the entries carry its printed numbers,
never placeholders.

**Files.**
- `docs/STATUS.md` — a head entry inserted after the `---` (`:11`), before the kokoro-binding
  entry (`:13`). Shape (the kokoro-binding pattern, `:13-103`):
  - **Bold-led summary** — e.g. `**The \`turn-taking-barge-in\` unit's machinery shipped
    2026-09-15 — C10's seams, loop and echo gate are real; no gate passes, no user-visible
    surface.**` `feat/turn-taking-barge-in/aliz`.
  - **What shipped, per aspect** — one sentence per aspect, written from the **landed
    commits**, not the PRD: `sdk-vetting` (the six findings, the EOU-shape correction, the
    version decision, the silero manifest), `voice-detection` (the two seams + the two pure
    fallbacks + the lint), `sdk-adapters` (the real VAD/EOU adapters, H8b amendment, the
    env-gated suite), `streaming-capture` (`ContinuousAudioSource` + `StreamingCapture`, the
    no-touch pin), `playback-ducking` (`PlaybackEngine` + `PlaybackLevel` + `SystemPlayback`
    + the reviewed AVFoundation amendment, the offline-render outcome — **green, or removed
    with the environmental reason recorded**, per its fallback rule), `barge-in-loop` (the
    loop, the echo gate, the harness + scripted corpus, `PROBE-TURN`), and this `record`
    aspect. Where a sibling plan promised a hand-off (the offline-render outcome, the
    `voice-detection` lint's seam-name rows amendment, the ARCHITECTURE table-row
    correction), the entry records what **actually** happened.
  - **Measured, recorded never gated** — the SMOKE 131-133 rows verbatim if the founder ran
    them (Phase 1's rows are the format); if not yet run, the entry says exactly that: the
    steps are written and runnable; the executed rows land when the founder runs them (Edge
    case 3). The headless/CI numbers the unit owns (turn-commitment over the scripted corpus,
    the composed headless halt, the echo-gate headless proof) are recorded as CI facts with
    their margins, never as gate passes.
  - **The honesty block** — the closing bullets: no P2/P3 gate passes (the P3 gate needs a
    full spoken exchange — C11's surface; P2 unchanged); numbers recorded never gated;
    no user-visible surface ships (no silent listening state — continuous capture is composed
    only in probe/suites until C11); the dictation path untouched (the byte-level pin);
    zero network (interposer green, nothing hands a URL to any SDK surface, artifacts
    provision through the C2 store); interim states recorded, not papered over
    (`PlaybackEngine` one implementation; `ContinuousAudioSource` one conformance; the EOU's
    ASR-integrated shape).
  - **The floor line** — `Floor <N> (executed <E>)` from the Phase 2 floor run.
- `CLAUDE.md` — one new status paragraph, inserted as the newest unit above the kokoro-binding
  paragraph (between the header blockquote's blank `>` at `:9` and `:10`), and the header's
  date `Status (2026-09-14)` (`:5`) updated to the commit's date (expected 2026-09-15). The
  paragraph carries the same substance in the kokoro-binding wording pattern: the unit's
  capability, what shipped per aspect (compressed), the SMOKE rows' measured numbers (or the
  "steps written, founder runs them" state), the posture sentence — "**No gate passes; no
  user-visible surface ships in this unit** (the converse surface is C11's)" — and
  `Test floor: **<N>**.` Self-dated like every paragraph; history below it untouched.

**Validation.**
1. **Run `./Scripts/test-with-floor.sh` first** — full run, green; record the printed
   executed count `E` and the floor constant `N` at `Scripts/test-with-floor.sh:1639`. Both
   entries are written with those numbers.
2. Read both entries against Vetted fact 10's checklist: every honesty obligation has a line;
   no sentence claims a gate pass, a user-visible surface, or a number CI did not produce.
3. `git diff --stat` — exactly `docs/STATUS.md`, `CLAUDE.md`, and the already-dirty planning
   docs.

**Commit message.** `record: the C10 unit record — STATUS head entry and the CLAUDE.md status paragraph`

---

## Phase 3 — the ARCHITECTURE.md sync, the floor verification, the honesty-block check

**Goal.** `docs/technical/ARCHITECTURE.md` describes the tree this unit's close ships in:
the seam-table corrections, the reservations now real, the two-engine caution resolved; the
floor verified (not ratcheted); the honesty block confirmed complete.

**Files.** `docs/technical/ARCHITECTURE.md` — the named edits, each verified against the tree
before writing:

1. **The seam table** (`:254-265`):
   - Row "Capture" (`:256`) — replace the stale `AudioCapture` / `PushToTalkCapture,
     StreamingCapture (C10)` with the shipped family, using the `streaming-capture` plan's
     recorded correction (Vetted fact 6): the dictation seam `SessionAudioSource` +
     `MicrophoneSource`, and the C10 seam `ContinuousAudioSource` + `StreamingCapture` (one
     row naming both pairs, or two rows — the content, not the count, is the correction). The
     `(C10)` suffix drops: C10 shipped.
   - Row "TTS" (`:261`) — `KokoroSynthesizer` → **`KokoroEngine`** (the shipped class).
   - Rows "VAD" / "Turn detection" (`:262-263`) — annotate as shipped: the real adapters
     landed (per `sdk-adapters`); if the shipped `TurnDetector` implementation's name differs
     from `ParakeetEOU` (the EOU is ASR-integrated — the `sdk-vetting` correction), the row
     names the shipped name, never the assumed one.
2. **The reservations** (`:107-108`): `Playback/` gains the now-real note (its first file,
   `SystemPlayback.swift`, C10); `VAD/` is annotated per the truth of the tree — if the
   adapters live in `VoccaASR` (H8b), the placement comment is corrected to say so; it is
   never claimed real if it stayed a reservation.
3. **The two-engine caution** (`:344`): append the C10 resolution line — the playback path
   ships a separate `AVAudioEngine` (`SystemPlaybackOutput`), the naive reuse of the capture
   engine was not taken; the caution's "worth remembering at C9" is closed.
4. **The §14 harness table** (`:685`, the "Conversational set" row): annotate as shipped —
   the scorer with the 5× weighting runs in CI over the scripted corpus; the founder-recorded
   set is SMOKE 131, recorded never gated.
5. **Open question 4** (`:727`, echo on speakers): when the SMOKE 133 row is executed, append
   a pointer to the recorded row; until then, leave the question open exactly as written —
   this unit's answer is the deterministic gate + SMOKE verification, never a claim.

**Floor verification (no edits).**
1. `./Scripts/test-with-floor.sh` from the worktree root — green; the executed count `E` and
   floor `N` print. Verify `N == E` (the record adds no tests, so the floor and the executed
   count are the same number the last aspect ratcheted to).
2. **If `N != E`**: do NOT edit `Scripts/test-with-floor.sh` — the doc-sync rule names the
   truth, not the chase; surface the discrepancy to the integrator and record both numbers
   honestly (Edge case 5).

**Honesty-block check.** Re-read the Phase 2 entry against Vetted fact 10 and the SMOKE rows
against Vetted fact 4: every obligation has a line, every row carries the
`recorded-never-gated` token, nothing in the tree's record claims a gate pass. The
`git diff --stat` of the whole aspect (its three commits) lists no `Sources/`, no `Tests/`,
no `Scripts/` change — the unit's code shipped in the sibling aspects' commits; this aspect's
diff is docs only.

**Commit message.** `record: sync ARCHITECTURE.md to the shipped C10 seams and reservations, and verify the floor`

---

## File-by-file build order

| File | Edit | Where it lands | Exports |
|---|---|---|---|
| `docs/SMOKE_CHECKLIST.md` | Phase 1: the section 19 header + steps 131-133, each with *Gesture / Verify the state was entered / Pass / Failure* (+ *Void* for 133) | after step 130 (`:2766`), before the `---` + "When this file is wrong" (`:2768-2770`) | the three recorded-never-gated rows |
| `docs/STATUS.md` | Phase 2: the head entry — bold-led summary, per-aspect shipped, measured rows, honesty block, floor line | after the `---` (`:11`), before the kokoro-binding entry (`:13`) — newest first | the unit record |
| `CLAUDE.md` | Phase 2: the new status paragraph + the header date (`:5` `2026-09-14` → the commit's date) | between the header blockquote's blank `>` (`:9`) and the kokoro-binding paragraph (`:10`) | the always-loaded unit summary |
| `docs/technical/ARCHITECTURE.md` | Phase 3: seam-table rows `:256`, `:261`, `:262-263`; reservations `:107-108`; the `:344` resolution note; the §14 harness row `:685`; the `:727` pointer (when executed) | the sections named | the authoritative-tree sync |
| (untouched, by design) | `Scripts/test-with-floor.sh`, `Package.swift`, every `Sources/` file, every `Tests/` file | — | — |

## Testing strategy

- **This aspect adds no tests.** There is no RED/GREEN and no filtered run; the validation is
  the doc-consistency read and the floor script:
  - `./Scripts/test-with-floor.sh` — twice (Phase 2 to read the numbers the entries record,
    Phase 3 to verify `N == E`). The record does not change the executed count, so a green
    run at the unchanged floor is the whole test.
  - The doc-sync read: STATUS head, CLAUDE.md paragraph and ARCHITECTURE's named sections all
    describe the same tree (the same floor number, the same seam names, the same interim
    states) — the "describe the state of the tree this file ships in" rule.
  - The SMOKE rows' internal consistency: the budgets and weights match ROADMAP/ARCHITECTURE
    (≥95%, 5×, ≤200 ms, 0 instances), and the commands match the landed plans' wording.
- **What CI structurally cannot cover here** is stated, not hidden: the rows themselves (real
  VAD/EOU, real playback, speakers) are founder-run by definition — that is why they are
  SMOKE rows with the recorded-never-gated token, and why the skip message is the tell-tale.

## Environment & privacy

- **Docs-only: no code, no network, no model bytes, no runtime of any kind in this aspect.**
  The only executed command is the floor script (a local `swift test` run).
- The SMOKE rows **never claim egress**: every gesture runs local suites over locally
  provisioned artifacts (the C2 store / `Scripts/provision-vad-fixtures.sh`, founder-run at
  provisioning time — the kokoro checkpoint pattern); nothing in the rows hands a URL to any
  SDK surface, and the zero-network invariant stays covered by the suite the floor script
  runs.
- The rows carry no transcript text and no audio: they record numbers and outcomes, matching
  the usage-ledger byte-level pin's spirit (`docs/STATUS.md`, daily-use-ledger entry).

## Edge cases

1. **An over-budget real number** — the row wording must handle it before it happens: each
   *Pass:* says the measured row is recorded **verbatim** with the never-gated note, and "an
   over-budget number is recorded verbatim, never a pass" (the 129/130 wording). A 92%
   commitment (131), a 340 ms halt (132) or 1+ echo instance (133) are all *recorded rows*,
   never failures of the record aspect and never gate passes.
2. **A voided SMOKE run** — rule 1: a run whose state was not entered (skip message printed —
   the env gate did not engage; headphones instead of speakers for 133; muted/silent
   playback for 132) is **void, not fail**: it is never recorded as a number, and the STATUS
   entry says the row awaits a valid run. The steps' *Verify the state was entered:* fields
   and 133's *Void — not fail — if:* clause carry this.
3. **The founder has not run the steps by merge time** — the rows are written runnable,
   executed-later; the STATUS/CLAUDE entries say exactly that ("steps 131-133 written; the
   executed rows land when the founder runs them"). Precedent: steps 129/130 were executed at
   unit close; that is the *desired* timing here, not a requirement. **This aspect's merge
   does NOT depend on the steps having been executed** — recommended, and the integrator may
   decide otherwise (Agent notes, checkpoint 2). A non-executed step is never recorded as a
   pass, never as a fail — it is an unrun step, stated as such.
4. **The `barge-in-loop` plan has not landed** at record time — STOP and surface; this
   aspect's rows cannot cite suite names it would have to invent. (Should not occur: record
   runs last by sequencing, `prd.md:245-247`.)
5. **Floor mismatch at verification** (`N != E` at Phase 3) — record both numbers honestly in
   the STATUS entry, surface to the integrator, and never edit the script. The doc-sync rule
   is about describing the tree, not correcting it.
6. **A sibling hand-off that did not happen** — e.g. the `voice-detection` lint's seam-name
   rows were not amended by `barge-in-loop`, or the playback offline-render fallback was
   taken without a record: the STATUS entry records the **actual** state (including the
   omission) rather than the promised one. Never paper over a gap in this unit's record.
7. **The env-gated suite's visible skips count as executed** — the floor arithmetic assumes
   it; the record's floor line uses the script's printed executed count, which includes them.
   The SMOKE rows, by contrast, require real executions: the skip message is the tell-tale.

## Agent notes

- **Checkpoint 1 (after Phase 1):** the three SMOKE rows read back against the landed plans —
  the integrator reviews that every *Gesture:* is the landed wording and every budget is the
  PRD/ROADMAP number. Surface the row drafts before Phase 2.
- **Checkpoint 2 (the founder check):** the SMOKE steps are ready to run on the founder's
  machine. The recommended merge posture: **this aspect merges without requiring the runs**
  (Edge case 3) — the rows exist as runnable, executed-later steps; the founder runs them
  after the unit merges, and the executed rows land as follow-up edits to the SMOKE section
  and STATUS entry. If the integrator prefers the rows executed before merge, the founder
  runs them during this aspect — recorded as the unit's final state either way.
- **Checkpoint 3 (after Phase 3):** the ARCHITECTURE diff reviewed (seam rows, reservations,
  `:344`, harness row), the floor verified (`N == E`, or surfaced), and the final `git log`
  shows this aspect's three commits on top of the six sibling aspects'.
- **Merge order:** `record` is the unit's last aspect; after its merge the unit closes — the
  branch merges to `master`, the unit's record is complete, and the P3 gate stays uncleared
  with the posture named.
- **STOP and surface** (never improvise): the `barge-in-loop` plan absent (Edge case 4); any
  ARCHITECTURE fact that contradicts what shipped (a seam name, a directory, an
  implementation count) beyond the recorded corrections; any instruction to edit
  `Scripts/test-with-floor.sh`; any temptation to "fix" a sibling aspect's state from the
  record aspect.
- **Do NOT touch** (by design): every `Sources/` file, every `Tests/` file, `Package.swift`,
  `Scripts/test-with-floor.sh` (the floor line included), the dictation path, and the
  already-shipped planning docs' history — the record appends; it does not rewrite.
- **Optional consistency check (flag, don't silently skip):** if no sibling aspect updated
  `docs/technical/CAPABILITY_ROADMAP.md`'s C10 row to shipped (the kokoro-binding sync-commit
  precedent, `71a4ee6`), flag it to the integrator in checkpoint 3 — the record can carry it
  in Phase 3 if the integrator wants the roadmap row closed with the unit.