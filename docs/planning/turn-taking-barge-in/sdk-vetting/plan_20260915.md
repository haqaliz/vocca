# Plan: sdk-vetting — 2026-09-15

> Aspect of `turn-taking-barge-in` (C10, the unit's FIRST aspect and first commit). Requirements:
> `docs/planning/turn-taking-barge-in/prd.md` R1 + G1 + risks O1/O2/O6. Understanding:
> `docs/planning/_card/understanding.md` (the four ratified decisions and the four unverified
> items at `:72-82` — this gate exists to close them). Precedent — the house vetting-gate shape:
> `docs/planning/kokoro-binding/port-vetting/spec.md` + `plan_20260914.md` (six verification
> facts, the record in `_card/understanding.md`, the provenance pin RED first, the floor ratchet
> in the same commit as the tests it counts). Executed by one agent under strict TDD, on branch
> `feat/turn-taking-barge-in/aliz`.
>
> Suite command: full runs `./Scripts/test-with-floor.sh` from the repo root; filtered runs use
> BARE `swift test --filter <X>` — never through the floor script (it misreads a filtered run's
> executed count). Floor: **1978** (`Scripts/test-with-floor.sh:1639`). The floor constant is
> never raised in passing — it ratchets only in Phase 3's commit, with the exact executed count
> from a full green run.

## Setup

- Worktree: `/Users/aliz/dev/at/vocca/.claude/worktrees/feat-turn-taking-barge-in` (branch
  `feat/turn-taking-barge-in/aliz`, tracking `origin/master`). Local toolchain: Swift 6.3.3 /
  Xcode 26.6; CI `XCODE_MAJOR: "26"`. **No toolchain work is expected in this aspect** (see the
  toolchain finding below).
- **The worktree has no `.build` yet.** First action: `swift package resolve` from the worktree
  root (dependency fetch at plan time is fine — see Environment & privacy). This writes
  `Package.resolved` (gitignored — `.gitignore:7`; never commit it) and lands the SDK sources at
  `.build/checkouts/`. The FluidAudio checkout directory is `.build/checkouts/FluidAudio/` —
  verify the directory name from the worktree's own `Package.resolved` after resolve (identity
  `fluidaudio`) rather than assuming it.
- Fallback read path (reference ONLY if the worktree resolve fails and you state so in the
  record): the primary checkout's already-resolved SDK at
  `/Users/aliz/dev/at/vocca/.build/checkouts/FluidAudio/` — resolved there on 2026-09-15 to
  **version 0.15.5**, revision `19600a485baa4998812e4654b70d2bab8f2c9949`. The vetting record
  must cite the WORKTREE's own checkout, not the fallback.
- The uncommitted planning docs already on the branch (`docs/planning/_card/issue.md`,
  `docs/planning/_card/understanding.md`, `docs/planning/turn-taking-barge-in/prd.md`) land in
  this aspect's first commit together with the pin test and the record — the unit's first
  commit carries its planning docs (house precedent).

## Vetted facts (already recorded — do not re-research)

The four founder-ratified decisions (`_card/understanding.md:84-92`):

1. **VAD implementation**: FluidAudio `VadManager` (the Parakeet precedent — the dependency is
   already pinned, no new package), not a separate Swift port; `EnergyVAD` stays the
   fallback/test implementation.
2. **Posture**: seam-only, the C9 posture — no activation, no widget state, no user-visible
   surface.
3. **Echo depth**: energy-correlation gate + playback-window gating (headless-testable);
   reference cancellation as the second line; speakers verification is SMOKE, never CI.
4. **Conversational set**: scripted/synthetic corpus + scoring harness (5× false-cutoff weight)
   runs in CI; the founder-recorded human-labelled set is SMOKE 131.

Also already pinned (not re-litigated): FluidAudio `from: "0.12.4"` (`Package.swift:36`),
confined to `VoccaASR` by the H8b lint; the barge-in 200 ms budget and the ≤50 ms cancel
contract are load-bearing inputs from C9.

## Phase 1 — RED: the provenance pin

- **Goal:** a failing test suite that pins (a) the FluidAudio dependency edges as manifest
  facts and (b) the shipped Silero VAD artifact manifest — (b) is the RED.
- **Files:**
  - Create `Tests/HarnessTests/SileroVadProvenanceTests.swift` — mirror
    `Tests/HarnessTests/KokoroDependencyTests.swift` exactly (same header, same
    `PackageRootLocator.find(from: #filePath)` pattern, same `PackageManifest.load` helper with
    its isolated scratch path — that helper cannot drift from what SwiftPM believes):
    - `testPackageDeclaresTheFluidAudioPackageURL` — raw `Package.swift` text (the only place
      SwiftPM records package-level URLs) must contain
      `https://github.com/FluidInference/FluidAudio.git` and the range `from: "0.12.4"`.
      (Already green — a family pin, the KokoroDependencyTests shape.)
    - `testVoccaASRTargetDependsOnTheFluidAudioProduct` — `PackageManifest` dump output: the
      `VoccaASR` target's dependencies must include the `FluidAudio` product. (Already green —
      the H8b confinement, pinned as a manifest fact.)
    - `testShippedSileroManifestPinsTheVadModel` — **the RED.** Read
      `Sources/VoccaASR/Models/Manifests/silero-vad.json` (via `packageRoot`), decode as JSON
      (`JSONSerialization`; the `ModelManifest` decoder is EngineTier-keyed and out of scope —
      parse the raw file), and assert: `engineID == "silero-vad"`, `version == "1"`, and
      `files` contains one entry named `silero-vad-unified-256ms-v6.2.1.mlmodelc` whose
      `sha256` is 64 lowercase hex chars and whose `byteCount` is positive. The file name is
      asserted as recorded in Phase 2 (see the artifact finding); it must equal the name the
      resolved SDK actually demands.
- **Validation:** bare `swift test --filter SileroVadProvenanceTests` → the suite compiles;
  the two family pins PASS, `testShippedSileroManifestPinsTheVadModel` FAILS (no such file).
  Confirm the failure is the assertion (missing manifest), not a compile error. Do not run this
  through `test-with-floor.sh`.
- **Commit:** none — the RED is the checkpoint.

## Phase 2 — GREEN + record: the vetting reads, the corrections, the manifest

- **Goal:** the six verification facts read from the SDK's code (not its README — the
  Misaki-correction precedent), corrections recorded verbatim, the version-pin decision made,
  the manifest + digests from actual bytes, the pin suite green.

### Step 2.1 — the SDK reads (each becomes a numbered finding in the record)

Read each file under the worktree's `.build/checkouts/FluidAudio/` and record the finding with
the `file:line` observed and the verbatim quote. The expected shapes below were observed in the
primary checkout's resolved 0.15.5 — re-verify every one in the worktree's own checkout:

1. **`VadManager` API shape** — `Sources/FluidAudio/VAD/VadManager.swift`: a `public actor`;
   `public static let chunkSize = 4096` (256 ms @ 16 kHz), `sampleRate = 16000`,
   `isAvailable`; `process(_:)` overloads over `URL` / `AVAudioPCMBuffer` / `[Float]`;
   THREE inits: `init(config:vadModel:)` (pre-loaded `MLModel` — the store-compatible staging
   path), `init(config:modelDirectory:progressHandler:)` (directory staging), and
   `init(config:progressHandler:)` (the ModelHub download path — the one the app must never
   take). Record the **Beta Status** doc comment verbatim — it is a finding (adapter risk
   note), not a blocker. Streaming surface: `Sources/FluidAudio/VAD/VadManager+Streaming.swift`
   (`makeStreamState()`, `processStreamingChunk(...)`). Segmentation:
   `VadManager+SpeechSegmentation.swift`.
2. **Hysteresis exposure (O6/S1)** — `Sources/FluidAudio/VAD/VadTypes.swift`: `VadConfig`
   (`defaultThreshold` 0.85, `debugMode`, `computeUnits` default `.cpuAndNeuralEngine`) and
   `VadSegmentationConfig` (`minSpeechDuration` 0.15, `minSilenceDuration` 0.75,
   `maxSpeechDuration` 14.0, `speechPadding` 0.1, `silenceThresholdForSplit` 0.3,
   `negativeThreshold`/`negativeThresholdOffset` — the hysteresis pair —
   `effectiveNegativeThreshold(baseThreshold:)`). Expected verdict for O6: hysteresis IS
   exposed as plain `Sendable` data, injectable from the composition root — the
   wrap-with-injectable-thresholds fallback is not needed; record which of the two the adapter
   aspect must use.
3. **EOU availability and shape (O2)** —
   `Sources/FluidAudio/ASR/Parakeet/Streaming/EOU/StreamingEouAsrManager.swift`: expected
   PRESENT at the resolved version — `StreamingChunkSize` (`ms160` default, `ms320`,
   `ms1280`), model files via `ModelNames.ParakeetEOU.requiredModels` =
   `streaming_encoder.mlmodelc`, `decoder.mlmodelc`, `joint_decision.mlmodelc`, `vocab.json`
   (`Sources/FluidAudio/ModelNames.swift:501-514`). **The shape correction to record
   verbatim:** the EOU is ASR-integrated (it streams the Parakeet ASR encoder), not a
   standalone turn-decision call — this differs from the PRD's assumption of a `ParakeetEOU`
   adapter as a free-standing `TurnDetector` implementation; the `sdk-adapters` aspect must
   plan around `StreamingEouAsrManager`'s shape. EOU present → no "pending" state is recorded
   now; if a later aspect finds the integrated shape unusable as a standalone seam
   implementation, THAT interim state is recorded then (the C9 first-half precedent). If the
   EOU surface is instead ABSENT in the resolved version — the O2 fork — record it verbatim
   and proceed per Edge case 1; do NOT invent an EOU.
4. **Version coverage (O1)** — read the WORKTREE's `Package.resolved` after the setup resolve:
   expected `fluidaudio` → `0.15.5` (revision `19600a485baa4998812e4654b70d2bab8f2c9949`, the
   primary checkout's resolved value on 2026-09-15). If the resolved version's sources carry
   findings 1-3, the pinned range `from: "0.12.4"` ALREADY carries the VAD/EOU surface → **no
   bump**; record the resolved version + revision. If the resolved version lacks the surface →
   the pin must bump to the carrying version (a reviewable CI/dependency decision like the
   Xcode 26 bump) — land the bump in `Package.swift` and re-resolve. **If the resolved version
   differs from 0.15.5 and the surface verification is ambiguous — STOP and surface to the
   integrator (Edge case 2); never adapt silently.**
5. **The VAD model artifact** — `Sources/FluidAudio/ModelNames.swift:491-496`:
   `ModelNames.VAD.sileroVad = "silero-vad-unified-256ms-v6.2.1"`,
   `sileroVadFile = silero-vad-unified-256ms-v6.2.1.mlmodelc`, `requiredModels = [sileroVadFile]`;
   repo `case vad = "FluidInference/silero-vad-coreml"` (`ModelNames.swift:5`). Staging shape
   from `Documentation/VAD/GettingStarted.md:37-78`: the `.mlmodelc` DIRECTORY staged anywhere,
   handed to `VadManager(config:vadModel:)`; quote verbatim the doc's "no fallback download
   attempts occur" line (`GettingStarted.md:78`). Two sub-checks: (a) **bundled?** — search the
   SDK checkout for the `.mlmodelc`; expected NOT bundled (the SDK's ModelHub downloads when
   missing — which is why the adapter must use the pre-loaded init and the C2 store, never the
   download init); (b) **32 ms variant** — search the SDK source for `32ms`; expected
   unreferenced (only the 256 ms unified model is named at v6.2.1). Record either outcome.
6. **License** — re-verify the SDK's own `LICENSE` file is Apache-2.0 (verified verbatim at the
   primary checkout); then verify the artifact repo `FluidInference/silero-vad-coreml`'s
   license — expected **MIT** (Silero VAD); verify from the repo's own LICENSE, verbatim, not
   from a README claim. If it is not MIT — STOP and surface (Edge case 4).
7. **Toolchain** — the SDK's `Package.swift` declares `swift-tools-version: 6.0` and
   `platforms: [.macOS(.v14)]` (expected; re-verify): matches our 6.0, below our `.v15` — no
   CI toolchain change, no platform bump.

### Step 2.2 — the record

- Append a `## Vetting record (sdk-vetting, 2026-09-15)` section to
  `docs/planning/_card/understanding.md`: the six findings, each as fact → `file:line` →
  verbatim quote, plus the EOU-shape correction and the version-pin decision. Where an SDK
  fact contradicts a claim already in the understanding note (e.g. the exact
  `init(config:vadModel:)` signature or the EOU shape), edit that claim in place with the
  correction marked — corrections are recorded VERBATIM, never paraphrased (the
  Misaki-correction precedent).

### Step 2.3 — the manifest from actual bytes

- Create `Scripts/provision-vad-fixtures.sh` in the `Scripts/provision-kokoro-fixtures.sh`
  shape (same header, same flags, same printed-output contract): downloads the artifact file
  `silero-vad-unified-256ms-v6.2.1.mlmodelc` from `FluidInference/silero-vad-coreml` (the
  HF repo; use the exact packaging the repo actually ships — bare `.mlmodelc` directory entry,
  or a tarball if that is what the repo publishes — recorded finding), lands it at the C2 store
  shape `<root>/silero-vad/1/<sdkDirectory>/`, marker-guarded and idempotent, prints the digest
  commands' output, and writes `<root>/silero-vad/1/manifest.json` — commit its content as the
  shipped manifest.
- **The founder runs the script once** (network at provisioning time is fine — the app's code
  never downloads; this is the kokoro checkpoint: the agent prepares the script, the founder
  runs it, the digests land in the manifest commit — surface the checkpoint to the integrator
  before this step). Exact digest commands, run against the ACTUAL provisioned bytes:
  - `shasum -a 256 <artifact>` → the `sha256` field
  - `stat -f%z <artifact>` (or `wc -c < <artifact>`) → the `byteCount` field
  - Never take either number from a README or a web page.
- Commit the script's output as `Sources/VoccaASR/Models/Manifests/silero-vad.json` in the
  kokoro-82m.json pattern: `engineID "silero-vad"`, `version "1"`, `sdkDirectory "vad"`,
  `files: [{name, sha256, byteCount}]` (tarball entry + sdkDirectory if the artifact ships as
  a tarball — follow kokoro's shape then). The manifest is a bundle resource
  (`Package.swift` declares `Models/Manifests` as `.copy` — no Package.swift resource change
  is needed; verify the existing declaration covers the new file).

### Step 2.4 — the version pin

- Only if finding 4 says bump: edit `Package.swift:36`'s range, re-run `swift package resolve`
  (Package.resolved stays gitignored — never committed), and note the bump in the record as
  the reviewable decision. Otherwise leave the pin untouched and record why.

- **Validation:** bare `swift test --filter SileroVadProvenanceTests` → 3/3 pass. Then a FULL
  `./Scripts/test-with-floor.sh` run — it must stay green (1978 floor) and its log's executed
  count is Phase 3's ratchet number (expected 1981: +3 pin tests — the actual count is
  authoritative).
- **Commit:** `sdk-vetting: record the FluidAudio VAD/EOU surface as found in the SDK (test-first)`
  — carrying the plan doc, the pin test, the understanding record, the provisioning script, the
  silero manifest, the already-drafted planning docs (issue.md/understanding.md/prd.md), and
  any Package.swift bump.

## Phase 3 — record + ratchet

- **Goal:** the PRD carries the gate's findings; the floor ratchets with the tests it counts.
- **Files:**
  - `docs/planning/turn-taking-barge-in/prd.md` — append a
    `## Findings from the sdk-vetting gate (2026-09-15)` section: the six facts, the EOU-shape
    correction (O2 resolved — present but ASR-integrated), the hysteresis verdict (O6 resolved —
    exposed as plain data), the version-pin decision (O1 resolved — no bump; resolved revision
    recorded). Update the O1/O2/O6 risk-table rows with their resolutions.
  - `Scripts/test-with-floor.sh:1639` — raise `MINIMUM_EXECUTED_TESTS` from 1978 to the exact
    executed count of the Phase 2 full run (the count, not a guess); append the ledger paragraph
    above the constant in the same commit (the kokoro `1974 -> 1978` paragraph at `:1620-1638`
    is the template; the last raise's pattern was restored there — keep the pattern).
- **Validation:** full `./Scripts/test-with-floor.sh` green at the new floor; `git log --oneline`
  shows the aspect's two commits.
- **Commit:** `sdk-vetting: ratchet the floor to <N> and record the gate's findings in the PRD`

## File-by-file build order

| File | Purpose | Exports | Depends on |
|---|---|---|---|
| `docs/planning/turn-taking-barge-in/sdk-vetting/plan_20260915.md` | this plan — the executing agent's brief | — | PRD R1/G1/O1/O2/O6, understanding |
| `Tests/HarnessTests/SileroVadProvenanceTests.swift` | the provenance pin (RED first) | — | `PackageManifest.swift`, `PackageRootLocator.swift`, `KokoroDependencyTests.swift` (shape) |
| `docs/planning/_card/understanding.md` | the vetting record — six findings + corrections, verbatim | — | the Step 2.1 reads |
| `Scripts/provision-vad-fixtures.sh` | one-shot staged download + digest generator (founder-run) | the manifest JSON to commit | C2 store shape (`ModelStore.swift`), `provision-kokoro-fixtures.sh` (shape) |
| `Sources/VoccaASR/Models/Manifests/silero-vad.json` | the pinned artifact manifest, digests from actual bytes | bundle resource (`.copy` declaration already covers `Models/Manifests`) | the script's run |
| `Package.swift` (+ re-resolve) | the FluidAudio range — bumped ONLY if finding 4 demands | — | the Step 2.1 version-coverage read |
| `docs/planning/turn-taking-barge-in/prd.md` | the findings appendix + O1/O2/O6 resolutions | — | the vetting record |
| `Scripts/test-with-floor.sh` | the floor ratchet + ledger paragraph | — | the Phase 2 full green run |

## Testing strategy

- **Written first:** `SileroVadProvenanceTests` (Phase 1, RED) — the only new tests in this
  aspect; three tests, one of them the failing manifest pin. No other aspect's tests exist yet
  (voice-detection/sdk-adapters come after this gate).
- **Filtered runs:** bare `swift test --filter SileroVadProvenanceTests` for RED and GREEN —
  never through `test-with-floor.sh` (its executed-count arithmetic assumes the full suite).
- **Full runs:** `./Scripts/test-with-floor.sh` — once in Phase 2 (must stay green at 1978;
  its count feeds Phase 3) and once in Phase 3 at the raised floor.
- **Not in this aspect:** the env-gated digest-verification row against provisioned bytes (the
  kokoro `ManifestDigestVerificationTests` row pattern) lands with the store integration in
  `sdk-adapters`, where `VOCCA_MODEL_DIR` gating lives. CI cannot run the SDK's real VAD here —
  no model bytes, no audio — and that is by design: the pin test makes this gate's decisions
  enforceable, nothing more.

## Environment & privacy

- **Zero network at runtime** (the permanent release blocker). `swift package resolve` at plan
  time and dependency fetch at CI build time are not app code — they are fine.
- **No URL is ever handed to any SDK surface** (the port-downloader-suppression pin precedent):
  the adapter path this gate locks is `VadManager(config:vadModel:)` with a store-staged model,
  so the SDK's ModelHub download init never runs inside the app.
- The artifact is staged **offline via the C2 store**, founder-run, digest-verified, marker-
  guarded, sequenced after ASR preparation and never inside `AppBootstrap.configure` (the probe
  contract). The app's dictation-time code path never downloads.
- `Package.resolved` is gitignored (`.gitignore:7`): the resolved revision is a RECORDED fact
  (the understanding note), not a committed pin.

## Edge cases

1. **EOU absent (the O2 fork)** — if the resolved SDK has no EOU surface: record the finding
   verbatim; `TurnDetector`'s real implementation (`ParakeetEOU`) is PENDING and that interim
   state is recorded honestly; `SilenceThresholdDetector` ships as the second implementation
   regardless (R3 unchanged); do NOT invent an EOU or a fabricated conformance. Expected
   outcome is the opposite — present at 0.15.5, but ASR-integrated: record that shape.
2. **Resolved version surprises** — the worktree's resolved FluidAudio differs from 0.15.5:
   if newer and the surface verifies, record and proceed without a bump; if older or the
   surface is ambiguous, **STOP and surface to the integrator** — never adapt silently (the
   kokoro engine-binding precedent).
3. **Artifact naming drift** — the HF repo renames or re-versions the `.mlmodelc` (a v7, a
   32 ms variant becoming required): record the exact name the RESOLVED SDK names
   (`ModelNames.VAD.sileroVadFile`); the pin test asserts the recorded name, so a future SDK
   rename turns the pin RED — that is the mechanism working, not a defect.
4. **License mismatch** — the artifact's license is not MIT (or the repo carries no LICENSE):
   STOP and surface; never absorb a license silently. FluidAudio's own Apache-2.0 is
   re-verified in the worktree's checkout.
5. **Beta-status doc comment** — recorded as a finding (adapter risk note), not a blocker.
6. **Download-attempt hazard** — the SDK's default init downloads via ModelHub when the bundle
   is missing; the record must name the pre-loaded init as the only acceptable adapter path so
   the sdk-adapters aspect cannot drift into a runtime download.
7. **`from:` 0.x semantics** — a future 0.16+ resolves silently under `from: "0.12.4"` (0.x:
   minor is breaking); a silent update breaking the adapter surfaces in the sdk-adapters suite
   and in the pin family — the kokoro precedent, recorded in the vetting record.

## Agent notes

- **Checkpoint 1 (after Phase 1):** the pin suite RED for the right reason — the manifest test
  fails on the missing file, the two family pins pass. Surface the RED to the integrator.
- **Checkpoint 2 (after Phase 2):** the vetting findings summary — the six facts, the EOU
  shape, the version decision — in the commit message; surface the provisioning-script
  checkpoint so the founder runs it before the manifest commit.
- **STOP and surface** (the integrator's call, never silently absorbed): any resolved-version
  surprise (case 2), any license mismatch (case 4), or any SDK surface that CONTRADICTS the PRD
  beyond recordable corrections — e.g. a resolved version without `VadManager` at all would
  reopen the VAD pick, the card's nearest feasibility risk.
- **Do NOT touch** (belongs to later aspects): the `VoiceActivityDetector`/`TurnDetector`
  seams, the `EnergyVAD`/`SilenceThresholdDetector` implementations, the FluidAudio adapters,
  the `ModelStore`/`ShippedModelManifest`/`EngineTier` machinery, the probe, the floor constant
  (until Phase 3), and `AppBootstrap.configure`.
- **Sequence:** this is the unit's first commit; hand off to `voice-detection` when Phase 3 is
  green — the seam work needs no SDK decisions beyond what this gate recorded.