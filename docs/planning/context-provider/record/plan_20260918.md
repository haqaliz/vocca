# Plan: record — 2026-09-18

> Aspect of `context-provider` (C12, the unit's LAST aspect — it owns the records, not the
> code). Requirements: S2 (`prd.md:131-133`, the PRODUCT_SPEC context section — the spec has no
> context surface today), R2 (`prd.md:171-175`, the never-read scope's wording), R3
> (`prd.md:184-188`, real-app resolution is env-gated by nature — recorded, never gated), R4
> (`prd.md:189-191`, the G5 pin re-anchor — the *record* of its rationale, the commit itself is
> `bootstrap-wiring`'s), the aspect boundary (`prd.md:225`: "STATUS/CLAUDE/ARCHITECTURE/
> PRODUCT_SPEC sync, SMOKE rows, the unit record"), the card's binding honesty obligations
> (`_card/issue.md:42-56`, `_card/understanding.md:76-88`), and the posture (`prd.md:161-162`,
> the fourth unit built ahead of the uncleared P2/P3 gates). Consumed from the sibling plans:
> `context-seam`, `accessibility-context`, `consent-store`, `widget-indicator`,
> `byok-context-grant`, and `bootstrap-wiring` — their SMOKE-visible surfaces, seam names, probe
> post-conditions and hand-offs are cited below and must be consumed, never re-derived. Suite
> command: `./Scripts/test-with-floor.sh` — **full runs only; this aspect adds no tests and no
> filtered runs exist**. Floor: whatever `Scripts/test-with-floor.sh:1770` holds at record time
> (started **2350**; `bootstrap-wiring` ratcheted it) — **verified, never blindly edited**; the
> deliberate-ratchet shape of the C11 record (`docs/STATUS.md:82-85`, eeacd87) is the precedent
> if the tree grew without one. Docs-only: there is no TDD RED/GREEN; the discipline is the
> doc-sync rule — *describe the state of the tree this file ships in* (`CLAUDE.md:147-148`).

## What this aspect ships

1. **SMOKE steps 139-143** — section 21 in `docs/SMOKE_CHECKLIST.md`, the five C12 rows appended
   after step 138 (`:3004`), each in the house row shape, each written **runnable,
   executed-later**: 139 the first real app/selection resolution run (S1 — the ≥95%
   matrix-resolution claim's real half), 140 the consent grant + the never-read audit, 141 the
   visible indicator (M8), 142 the one-action kill switch incl. the mid-turn discard (M9), 143
   the BYOK never-in-payload audit.
2. **The PRODUCT_SPEC context section** — `docs/product/PRODUCT_SPEC.md` gains the C12 surface
   (S2): the badge, the consent UI, the kill switch — written from the landed `widget-indicator`
   and `byok-context-grant` aspects' shipped copy and surfaces, never invented.
3. **The G5 pin re-anchor record** — the re-anchor *commit* lands in `bootstrap-wiring`; this
   aspect records the rationale and the before/after digests in the STATUS entry.
4. **The doc sync** — STATUS head entry, CLAUDE.md status paragraph, ARCHITECTURE.md seam-table
   + module-list + harness-table amendments (the house pattern: capability up in the commit that
   lands it, STATUS append, CLAUDE.md status paragraph, ARCHITECTURE seam amendments).
5. **The floor verification** — the record runs the floor script, reads the printed numbers,
   records `Floor <N> (executed <E>)`, and surfaces — never silently edits — a mismatch; the
   deliberate ratchet (the eeacd87 precedent) only as its own reviewed commit if the tree grew
   without one.
6. **The honesty block** — no gate passes (the fourth build-ahead unit); no context-accuracy
   percentage quoted until a real run exists; numbers recorded never gated; the dictation path
   byte-for-byte untouched (digests); AppBootstrap re-anchored with its new digest recorded;
   never-read and never-in-payload are structural, observed on the real surface by SMOKE 140/143;
   zero network.

## Setup

- Worktree: `/Users/aliz/dev/at/vocca/.claude/worktrees/feat-context-provider`, branch
  `feat/context-provider/aliz` (verify the actual branch name with `git branch --show-current`).
- **Prerequisite — the whole unit has landed on this branch**: `context-seam`,
  `accessibility-context`, `consent-store`, `widget-indicator`, `byok-context-grant`, and
  `bootstrap-wiring` (record last by sequencing). **In particular the `bootstrap-wiring` plan
  must have landed** — its re-anchor commit names the new AppBootstrap digest, its `PROBE-CONTEXT`
  leg names the probe post-condition, and its SMOKE-visible surfaces name the filters this
  aspect's rows cite. If any sibling plan has not landed, STOP and surface (Edge case 1); never
  invent a suite name, a filter, a digest, or a copy string.
- **First action of the executing agent: read the six landed sibling plans' Testing strategies +
  Agent notes and their execution records (commits), then re-read this plan's Vetted facts.**
  The record is written from what **shipped**, not from what the PRD promised. Every seam name,
  filter, env var and printed row tag in the SMOKE rows below is taken **verbatim from the
  landed plans** — the drafts here name the source of each; the executor fills the exact wording
  from the landed plan.
- No `swift package resolve` needed. The only commands this aspect runs are the full floor script
  (twice: once in Phase 2 to read the numbers the entries record, once in Phase 3 to verify) and
  git read commands.
- The uncommitted planning docs on the branch (`_card/issue.md`, `_card/understanding.md`,
  `docs/planning/context-provider/prd.md`, the six landed sibling plans, and this plan) ride in
  this aspect's first commit together with the SMOKE section (the house precedent: planning docs
  land with the record).

## Vetted facts (recorded — do not re-research)

1. **The SMOKE step-entry shape** (the C11 rows 134-138, `docs/SMOKE_CHECKLIST.md:2879-3004`):
   numbered `N. **Title.**` + *Gesture:* (the exact command) + *Verify the state was entered:*
   (rule 1 — a skipped test ran nothing; the skip message is the tell-tale) + *Pass:* (the
   recorded row verbatim with the `recorded-never-gated` token) + *Failure:* + optional
   *Void — not fail — if:*. The two preamble rules (`SMOKE_CHECKLIST.md:14-37`) bind every row:
   rule 1 (verify the state was entered; void, never fail) and rule 2 (a pass criterion looser
   than the failure it guards accepts the failure). Section headers are numbered (`## 20. Dual
   mode — \`dual-mode\`` at `:2866`); the new section is **`## 21. Context provider —
   \`context-provider\``**, inserted after step 138's `---` (`:3007`), before "When this file is
   wrong" (`:3009`). Steps continue **139, 140, 141, 142, 143** — no gap.
2. **The STATUS head-entry shape** (the C11 entry, `docs/STATUS.md:13-116`): bold-led summary →
   corrections/decisions by name → "What shipped, per aspect" with commit hashes and floor
   transitions → "The G5 pin re-anchor record" → "Measured (recorded, never gated)" with the
   verbatim rows → "The honesty block" as closing bullets → the floor as `Floor <N> (executed
   <E>)`. Newest entry first (insert after the `---` at `:11`, before the C11 entry at `:13`).
3. **The CLAUDE.md status-paragraph shape** (`CLAUDE.md:10-24`, the C11 paragraph): capability,
   what shipped per aspect (compressed), the measured numbers with "recorded, never gated", the
   posture sentence, and `Test floor: <N>.` One paragraph, self-dated, inserted as the newest
   unit above the C11 paragraph (between the header blockquote's blank `>` at `:9` and `:10`);
   the header's date `Status (2026-09-16)` (`:5`) updated to the commit's date (2026-09-18).
4. **The C12 acceptance and its real half** (`CAPABILITY_ROADMAP.md:345-359`): the ≥95%
   resolution acceptance is **real-app work** — scripted corpus in CI over the stub (R3,
   `prd.md:184-188`), the real number is the founder's machine with an Accessibility grant
   (executed by nothing in CI, now or ever — the tap-adapter precedent). **No context-accuracy
   percentage may be quoted until a real run exists** (`prd.md:40-41`); SMOKE 139 is that run's
   row, recorded never gated.
5. **The two privacy acceptances are structural** (the roadmap's "auditable rather than
   promised", `CAPABILITY_ROADMAP.md:355`): never-read (M5) asserted in CI by the consent-store
   gate's test + lint, observed on the real surface by SMOKE 140; never-in-payload (M6) asserted
   against the one payload-building site (`BYOKCleanupProvider.clean`), observed by SMOKE 143.
   The record states both as structural, never as discipline.
6. **The G5 digest pin and the re-anchor contract** —
   `Tests/HarnessTests/TurnTakingComposedAcceptanceTests.swift:311-347`: three pinned files:
   `Sources/VoccaCore/SessionMachine.swift`
   `1baeb2de2c45149746468bfef49862a08279008d3d2f305be892122d5727537e`,
   `Sources/VoccaCore/DictationPipeline.swift`
   `ce70ca10c15914d6960f07e53da8571a5fa9ec1fb58b8f0051ef051f16c07a84`,
   `Sources/VoccaBootstrap/AppBootstrap.swift`
   `6d98acf4765139b98ef5e6dea2bb635e54a4226f541bee1328b09a31bd120448` (the **pre-C12** digest —
   C11's final). The pin's own contract (`:340-344`): a deliberate edit recomputes the digest
   and edits the pin **in review**; "it must never be edited to match a moved tree." C12's
   wiring is additive context composition in `AppBootstrap` (the one pinned file that
   legitimately changes), so the re-anchor commit lands in `bootstrap-wiring`, and the first two
   files stay byte-for-byte untouched. **This aspect records the rationale and the new
   AppBootstrap digest in the STATUS entry — the digest taken verbatim from the landed
   re-anchor commit, never invented (Edge case 6).**
7. **The floor** (`Scripts/test-with-floor.sh:1770`): `MINIMUM_EXECUTED_TESTS=2350` as of the
   PRD; `bootstrap-wiring` ratchets it in the same commit as the tests it counts. At record time
   the floor is whatever the last sibling aspect set. The record **never edits the script
   silently**; it runs it, reads the executed count `E` and the floor `N`, records
   `Floor N (executed E)`. If `N != E` (a sibling aspect grew tests without a ratchet), the C11
   precedent applies: the deliberate, reviewed ratchet lands as **its own commit** (`docs/STATUS.md:82-85`:
   the routing close grew `ModeRoutingCompositionTests` without a ratchet; the mismatch was
   surfaced and eeacd87, 2334 → 2350, landed as its own commit).
8. **The real-run command precedent** (SMOKE 129/130 and 131-133): env-gated filtered runs
   (`VOCCA_RUN_REAL_*` vars) run **bare** — never through the floor script; row format follows
   the house tag=value shape. The C12 rows are **app-surface rows**, not suite rows: the gesture
   is running the built bundle and using the real surface; suite-driven sub-observations (the
   `PROBE-CONTEXT` post-condition) cite the landed plans' wording verbatim.
9. **The unit's honesty obligations** (binding, `_card/issue.md:42-56` and `prd.md:229-242`):
   no P2/P3 gate passes; the fourth unit built ahead of the uncleared gates under the recorded
   posture — every record says "No gate passes"; the dictation path byte-for-byte untouched (G5
   pin); the zero-network invariant and transcript-never-lost invariant are load-bearing;
   context is never persisted beyond the turn and never egresses (M10; the BYOK payload
   exclusion is the point); no context-accuracy percentage quoted until a real run exists; the
   seam is local-only by design — no hosted counterpart.

---

## Phase 1 — SMOKE steps 139-143 (the rows, written runnable) and the PRODUCT_SPEC context section (S2)

**Goal.** The five C12 rows appended to `docs/SMOKE_CHECKLIST.md` as a new section 21 after step
138 (`:3004`) and the section's `---` (`:3007`), before "When this file is wrong" (`:3009`),
each in the 134-138 shape with the **exact gesture commands cited from the landed sibling
plans** and the state-entered checks per rule 1. The rows are written **runnable,
executed-later**: they state the observation, the gesture, the budget, and the
recorded-never-gated contract; whether the founder has run them by merge time is the Edge case 2
question, not a Phase 1 concern. And the `PRODUCT_SPEC.md` context section (S2) — the spec has
**no context surface today** (13 sections, no "context" matches); this unit defines it, and the
record lands it. The uncommitted planning docs ride in this commit.

**Section header + intro** (house shape, after the `---` at `:3007`):

> `## 21. Context provider — \`context-provider\``
>
> Nothing in this section runs in CI. The two privacy acceptances are proven headlessly and
> structurally (with context capture off for an app, **no content read of that app occurs at
> all** — not read-then-discard, never read, asserted by test and lint; context never appears in
> a BYOK request payload without the separate explicit grant, asserted against the one
> payload-building site); the real app/selection resolution is executed by nothing in CI — real
> apps need a real machine with an Accessibility grant (the tap-adapter precedent). These five
> rows are the founder's machine producing the first real observations of the C12 surface —
> recorded, never gated, each under **rule 1**: the state must actually have been entered before
> a row means anything. **No context-accuracy percentage is quoted until a real run exists
> (step 139)** — nothing below may be read as a gate pass.

**139. The first real app/selection resolution run — the ≥95% claim's real half (C12, recorded — never gated).**

- *Gesture:* on the founder's machine with the Accessibility grant in place, run the env-gated
  resolution suite from the landed `accessibility-context` plan (verbatim filter + env var), and
  additionally resolve the focused app + selection live across a real working session in a
  consented app: focus a window with a known selection, run the resolution, and compare the
  resolved bundle ID / window title / selected text against the known truth.
- *Verify the state was entered:* the env-gated suite actually ran (its skip message absent —
  the skipped-test tell-tale), the accessibility grant was actually in place, and the live
  check actually read a real focused window with a real selection.
- *Pass:* the row recorded verbatim with the never-gated note: the observed resolution count and
  accuracy against the C8 matrix rows actually exercised, in the `recorded-never-gated` shape —
  **a number below the ≥95% target is recorded verbatim, never a pass** (the 130 wording
  precedent). The acceptance's ≥95% (`CAPABILITY_ROADMAP.md:355`) stays the roadmap's.
- *Void — not fail — if:* the grant was absent, the env-gated suite skipped, or no real focused
  selection was read (rule 1).
- *Failure:* a skipped run recorded as a number, a number recorded as a gate pass, or the row
  missing the never-gated note.

**140. The consent grant and the never-read audit (C12, recorded — never gated).**

- *Gesture:* in a real app (Notes, an editor), **without** consent for that app, run a real
  dictation while a selection is present in the focused app, then verify — per the landed
  `consent-store` plan's audit wording — that **no content read** of that app occurred (the
  provider's read ledger + the unified log, `log stream --predicate 'subsystem ==
  "dev.vocca.Vocca"'`); then grant consent for the app (the Apps-tab surface, verbatim from the
  landed `consent-store`/`widget-indicator` plans), run a second dictation, and verify the read
  now occurs.
- *Verify the state was entered:* the dictations really ran (a delivered transcript each), the
  selection was really present in the focused app, and consent was really flipped between the
  two runs (the store's answer observed to change).
- *Pass:* the row recorded verbatim with the never-gated note: **0 content reads before
  consent, reads after** — the "not read-then-discard, never read" claim (`prd.md:84-85`)
  observed on the real surface, with the M5 scope recorded (bundle ID + window title are
  legitimately read by the dictate path; the boundary is content = selected text).
- *Void — not fail — if:* no real dictation ran in either state (rule 1 — a window with no
  dictation proves nothing about reads).
- *Failure:* a content read observed before consent (the privacy incident — recorded verbatim
  and surfaced loudly; it is a product bug, never silently absorbed).

**141. The visible indicator (C12, recorded — never gated).**

- *Gesture:* with consent active for the focused app, verify the widget shows the context
  indicator persistently (the badge state from the landed `widget-indicator` plan — copy
  verbatim), and that it is **distinct from the egress ☁ badge** (both visible when a BYOK
  provider is active); focus an app without consent and verify the indicator clears; focus a
  Secure Input field in a consented app and verify the indicator **never lights** (M5b).
- *Verify the state was entered:* consent was really active for the focused app (the store's
  answer), the egress badge was really lit where the distinctness check ran, and the Secure
  Input field was really focused (the system's own indicator visible).
- *Pass:* the row recorded verbatim with the never-gated note: indicator present iff consent
  active for the focused app, distinct copy from egress, never lit on Secure Input — the M8
  contract observed on the real surface.
- *Failure:* the indicator lit without consent, lit on a Secure Input field, or was
  indistinguishable from the egress badge.

**142. The one-action kill switch, incl. the mid-turn discard (C12, recorded — never gated).**

- *Gesture:* with consent active and the indicator lit, throw the kill switch **mid-session**
  (the menu-bar row, verbatim from the landed `widget-indicator` plan — one action): verify the
  indicator clears in the same fold, further reads stop immediately (a fresh dictation in the
  consented app reads nothing — the provider's ledger), and the current turn's snapshot is
  discarded (nothing persisted — the consent store holds bundle IDs only, the byte-level pin
  precedent); then verify the kill switch never reads as an invitation to grant (the row's copy,
  verbatim).
- *Verify the state was entered:* the kill was thrown during a live session with the indicator
  lit (a kill while nothing was armed proves nothing about mid-turn semantics), and a real
  dictation ran after the kill.
- *Pass:* the row recorded verbatim with the never-gated note: **one action revoked globally,
  reads stopped immediately, the in-flight snapshot discarded, the badge cleared in the same
  fold** (M9's mid-turn semantics) — observed on the real surface.
- *Failure:* a kill that takes more than one action, reads continuing after the kill, a snapshot
  surviving the kill, or a badge that outlives the fold.

**143. The BYOK never-in-payload audit (C12, recorded — never gated).**

- *Gesture:* with consent active for a focused app **and** a BYOK cleanup provider configured
  but the separate global grant **off**, dictate in the consented app and verify the captured
  request payload carries **no context field** (the grant's off-state, `byok-context-grant`'s
  shipped payload shape); then turn the grant on (the surfaced surface, verbatim), dictate
  again, and verify the payload carries the granted context — bundle ID, window title, and the
  bounded selected text — and nothing else.
- *Verify the state was entered:* both dictations really ran through the BYOK provider (the
  provider's own request log — a run that never reached the payload builder proves nothing),
  consent was really active, and the grant was really flipped between the two runs.
- *Pass:* the row recorded verbatim with the never-gated note: **context absent from the
  payload with the grant off, present and bounded with it on** — the AND-gate (per-app consent
  **and** the global grant, never either alone, `prd.md:104-107`) observed on the real surface.
- *Void — not fail — if:* no BYOK request was actually sent in either state (rule 1).
- *Failure:* context in the payload without the grant, the grant on without the per-app consent,
  or unbounded selection text in the payload (the bound is `byok-context-grant`'s, verbatim).

**The PRODUCT_SPEC section (S2).** `docs/product/PRODUCT_SPEC.md` gains the C12 surface as a new
section — the house shape avoids renumbering: **`## 8a. Context (P4) — the reading surface`**
inserted after §8 (the egress badge, `:312-329`), before §9 (Sound, `:330`). Content, written
**verbatim from the landed aspects' shipped copy and surfaces** (never invented): the indicator's
behavior and copy (the `widget-indicator` plan's shipped badge state and `WidgetCopy` strings),
the consent UI (the Apps-tab surface from the landed `consent-store` plan), the kill switch (the
menu-bar row + Settings surface, M9's mid-turn semantics), the BYOK grant surface and the payload
shape + bound (`byok-context-grant`'s), and the default-off posture sentence. The spec's
preamble rule — `PRODUCT_SPEC.md` wins on *user-visible behavior* — is what makes this section
authoritative once landed; the record cites it as such.

**Validation.**
1. Read the five rows back against the landed sibling plans: every *Gesture:* names the landed
   plan's own surface (the env-gated filter, the Apps-tab consent row, the badge copy, the
   menu-bar kill row, the grant toggle), every *Pass:* carries the `recorded-never-gated` token
   and its numbers, every row has *Verify the state was entered:*, and steps 139/140/143 have
   their *Void* clauses. The no-percentage line is present in 139 and the section intro.
2. The steps' numbering continues 134-138 → 139-143 with no gap; the section is numbered 21
   after section 20; the PRODUCT_SPEC section is 8a between 8 and 9 with no renumbering.
3. `git diff --stat` shows exactly `docs/SMOKE_CHECKLIST.md`, `docs/product/PRODUCT_SPEC.md`
   (plus the already-dirty planning docs) — no `Sources/`, no `Tests/`, no `Scripts/`.

**Commit message.** `record: SMOKE steps 139-143 — the context-provider real surface rows (the first resolution run, the consent/never-read audit, the indicator, the kill switch, the BYOK never-in-payload audit) and the PRODUCT_SPEC context section (S2)`

---

## Phase 2 — the STATUS head entry (with the pin re-anchor record), the CLAUDE.md status paragraph

**Goal.** The unit's record in the two always-read files, in the C11 pattern, with **real
numbers** — the floor script is run first, and the entries carry its printed numbers, never
placeholders.

**Files.**
- `docs/STATUS.md` — a head entry inserted after the `---` (`:11`), before the C11 entry
  (`:13`). Shape (the C11 pattern, `:13-116`):
  - **Bold-led summary** — e.g. `**The \`context-provider\` unit shipped 2026-09-18 — C12's
    context half of the wedge: the seam with two local implementations, the per-app consent
    gate, the visible indicator and the one-action kill switch, the BYOK exclusion grant; the
    privacy claim is auditable rather than promised; no gate passes.**` `feat/context-provider/aliz`.
  - **What shipped, per aspect** — one sentence per aspect, written from the **landed commits**
    (hashes + floor transitions taken from the landed `git log` — never invented): `context-seam`
    (the `ContextProvider` protocol + `ContextSnapshot` + `NullContext` + the seam lint),
    `accessibility-context` (the `VoccaContext` target + `AccessibilityContext` + the per-seam
    AX permit + the scripted-corpus harness), `consent-store` (the per-app persistence,
    bundle-IDs-only, the never-read gate), `widget-indicator` (the badge + the kill switch's
    surface, M8/M9), `byok-context-grant` (the global off-by-default grant + the gated payload
    field, M6/M7), `bootstrap-wiring` (the additive `AppBootstrap` composition, `PROBE-CONTEXT`,
    and the **G5 pin re-anchor commit**), and this `record` aspect. Where a sibling plan
    promised a hand-off, the entry records what **actually** happened.
  - **The pin re-anchor record** — its own short paragraph: `AppBootstrap.swift` is in the G5
    pin (`TurnTakingComposedAcceptanceTests.swift:311-347`); C12's wiring is additive context
    composition in exactly that file, so the pin was **deliberately re-anchored in a reviewed
    commit** (`bootstrap-wiring`'s re-anchor hash, cited) — per the pin's own contract ("a
    deliberate edit recomputes the digest and edits the pin in review; it must never be edited
    to match a moved tree"). The dictation files' digests are unchanged:
    `SessionMachine.swift 1baeb2de2c45149746468bfef49862a08279008d3d2f305be892122d5727537e`,
    `DictationPipeline.swift ce70ca10c15914d6960f07e53da8571a5fa9ec1fb58b8f0051ef051f16c07a84`;
    `AppBootstrap.swift` re-anchored from `6d98acf4…0448` to the **new digest taken verbatim
    from the landed re-anchor commit** (never invented).
  - **Measured, recorded never gated** — the SMOKE 139-143 rows' state: written and runnable;
    if the founder ran any of them before merge, the verbatim rows (with the no-percentage
    honesty line on 139); if not, the entry says exactly that. The unit's headless/CI facts are
    recorded as CI facts with margins, never gate passes (the never-read test, the
    never-in-payload test, the `PROBE-CONTEXT` post-condition, the scripted corpus).
  - **The honesty block** — the closing bullets: **no P2/P3 gate passes** (the fourth unit
    built ahead of the uncleared gates under the recorded posture); **no context-accuracy
    percentage quoted** — the ≥95% acceptance is SMOKE 139, recorded never gated, and no
    percentage may be quoted until a real run exists; numbers recorded never gated; the
    dictation path byte-for-byte untouched (the two digests above) with AppBootstrap's
    re-anchor recorded; never-read and never-in-payload are structural, asserted in CI,
    observed on the real surface by SMOKE 140/143; context never persisted beyond the turn
    (bundle-IDs-only consent store, the byte-level pin precedent) and never egresses (the BYOK
    exclusion is the point; the seam has no hosted counterpart by design); zero network
    (`PROBE-CONTEXT` inside the interposer, no URL in the context path); interim states
    recorded, not papered over (real-app resolution is env-gated by nature — executed by
    nothing in CI; C13's consumption is out of scope — nothing user-visible consumes context
    beyond the BYOK grant field and the tests).
  - **The floor line** — `Floor <N> (executed <E>)` from the Phase 2 floor run.
- `CLAUDE.md` — one new status paragraph, inserted as the newest unit above the C11 paragraph
  (between the header blockquote's blank `>` at `:9` and `:10`), and the header's date
  `Status (2026-09-16)` (`:5`) updated to the commit's date (2026-09-18). The paragraph carries
  the same substance in the C11 wording pattern: the unit's capability; what shipped per aspect
  (compressed); the pin re-anchor in one line; the SMOKE rows' state (or their measured numbers)
  with the no-percentage honesty line; the posture sentence — "**No gate passes; no
  context-accuracy percentage may be quoted until a real run exists**" — and
  `Test floor: **<N>**.` Self-dated like every paragraph; history below it untouched.

**Validation.**
1. **Run `./Scripts/test-with-floor.sh` first** — full run, green; record the printed executed
   count `E` and the floor constant `N` at `Scripts/test-with-floor.sh:1770`. Both entries are
   written with those numbers.
2. Read both entries against Vetted fact 9's checklist: every honesty obligation has a line; no
   sentence claims a gate pass, a percentage, or a number CI did not produce; the AppBootstrap
   digest in the pin-re-anchor paragraph matches the landed re-anchor commit verbatim.
3. `git diff --stat` — exactly `docs/STATUS.md`, `CLAUDE.md`, and the already-dirty planning
   docs.

**Commit message.** `record: the C12 unit record — STATUS head entry (the pin re-anchor rationale, the SMOKE rows' state, the honesty block) and the CLAUDE.md status paragraph`

---

## Phase 3 — the ARCHITECTURE.md sync, the floor verification, the honesty-block check

**Goal.** `docs/technical/ARCHITECTURE.md` describes the tree this unit's close ships in: the
C12 seam row real, the module list real, the harness table amended; the floor verified (not
silently edited); the honesty block confirmed complete.

**Files.** `docs/technical/ARCHITECTURE.md` — the named amendments, each verified against the
tree **and the landed sibling plans** before writing (seam names verbatim from the plans; never
claimed if not shipped):

1. **The seam table** (`:268-282`): the Context row (`:281`) is annotated shipped — the
   `ContextProvider` protocol in `VoccaCore`, `AccessibilityContext` + `NullContext` real (names
   verbatim from the landed `context-seam`/`accessibility-context` plans), the consent gate, the
   indicator + kill switch, the BYOK grant — and "No — by design" (hosted) confirmed as shipped
   truth, not a reservation.
2. **The §3 directory layout line** (`:151`, `VoccaContext/ # P4 — ContextProvider`): annotated
   per the shipped truth — the target exists in `Package.swift`, what it owns (the AX adapter +
   its per-seam permit + the consent store, per the landed plans), never a claimed layout.
3. **A C12 wiring annotation** (the §5 seam-table status block or a new §5 note, the house
   shape): the composed wiring in `VoccaBootstrap` (additive — the recipe + the root slots), the
   consent-before-read gate, the indicator fold, the kill-switch routing, the G5 re-anchor
   record, and the `PROBE-CONTEXT` leg inside the zero-network interposer.
4. **The §14 harness table** (`:688-697`): a new row naming the C12 acceptances — the never-read
   test, the never-in-payload test, the scripted-corpus resolution harness, the `PROBE-CONTEXT`
   drive — what runs in CI, what is founder-run (SMOKE 139-143), recorded never gated.
5. **Any §16 open question the unit closes** — verify at execution time; do not close anything
   else (the context transcript surface stays C13's).

**Floor verification (no silent edits).**
1. `./Scripts/test-with-floor.sh` from the worktree root — green; the executed count `E` and
   floor `N` print. If `N == E` (the expected state — the record adds no tests), record
   `Floor N (executed E)` in the STATUS entry and verify the Phase 2 entry already says so.
2. **If `N != E`**: do NOT edit the script in this commit — the C11 precedent is the
   deliberate-ratchet commit (`docs/STATUS.md:82-85`, eeacd87): the mismatch is surfaced to the
   integrator, and the reviewed ratchet lands as **its own commit** (`record: ratchet the floor
   N -> E — the C12 close's executed count`), naming the tests that grew the count. The STATUS
   entry then carries both numbers and the ratchet's rationale.

**Honesty-block check.** Re-read the Phase 2 entry against Vetted fact 9 and the SMOKE rows
against Vetted facts 4-5: every obligation has a line; every row carries the
`recorded-never-gated` token; the no-percentage line is present; nothing in the tree's record
claims a gate pass or a quoted accuracy number. The `git diff --stat` of the whole aspect lists
no `Sources/`, no `Tests/`, no `Scripts/` change — the unit's code shipped in the sibling
aspects' commits; this aspect's diff is docs only (plus, only in the surfaced-mismatch case, the
single floor line in its own commit).

**Commit message.** `record: sync ARCHITECTURE.md to the shipped C12 seams, module list and harness rows, and verify the floor`

---

## Commit sequence for this aspect

| # | Files | Message |
|---|-------|---------|
| 1 | `docs/SMOKE_CHECKLIST.md`, `docs/product/PRODUCT_SPEC.md` (+ the uncommitted planning docs riding: `_card/issue.md`, `_card/understanding.md`, `docs/planning/context-provider/`, including this plan) | `record: SMOKE steps 139-143 — the context-provider real surface rows (the first resolution run, the consent/never-read audit, the indicator, the kill switch, the BYOK never-in-payload audit) and the PRODUCT_SPEC context section (S2)` |
| 2 | `docs/STATUS.md`, `CLAUDE.md` | `record: the C12 unit record — STATUS head entry (the pin re-anchor rationale, the SMOKE rows' state, the honesty block) and the CLAUDE.md status paragraph` |
| 3 | `docs/technical/ARCHITECTURE.md` | `record: sync ARCHITECTURE.md to the shipped C12 seams, module list and harness rows, and verify the floor` |
| 3a | (only if the Phase 3 floor run surfaced `N != E`) `Scripts/test-with-floor.sh` | `record: ratchet the floor N -> E — the C12 close's executed count (the eeacd87 precedent)` |

No `Sources/`, no `Tests/`, no `Package.swift` in any commit; `Scripts/test-with-floor.sh`
only in the surfaced-mismatch case, in its own commit.

## File-by-file build order

| File | Edit | Where it lands | Exports |
|---|---|---|---|
| `docs/SMOKE_CHECKLIST.md` | Phase 1: the section 21 header + intro + steps 139-143, each with *Gesture / Verify the state was entered / Pass / Failure* (+ *Void* for 139, 140, 143) | after step 138 (`:3004`) and its `---` (`:3007`), before "When this file is wrong" (`:3009`) | the five recorded-never-gated rows |
| `docs/product/PRODUCT_SPEC.md` | Phase 1: the §8a context section — the indicator, the consent UI, the kill switch, the BYOK grant, the default-off posture, copy verbatim from the landed aspects | after §8 (`:312-329`), before §9 (`:330`) | the S2 surface definition |
| `docs/STATUS.md` | Phase 2: the head entry — bold-led summary, per-aspect shipped (hashes/floors from the landed log), the pin re-anchor record, measured rows, honesty block, floor line | after the `---` (`:11`), before the C11 entry (`:13`) — newest first | the unit record |
| `CLAUDE.md` | Phase 2: the new status paragraph + the header date (`:5` `2026-09-16` → 2026-09-18) | between the header blockquote's blank `>` (`:9`) and the C11 paragraph (`:10`) | the always-loaded unit summary |
| `docs/technical/ARCHITECTURE.md` | Phase 3: the seam-table Context row (`:281`), the §3 module line (`:151`), the wiring annotation, the §14 harness row (`:688-697`), any §16 close | the sections named | the authoritative-tree sync |
| (untouched, by design) | `Scripts/test-with-floor.sh` (except the surfaced-mismatch ratchet in its own commit), `Package.swift`, every `Sources/` file, every `Tests/` file | — | — |

## Testing strategy

- **This aspect adds no tests.** There is no RED/GREEN and no filtered run; the validation is
  the doc-consistency read and the floor script:
  - `./Scripts/test-with-floor.sh` — twice (Phase 2 to read the numbers the entries record,
    Phase 3 to verify `N == E`, or to surface and ratchet per the eeacd87 precedent). The record
    does not change the executed count, so a green run at the unchanged floor is the whole test.
  - The doc-sync read: STATUS head, CLAUDE.md paragraph and ARCHITECTURE's named sections all
    describe the same tree (the same floor number, the same seam names, the same interim
    states) — the "describe the state of the tree this file ships in" rule.
  - The SMOKE rows' internal consistency: the numbers match the PRD/roadmap (≥95% unquoted
    until a real run, 0 content reads without consent, indicator iff consent + never on Secure
    Input, one-action revoke with mid-turn discard, payload exclusion without the grant), and
    the gestures match the landed plans' wording.
- **What CI structurally cannot cover here** is stated, not hidden: the rows themselves (real
  apps, real AX reads, real BYOK traffic) are founder-run by definition — that is why they are
  SMOKE rows with the recorded-never-gated token, and why the state-entered checks are the
  tell-tales.

## Environment & privacy

- **Docs-only: no code, no network, no model bytes, no runtime of any kind in this aspect.**
  The only executed command is the floor script (a local `swift test` run).
- The SMOKE rows **never claim egress**: every gesture runs the local app with the default
  zero-network configuration; the BYOK payload rows (143) observe the shipped payload builder's
  output — the grant's off-state is the default, and the row records the exclusion, never a
  sanctioned transmission.
- The rows carry no transcript text and no selection text: they record numbers, outcomes and
  counts, matching the usage-ledger byte-level pin's spirit and M10 (context is never
  persisted beyond the turn).

## Edge cases

1. **A sibling plan has not landed at record time** — STOP and surface; this aspect's rows
   cannot cite surfaces, filters, copy strings or digests it would have to invent. (Should not
   occur: `record` runs last by sequencing.)
2. **The founder has not run the steps by merge time** — the rows are written runnable,
   executed-later; the STATUS/CLAUDE entries say exactly that ("SMOKE 139-143 written; the
   executed rows land when the founder runs them"). Precedent: steps 134-138 were unexecuted at
   the C11 unit close. **This aspect's merge does NOT depend on the steps having been
   executed** — recommended, and the integrator may decide otherwise. A non-executed step is
   never recorded as a pass, never as a fail — it is an unrun step, stated as such.
3. **A voided SMOKE run** — rule 1: a run whose state was not entered (no real dictation in the
   window for 140, no BYOK request for 143, no grant for 139) is **void, not fail**: it is
   never recorded as a number, and the STATUS entry says the row awaits a valid run.
4. **An observed failure in the product** (a content read before consent in 140, the indicator
   lit on Secure Input in 141, reads continuing after the kill in 142, context in the payload
   without the grant in 143) — the row is **recorded verbatim** with the never-gated token, and
   the finding is surfaced to the integrator loudly (it is a product bug, not a record failure —
   and the never-read/never-in-payload violations are the privacy incidents
   `CAPABILITY_ROADMAP.md:347` names; silence would be the real error). Never "fix" the product
   from the record aspect.
5. **Floor mismatch at verification** (`N != E` at Phase 3) — the C11 precedent, `docs/STATUS.md:82-85`:
   record both numbers honestly in the STATUS entry, surface to the integrator, and land the
   deliberate, reviewed ratchet as its own commit (3a). Never silently edit the script.
6. **The pin re-anchor state is wrong at record time** — the landed `bootstrap-wiring` re-anchor
   commit is absent, or its new AppBootstrap digest contradicts the pinned tree: STOP and
   surface. Never invent a digest; the record cites the pin as it landed.
7. **A sibling hand-off that did not happen** — e.g. `bootstrap-wiring` did not update
   `CAPABILITY_ROADMAP.md`'s C12 row to shipped (the kokoro-binding sync-commit precedent,
   `71a4ee6`; C10's own close, `d7241ca`): flag it to the integrator — the record can carry it
   in Phase 3 if the integrator wants the roadmap row closed with the unit.

## Agent notes

- **Checkpoint 1 (after Phase 1):** the five SMOKE rows read back against the six landed sibling
  plans — the integrator reviews that every *Gesture:* is the landed wording (the env-gated
  filter, the Apps-tab consent surface, the badge copy, the menu-bar kill row, the grant
  toggle), every number is the PRD/roadmap number, and the no-percentage line is present in 139
  and the section intro. Surface the row drafts and the PRODUCT_SPEC §8a draft before Phase 2.
- **Checkpoint 2 (the founder check):** the SMOKE steps are ready to run on the founder's
  machine. The recommended merge posture: **this aspect merges without requiring the runs**
  (Edge case 2) — the rows exist as runnable, executed-later steps; the founder runs them after
  the unit merges, and the executed rows land as follow-up edits to the SMOKE section and
  STATUS entry. If the integrator prefers the rows executed before merge, the founder runs them
  during this aspect — recorded as the unit's final state either way.
- **Checkpoint 3 (after Phase 3):** the ARCHITECTURE diff reviewed (the seam-table Context row,
  the §3 module line, the wiring annotation, the §14 harness row), the floor verified (`N == E`,
  or surfaced and ratcheted per the eeacd87 precedent), the pin-re-anchor paragraph's digest
  cross-checked against the landed re-anchor commit, and the final `git log` shows this aspect's
  commits on top of the six sibling aspects'.
- **Merge order:** `record` is the unit's last aspect; after its merge the unit closes — the
  branch merges to `master`, the unit's record is complete, and the P2/P3 gates stay uncleared
  with the posture named.
- **STOP and surface** (never improvise): any sibling plan absent (Edge case 1); any
  ARCHITECTURE fact that contradicts what shipped beyond the recorded amendments; any
  instruction to edit `Scripts/test-with-floor.sh` silently; any instruction to "fix" a sibling
  aspect's state from the record aspect; any temptation to invent the AppBootstrap digest, a
  copy string, a percentage, or a suite name (Edge case 6).
- **Do NOT touch** (by design): every `Sources/` file, every `Tests/` file, `Package.swift`,
  `Scripts/test-with-floor.sh` (except the surfaced-mismatch ratchet in its own commit), the
  dictation path, and the already-shipped planning docs' history — the record appends; it does
  not rewrite.

## Checkpoints (for the human integrator)

1. **After Phase 1** — review the five SMOKE rows (139-143) against the six landed sibling
   plans: gesture wording verbatim, numbers exact, the no-percentage honesty line present, steps
   139/140/143 carry their *Void* clauses; review the PRODUCT_SPEC §8a draft against the landed
   copy pins. Approve before Phase 2.
2. **The founder check** — the rows are ready to run on your machine; the recommended posture
   is that this aspect merges without the runs (Edge case 2, the C11 precedent). If you want
   the rows executed before merge, run them during this aspect.
3. **After Phase 2** — read the STATUS head entry and the CLAUDE.md paragraph: the per-aspect
   hashes/floors match the landed log; the pin-re-anchor paragraph quotes the landed re-anchor
   commit's new AppBootstrap digest; the no-percentage line is present; the honesty block's
   "fourth build-ahead unit; no gate passes" line is present.
4. **After Phase 3** — review the ARCHITECTURE diff (the seam-table Context row, the §3 module
   line, the wiring annotation, the §14 harness row); confirm the floor verification printed
   `N == E` (or that the mismatch was surfaced and the ratchet landed as its own reviewed
   commit); confirm the final `git log` shows the record commits on top of the six siblings;
   decide whether the CAPABILITY_ROADMAP C12 row close rides here (Edge case 7) or lands as its
   own follow-up.
5. **At unit close** — no gate passes; the fourth build-ahead unit recorded as such; no
   context-accuracy percentage quoted; the dictation path is byte-for-byte untouched with the
   digests recorded; C13 blocks on this unit and is the next capability.