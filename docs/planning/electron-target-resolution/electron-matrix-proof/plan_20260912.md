# Plan: electron-matrix-proof (2026-09-12)

> Executes `electron-matrix-proof/spec.md`. Founder-in-the-loop (~5 × 30 s of dictation + the
> desktop check). Runs after `resolver-fallback` merges and v0.3.1 is built + installed.

## Preconditions (record each)

1. v0.3.1 installed: `defaults read /Applications/Vocca.app/Contents/Info CFBundleShortVersionString` → `0.3.1`.
2. Vocca running, armed, model prepared.
3. `Scripts/injection-matrix.sh --verify-bundle-ids` → 0 mismatched (from the worktree at master's tip).
4. `Scripts/injection-matrix.sh --self-check` → passes (guard + landing-rung pins active).
5. `strategies.json` snapshot → saved to the run notes (memory active, no reset).
6. Floor re-read: `MINIMUM_EXECUTED_TESTS` from `Scripts/test-with-floor.sh:1569`.

## The run (one `--row` per row, one run log)

```bash
LOG="$HOME/Library/Application Support/Vocca/matrix-runs/20260912-electron.jsonl"
Scripts/injection-matrix.sh --row VSCode   --run-log "$LOG"
Scripts/injection-matrix.sh --row Teams    --run-log "$LOG"
Scripts/injection-matrix.sh --row Discord  --run-log "$LOG"
Scripts/injection-matrix.sh --row ChatGPT  --run-log "$LOG"
Scripts/injection-matrix.sh --row Obsidian --run-log "$LOG"
```

Per row: click into the field, dictate the phrase, ⏎, then answer the landing-rung question
from the log (`log show --predicate 'subsystem == "dev.vocca.Vocca"' --last 30m | grep
"delivery target="`) + the `attempted:` first-method fact. Expected: `.clipboardPaste`
landing, first-attempted.

## The desktop refusal (negative proof)

1. Click the desktop (no window focused — Finder frontmost).
2. Press ⌥Space, dictate the phrase, stop.
3. Expect: no text anywhere; the failsafe shows **"Nothing was focused. Click where you want
   this, then press ⏎."**; the recovery journal gains a `{"reason":"noFocusedField"}` entry.
4. Record the journal entry + the failsafe copy in the run notes.

## Second-defect posture (pre-decided)

- **Delivered-but-invisible** (the log names clipboardPaste, bytes can't be compared because
  nothing shows, widget shows ✓): stop the run, record the evidence, and escalate to the
  integrator for the in-unit fix cycle (PRD's pre-decided posture — the paste mechanics are a
  rung-level defect, fixed test-first in this unit).
- A row that VOIDs: fix the precondition (grant/focus), re-run once; second void recorded.

## Record (after the run)

1. **Tracked-table v0.3.1 row** (`docs/SMOKE_CHECKLIST.md` §12): release, date, rows run
   (5 Electron rows), verdicts, the FMS math over the rows run (memory-ordered), the desktop
   refusal note, the 17/20 ceiling note (unchanged). Historical rows untouched.
2. **STATUS entry** (append-only, newest first): the shipped resolver fallback (commits, floor
   1936→…), the 5-row results, the desktop refusal, the NOT block (no gate passes; no
   percentage quoted; FMS over 17 still non-computable; step 92 + GoogleDocs + re-runs
   pending as before).
3. **CLAUDE.md front-door sync**: the Electron-defect paragraph replaced by the fix + proof
   summary.

## Commits

- `target-resolution: prove the Electron fallback — 5 matrix rows + desktop refusal (record)`

## Checkpoints

- Before the run: integrator confirms v0.3.1 installed + preconditions recorded.
- After the run: the tally + desktop proof go to the integrator for the record.