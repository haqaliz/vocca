# Plan: resolver-fallback (2026-09-12)

> Implements `resolver-fallback/spec.md` (PRD R1-R5, M4-M6, S1, N1). Strict TDD — no production
> code before a failing test. All work in the worktree
> `.claude/worktrees/feat-electron-target-resolution` on branch
> `feat/electron-target-resolution/aliz`. Floor: 1936 (re-read from
> `Scripts/test-with-floor.sh:1569` before starting).

## Task list (agents team — one agent per task, TDD inside each)

1. **T1 — RED: the resolver tests + seam.** In `Tests/HarnessTests/AccessibilityRungTests.swift`
   add (using the existing fake pattern, `FakeFocusedAppReader` at :356):
   - a `FakeFrontmostAppReader` actor fake (identity fixed at construction, `readCount`);
   - `testTheFrontmostFallbackSuppliesTheBundleIDWhenAXAnswersNil` (AX nil + regular VSCode →
     bundleID `com.microsoft.VSCode`, windowTitle nil);
   - `testNoFallbackWhenTheFrontmostAppIsNotRegular`;
   - `testNoFallbackWhenTheFrontmostAppIsFinder` (the seeded set);
   - `testNoFallbackWhenTheFrontmostAppHasNoBundleID`;
   - `testTheFallbackNeverConsultsTheFrontmostReadWhenAXAnswers` (AX Notes + frontmost fake →
     bundleID Notes, `readCount == 0` — M6);
   - `testSecureInputIsReadFreshOnTheFallbackPath` (AX nil + fallback + secure true →
     `isSecureInput == true`).
   All RED (the seam doesn't exist: compile failures — the right RED reason for a new seam;
   name it). Update `TargetResolutionSurfaceTests.swift` recipe test to the new init (RED —
   compile break).

2. **T2 — GREEN: the seam + gate + seed.** Edit `TargetResolution.swift` (add
   `FrontmostAppIdentity`, `FrontmostAppReading`, the init parameter, the gated fallback in
   `resolve()` — the fallback consults the frontmost read **only** when AX's bundleID is nil;
   windowTitle nil on fallback; single fresh secure-input read on every path; the header
   comment gains the fallback row — N1). Create `SystemFrontmostApp.swift`
   (`NSWorkspace.shared.frontmostApplication` → bundleID + `activationPolicy == .regular`;
   nil when no frontmost). Create `Allowlist/SeededNoFieldApps.swift` (the `["com.apple.finder"]`
   set + documented reasoning). Update `AppBootstrap.swift:186-187` and every test
   construction site the compile breaks name. GREEN: all new + existing tests pass.

3. **T3 — the seed pin + boundary row.** Add `SeededNoFieldAppsTests` (set contents
   `["com.apple.finder"]`, reasoning pin: the set's doc comment names the `.accessory`
   exclusion and the test asserts the set contains no `.accessory`-typical bundle). Add the
   `NSWorkspace` one-file family row to `InjectionSeamBoundaryTests` (S1): a planted
   `NSWorkspace` in another `VoccaInject` file must fail the scan (the planted-violation
   pattern the file already uses). RED→GREEN.

4. **T4 — full suite + floor.** `Scripts/test-with-floor.sh` — executed count rises (new
   tests), floor 1936 holds. Commit per task.

## Validation

- `swift test --filter AccessibilityRungTests` and `--filter TargetResolutionSurfaceTests` —
  RED at T1 end (compile breaks + missing gate), GREEN at T2 end.
- `swift test --filter InjectionSeamBoundaryTests` + the seed tests — GREEN at T3 end.
- `Scripts/test-with-floor.sh` — floor holds (T4).
- `git diff --stat` review: `InjectionLadderDecision.swift` and `TargetContext.swift` NOT in
  the diff (R5).

## Edge cases

- The compile-break RED across ~11 construction sites is mechanical — the agent updates each
  with the same three-argument shape; the recipe test documents the intended form.
- `FakeFrontmostAppReader` must default to answering nil (so unrelated tests keep the
  genuine-refusal semantics when they don't exercise the fallback).
- `isSecureInput` must be read exactly once per resolution (the fresh-read doctrine) — a
  read-count pin on the secure-input fake in the fallback-path test.
- No `NS*` identifiers in `TargetResolution.swift` (the boundary lints scan VoccaCore +
  Ladder/, but keep the file AppKit-name-free by doctrine anyway).

## Commits

- `target-resolution: frontmost-app fallback for the Chromium focused-field lie — RED pins, then GREEN (seam + gate + seed)`
- `target-resolution: pin the no-field seed and the NSWorkspace seam boundary (planted violations)`

## Checkpoints

- After T2: run the resolver/ladder filters, report counts.
- After T4: report the floor run + the `InjectionLadderDecision.swift`/`TargetContext.swift`
  no-change proof (R5).