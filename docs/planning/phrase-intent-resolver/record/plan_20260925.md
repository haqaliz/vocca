# Plan: record (2026-09-25)

Source: `../prd.md`, `./spec.md`. Last aspect; runs after the probe GREEN.

## Phase 1: SMOKE rows and the 148 correction

- `docs/SMOKE_CHECKLIST.md`:
  - **148: the correction.** The gesture's "enable `audit.clear` in the Actions tab" becomes the
    `action-config.json` hand-edit (the enablement row shape copied from
    `ActionConfigStore`'s byte pin). A dated *Correction* note keeps the original wording and
    cites F-B.
  - **New 154-156**, in the 148-150 shape (Gesture / Verify the state was entered / Pass /
    Void / Failure), recorded and never gated:
    - 154: a phrase for `audit.count` resolves and runs directly (read-only).
    - 155: edit the file mid-session, and the next turn resolves the new phrase.
    - 156: a shell-target row is refused, with the log line visible in `log stream
      --predicate 'subsystem == "dev.vocca.Vocca"'`.
- **Commit:** `record: SMOKE 154-156 and the 148 gesture correction`

## Phase 2: the unit record and the docs sync

- `docs/STATUS.md`: a new top entry in the shell-provider shape. It covers per-aspect
  "what shipped" with floor transitions; "Measured: nothing" (test counts only); and the
  honesty block from `record/spec.md`, including F-A, F-B and F-C (N1), the guardrail-7
  claim stated exactly ("two real classifiers, not composed together"), the narrowed
  unwired posture, and the process note: **no subagent tool was available in the planning
  session**, so the dig ran in the main thread.
- `CLAUDE.md`: a new status paragraph above the shell-provider one, the test floor, and
  the `resolver=PhraseIntentResolver` fact.
- `docs/technical/CAPABILITY_ROADMAP.md`: a C13 amendment. S1 shipped; what remains is
  coding-agent handoff, reply rendering, the composite resolver, an audit-tools arm section
  (F-A), the intent-seam shell leg, and time-boxed trust.
- `README.md`: only if it states the voice leg resolves nothing by default. Grep first.
- **Validation:** `./Scripts/test-with-floor.sh`, with `N == E` and the floor in the docs
  matching the script.
- **Commit:** `record: the phrase-intent-resolver unit record and the docs sync`
