# Plan: port-vetting — 2026-09-14

> Aspect of `kokoro-binding`. Requirements: `docs/planning/kokoro-binding/port-vetting/spec.md`
> + PRD R1/R2. Vetting evidence: `docs/planning/_card/understanding.md`. Executed by one
> agent under strict TDD; the agent stays on the `feat/kokoro-binding/aliz` worktree.

## Setup

- Worktree: `/Users/aliz/dev/at/vocca/.claude/worktrees/feat-kokoro-binding` (branch
  `feat/kokoro-binding/aliz`). Local toolchain: Swift 6.3.3 / Xcode 26.6 — the dependency
  builds locally. CI will prove the toolchain on the runner.
- The suite command is always `./Scripts/test-with-floor.sh` from the repo root (never a
  bare `swift test`). Floor: 1949.

## Vetted facts (already recorded — do not re-research)

- License: Apache-2.0, verified verbatim (both `Jud/kokoro-coreml` and `Jud/swift-bart-g2p`
  carry the same Apache-2.0 LICENSE blob).
- Model artifact: `kokoro-models.tar.gz` @ release `models-2026-03-23`.
- Phonemization: bundled English G2P + BART fallback — **not** Misaki (correction).
- Cancel path: per-sentence synchronous `synthesize` (R1b) — verified.
- Toolchain: both deps declare `swift-tools-version: 6.2`; the macos-15 runner image ships
  Xcode 26.0.1–26.3 (verified against the runner-images manifest), so
  `XCODE_MAJOR: "26"` resolves to Xcode 26.3.
- Port version: v0.11.2 latest; pin `.upToNextMinor(from: "0.11.2")` (0.x → minor is
  breaking; a silent update surfaces in the engine-binding suite).
- The port's product name is `KokoroCoreML`; its `Package.swift` deps: `swift-bart-g2p`
  (`BARTG2P` product — transitive), `swift-argument-parser`, `cbor` (CLI-only; resolved
  transitively but unused by our target — no action needed).

## Phase 1 — RED: the provenance pin

- **Goal:** a failing test that pins the dependency shape in `Package.swift`.
- **Files:**
  - Create `Tests/HarnessTests/KokoroDependencyTests.swift` — mirrors the repo's
    package-manifest test patterns (`Tests/HarnessTests/PackageManifest.swift` helper
    exists — use it or the established raw-text parse used by sibling tests):
    - assert `Package.swift` declares the kokoro-coreml package URL
      (`https://github.com/Jud/kokoro-coreml.git`);
    - assert the `VoccaSpeech` target's dependencies include the `KokoroCoreML` product
      from that package;
    - assert the `VoccaBootstrap` target depends on `VoccaSpeech`.
- **Validation:** `./Scripts/test-with-floor.sh --filter KokoroDependencyTests` → the new
  test FAILS (the manifest lacks the dep) while the rest of the suite stays green. The
  floor must still hold (the failing test is the RED; do not raise the floor yet).
- **Commit:** none yet (RED state is the checkpoint).

## Phase 2 — GREEN: Package.swift + CI bump

- **Goal:** the dependency lands and CI can build it.
- **Files:**
  - `Package.swift`:
    - `dependencies:` array (after the FluidAudio entry at `:36`):
      `.package(url: "https://github.com/Jud/kokoro-coreml.git", from: "0.11.2")`
      — note: SwiftPM requires the URL with `.git` or as given by the port; use the exact
      form `"https://github.com/Jud/kokoro-coreml.git"`.
    - `VoccaSpeech` target dependencies (`:114-118`):
      `["VoccaCore", .product(name: "KokoroCoreML", package: "kokoro-coreml")]`.
    - `VoccaBootstrap` target dependencies (`:139-152`): add `"VoccaSpeech"`.
    - Our package's `swift-tools-version` stays 6.0.
  - `.github/workflows/ci.yml:58`: `XCODE_MAJOR: "16" → "26"` (one env feeds all three
    jobs; the glob error path at `:76-82` fails loudly if the runner ever lacks it).
- **Validation:** `./Scripts/test-with-floor.sh` green locally (the dependency resolves,
  the provenance pin passes); the headless CI job is the authoritative toolchain proof.
- **Commit:** `kokoro-binding: vet the port and land the KokoroCoreML dependency + CI toolchain bump (Xcode 16 -> 26)`

## File-by-file build order

| File | Purpose | Depends on |
|---|---|---|
| `Tests/HarnessTests/KokoroDependencyTests.swift` | provenance pin (RED first) | PackageManifest helper |
| `Package.swift` | the dependency + product + bootstrap dep | — |
| `.github/workflows/ci.yml` | XCODE_MAJOR 26 | — |

## Testing strategy

- The provenance pin is the aspect's only new test (RED → GREEN in this aspect; the floor
  ratchets with it — see Phase 3).
- No model, no network: the test parses the manifest text only.
- CI is the toolchain test: the `headless` job must pass on Xcode 26.3.

## Environment & privacy

- No env vars. No runtime network: the fetch happens only at SwiftPM resolve/build time
  (CI/build machines), never inside the app or the probe.

## Edge cases

- SwiftPM URL spelling: if the `.git`-suffixed URL form fails to resolve, use the
  bare-URL form from the port's README (`https://github.com/Jud/kokoro-coreml.git` is
  what the port's own README uses — expect it to work).
- If resolution pulls a toolchain-version error despite Xcode 26: the runner's `swift`
  must come from Xcode 26's developer dir — `xcode-select` already handles it via the
  existing job step; only re-check if the error names the tools-version.

## Agent notes

- Do NOT touch the engine, the suite, the manifest, or the probe — those belong to the
  other aspects.
- Keep the floor unchanged until Phase 3 of this aspect; the RED→GREEN provenance pin is
  one test: raise the floor when it passes (add the ledger paragraph in the same commit —
  see the pattern at `Scripts/test-with-floor.sh:204-1567`; the last raise's paragraph is
  missing, restore the pattern with a `1949 -> 1950` row or the count at hand).
- Checkpoint for the integrator: the commit above + a green local run.

## Phase 3 — floor ratchet + checkpoint

- Raise `MINIMUM_EXECUTED_TESTS` in `Scripts/test-with-floor.sh:1569` with the exact new
  executed count from a full green run; append the ledger paragraph (same commit).
- **Validation:** full suite green; `git log --oneline` shows the aspect's commit(s).
- Hand off to `engine-binding`.