# Plan: wiring — plan_20260921.md

> Aspect spec: `docs/planning/action-surface-wiring/wiring/spec.md`. TDD, one agent task
> (the largest). Depends on all five prior aspects. This aspect's commit re-anchors G5
> exactly once.

## Setup

- Files in `Sources/VoccaBootstrap/` + `Sources/VoccaNetworkProbe/` + root slots in
  `AppBootstrap.swift`; tests in `Tests/HarnessTests/`.

## Phases

**Phase 1: RED — the composed contract**

- Files to create: `Tests/HarnessTests/ActionWiringTests.swift` (spec acceptances 1-7)
  driving a composed recipe over real temp stores and stub providers:
  - arm → confirm → invoke → audit reconstruct (headless, in-process);
  - binding mismatch → refused + fresh card presented (asserted by attempting the call);
  - arm while session in flight → refused;
  - the composed default reports `servers=0`, `spawnsSubprocess=false`;
  - wiring-family lint: no action-family name in the pinned dictation files; permitted
    set exactly {recipe, `AppBootstrap.swift`, probe drive};
  - transport prohibition lint's permitted set unchanged.
- Validation: RED.
- Commit: `wiring: RED — the composed action surface contract`.

**Phase 2: GREEN — the recipe and the composition**

- Files to create: `Sources/VoccaBootstrap/ActionWiring.swift` —
  `composeActionWiring(configStore:auditStore:sessionActive:root:) -> ActionWiring`
  (struct of closures: arm, preview, confirm, decline, discover, enable/disable,
  load/save config):
  - executor over the real store; policy floor `.none` **named in a comment as the
    recorded decision** (F1/F2 fail-safes confirm absent claims; stricter floor breaks
    M3 — cite `docs/planning/action-surface-wiring/prd.md` S1);
  - confirm: submit `.granted` with the exact sentence shown; mismatch → re-present a
    fresh card; decline → executor records the refused decision + dismiss;
  - arm refusal when `sessionActive()` is true;
  - probe-safe: nothing spawns/reads/starts at composition time.
- Files to modify:
  - `Sources/VoccaBootstrap/AppBootstrap.swift` — root slots (nullable, defaulted) +
    additive composition in `configure` (the context precedent) + the
    `spawnsSubprocess` fact fold into the root's report surface.
  - **G5 re-anchor in this same commit**: `shasum -a 256
    Sources/VoccaBootstrap/AppBootstrap.swift` → edit
    `Tests/HarnessTests/TurnTakingComposedAcceptanceTests.swift` (the pin line); the
    other two digests must not change (the pin asserts it).
- Validation: full suite green.
- Commit: `wiring: GREEN — the composition, the floor decision, and the G5 re-anchor`.

**Phase 3: GREEN — the probe line**

- Files to modify:
  - `Sources/VoccaNetworkProbe/` — the action drive runs the composed recipe (arm →
    confirm with the shown sentence → invoked → reload → reconstruct; the mismatch path;
    the default's fact line `servers=0 spawnsSubprocess=false`).
  - `Tests/HarnessTests/ZeroNetworkTests.swift` — the expected action lifecycle constant
    updated + its guard-the-guard test re-derived (refuse a weakened golden string).
- Validation: full suite green; the zero-network test runs the probe inside the
  interposer.
- Commit: `wiring: GREEN — PROBE-ACTION-SURFACE drives the composed default`.

**Phase 4: REFACTOR — the wiring-family lint**

- Files to create: `Tests/HarnessTests/ActionWiringSeamBoundaryTests.swift` — the
  `ContextWiringSeamBoundaryTests` shape: permitted set, family identifiers, no
  action-family names in the two pinned dictation files.
- Validation: full suite green.
- Commit: `wiring: REFACTOR — the wiring-family lint`.

## Edge cases

- Session-active flag at composition time: read lazily (a closure), never at composition
  (probe-safe).
- The executor's store at composition: real default directory in the app; the probe and
  tests inject temp dirs — the recipe takes the store, never constructs it.
- `spawnsSubprocess` fold: the report shows the composed *default* (no servers) — a
  configured server's fact is the server's own declaration, shown in the tab (D2).
- The card's confirm closure must carry the generation token (stale-card refusal, the
  confirmation-card aspect) — thread it through the wiring.

## Agent notes

- Read `Sources/VoccaBootstrap/ContextWiring.swift` in full — it is the template.
- The root slots pattern: nullable, defaulted, assigned after construction (the
  converse/context slots).
- Coordinate: this aspect lands after `executor` (its probe-drive migration) — the probe
  line's final shape belongs here.