# Plan: record — plan_20260921.md

> Aspect spec: `docs/planning/action-surface-wiring/record/spec.md`. One agent task; the
> final aspect. Requires the whole branch green first.

## Phases

**Phase 1: the unit record**

- Files to modify:
  - `docs/STATUS.md` — head entry: the six aspects shipped; the honesty block (no gate
    passes; ninth unit ahead of the uncleared gates; **N2's limit stated**: an approval
    asserts a human said yes and cannot verify it — the binding narrows what it can be
    replayed against, the seeing is asserted by the UI layer; R8 mitigated, not retired;
    D2: discovery spawns on explicit user action, default configuration cannot create a
    child; the `.none` floor recorded as a decision; the rendered card is executed by
    nothing in CI); the G5 re-anchor record; the floor; the "nothing is wired" sentence
    retired.
  - `CLAUDE.md` preamble — the `action-surface-wiring` status entry (current state, per
    convention; full record stays in STATUS.md).
  - `docs/technical/CAPABILITY_ROADMAP.md` — C13 amendment: slice 5 shipped; remaining
    C13 machinery named (intent layer incl. the "not confident" ask path, ShellProvider,
    coding-agent handoff, reply rendering); the §8 escape-valve note as the next slice's
    conversation.
  - `docs/technical/ARCHITECTURE.md` — the actions policy row: the `.none` floor
    decision, the executor, `action-config.json` (the file name), the D2 surface copy.
  - `docs/SMOKE_CHECKLIST.md` — steps 144-147 (recorded, never gated): D2 copy + no
    default server; arm → confirm → invoke → audit reconstruct on the real surface;
    enablement survives relaunch; the card shows the gate's sentence verbatim.
- Validation: doc-only; cross-check the four docs agree.
- Commit: `record: the action-surface-wiring unit record`.

**Phase 2: the floor ratchet**

- Run `Scripts/test-with-floor.sh` on the branch; take the executed count from the
  script's own parse; raise `MINIMUM_EXECUTED_TESTS` in
  `Scripts/test-with-floor.sh` in this same commit; re-run and verify `N == E`.
- Verify the G5 pin is green on the final tree (computed, not edited-to-match).
- Verify README's promise sentence is unchanged ("zero network calls and spawns no child
  process" — it still holds).
- Commit: `record: the floor ratchet <old> -> <new> (executed <new>)`.

**Phase 3: branch checks**

- Full suite green one final time; `git log` on the branch reads aspect-by-aspect;
  prepare the PR summary (the six aspects, the acceptances, the honesty block, the floor).

## Agent notes

- The STATUS entry cites the prior records' conventions (`record/spec.md` of the spine
  unit, the stdio-transport entry) — match the shape, update the numbers.
- Do not touch README's promise sentence unless a claim actually changed (it has not).