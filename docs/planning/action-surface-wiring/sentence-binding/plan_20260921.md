# Plan: sentence-binding — plan_20260921.md

> Aspect spec: `docs/planning/action-surface-wiring/sentence-binding/spec.md`. TDD: RED →
> GREEN → REFACTOR, one agent task. Suite: `Scripts/test-with-floor.sh` (floor 2629 —
> do not ratchet in this aspect; the record aspect owns the final ratchet).

## Setup

- No new modules, no Package.swift change, no dependency changes. VoccaCore stays
  Foundation-free.
- Test files: `Tests/HarnessTests/` auto-discovers new `.swift` files.

## Phases

**Phase 1: RED — pin the binding**

- Files to create: `Tests/HarnessTests/ActionGateSentenceBindingTests.swift`.
- Write the failing tests (spec acceptances 1-5) against `ActionGate.submit` with
  `approvedSentence:` — the parameter does not exist yet; the file must not compile until
  GREEN.
  - Mismatch sentence + granted → `.declined(.approvedSentenceMismatch)`, `invoke` called
    zero times (stub call log — `ActionProviderStubs.swift` doubles).
  - Matching sentence + granted → proceeds to invoke.
  - `approvedSentence == nil` + granted → today's behaviour (proceeds).
  - Key spelling/stable: `gate.approvedSentenceMismatch`.
  - Withheld + mismatch: pin whatever the gate already does for withheld (no change to
    existing withheld semantics; assert the decision is the existing withheld decline).
- Validation: `swift test --filter ActionGateSentenceBindingTests` fails (RED).
- Commit: `sentence-binding: RED — the approved sentence must match what the gate renders`.

**Phase 2: GREEN — the parameter and the refusal**

- Files to modify: `Sources/VoccaCore/Actions/ActionGate.swift` — add
  `approvedSentence: String? = nil` to `submit`; compare when `approval == .granted &&
  approvedSentence != nil`; add `ActionDeclineReason.approvedSentenceMismatch`
  (`reasonKey = "gate.approvedSentenceMismatch"`); refusal returns `.declined` with no
  summary (provider not reached — the existing declined semantics).
- All 42 existing call sites compile unchanged (default nil) — run the suite.
- Validation: full suite green via `Scripts/test-with-floor.sh`.
- Commit: `sentence-binding: GREEN — mismatch refuses by attempting the call`.

**Phase 3: REFACTOR — confinement notes**

- The new reason key joins `ActionDeclineReason` inside `ActionGate.swift` — Family A
  lints scan the file already; verify `ActionSeamBoundaryTests` green (no new family
  needed; the key is inside the permitted file).
- Doc comment: the N2 narrowing sentence (an approval asserts a human said yes; the
  binding narrows what it can be replayed against; the human's seeing is asserted by the
  UI layer).
- Validation: full suite green; `swift test` from a clean DerivedData if flaky.

## Edge cases

- Empty-string `approvedSentence`: a provider whose sentence renders empty cannot be
  approved with a shown empty string (mismatch on any non-empty render) — decide by test:
  pin that an empty shown sentence is treated as a mismatch (no sentence, no approval).
- Unicode: compare the exact `String` values (no normalization) — the UI passes back what
  it rendered, byte-for-byte.
- The gate must not call `describe` twice more than today's path already does (the
  comparison uses the decision's own summary — no extra provider calls).

## Agent notes

- Read `Sources/VoccaCore/Actions/ActionGate.swift` first (the check order doc comment).
- `ActionDeclineReason` is an enum in `ActionGate.swift`; keep keys bounded.
- If the withheld+mismatch pin (acceptance 5) reveals the gate's withheld path returns
  something unexpected, do not change withheld semantics — pin what exists and note it.