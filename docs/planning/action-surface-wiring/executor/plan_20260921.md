# Plan: executor — plan_20260921.md

> Aspect spec: `docs/planning/action-surface-wiring/executor/spec.md`. TDD, one agent
> task. Depends on the `sentence-binding` aspect (the `approvedSentence` parameter).

## Setup

- No new modules: `Sources/VoccaActions/` (auto-discovered). Dependency equality
  `["VoccaCore"]` unchanged.

## Phases

**Phase 1: RED — the executor contract**

- Files to create: `Tests/HarnessTests/ActionExecutorTests.swift` (spec acceptances 1-7).
  Failing tests: every decision class records exactly one reconstructable entry;
  reconstruction via a second store instance; recording failure → `auditRecorded ==
  false` + decision still returns (injected failing file-system seam); the executor's
  `approvedSentence` reaches the gate (mismatch refused); dry-run records `dryRun` with
  zero `invoke` calls (stub call log); no network/`Process` names in the new file.
- Validation: RED.
- Commit: `executor: RED — every gate decision lands in the audit store`.

**Phase 2: GREEN — the executor**

- Files to create: `Sources/VoccaActions/ActionExecutor.swift` — actor:
  - `struct ExecutedDecision { let decision: ActionDecision; let auditRecorded: Bool }`
  - `submit(_ invocation: ActionInvocation, enablement: ActionEnablement, policy:
    ActionRadiusPolicy, approval: ActionApproval, approvedSentence: String?, mode:
    ActionGate.Mode) async -> ExecutedDecision` — calls `ActionGate.submit`, maps the
    decision to an `ActionAuditEntry` (the existing init), records via the injected
    store; recording failure caught, logged loudly (injected `log` closure), never thrown.
  - Doc comments: the no-dropped-audit-record rule lives here (a failed record is a
    surfaced `false`, not a swallowed error); the approved-sentence obligation.
- Validation: full suite green (existing `ActionAuditDrive` in the probe still compiles —
  it builds decisions directly; the wiring aspect migrates it).
- Commit: `executor: GREEN — the gate→audit round trip`.

**Phase 3: REFACTOR — the probe drive body**

- Modify `Sources/VoccaNetworkProbe/ActionAuditDrive.swift` to drive the executor round
  trip (arm → confirmationRequired → grant with the shown sentence → invoked → reload →
  reconstruct) instead of hand-built decisions — the drive body changes, the report shape
  may grow a `binding` field; the probe line's expected constant in `ZeroNetworkTests.swift`
  must be updated in the same commit, and its guard-the-guard test re-derived. **This is
  the wiring aspect's probe line — coordinate: the wiring aspect owns the final expected
  constant; this aspect only replaces the decision source and updates the constant that
  exists.**
- Validation: full suite green.
- Commit: `executor: REFACTOR — the probe drives the executor, not hand-built decisions`.

## Edge cases

- Store unavailable (directory unreadable): `record` throws → caught → `auditRecorded ==
  false`, loud log; the decision stands (the action already happened).
- The clear tool's own record (written by `AuditActionProvider` after the clear) coexists
  with the executor's decision record — two entries for one clear; the counterfactual pin
  on ordering stays green (do not reorder).
- A declined-without-describe decision (toolNotEnabled) carries `blastRadius: nil` —
  the entry mapping already handles it; assert it in the reconstruction test.

## Agent notes

- Read `ActionAuditEntry.init(id:instant:invocation:decision:)` — it exists precisely for
  this mapping; do not build a parallel encoder.
- `ActionGate.Mode` is `live`/`dryRun`; `ActionRadiusPolicy.none` exists — the executor
  takes the policy as a parameter (the wiring supplies the decision).
- Coordinate the probe constant change with the wiring aspect if they land concurrently —
  sequence this aspect before wiring.