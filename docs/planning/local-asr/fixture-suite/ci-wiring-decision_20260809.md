# CI wiring decision — fixture suite

**Date:** 2026-08-09 · **Status:** recorded, wiring lands with the F1 runner verdict

## The two paths (per `spec.md` and the F1 spike)

| Verdict | Wiring |
|---|---|
| **GO** (the runner transcribes within budget) | A model-cache step in the headless job feeds `VOCCA_MODEL_DIR` (the `Scripts/provision-asr-fixtures.sh` output) into the suite; `ParakeetEngineWERTests` stops skipping and becomes the gate it already is on founder hardware. |
| **NO-GO** | The env-gated test keeps skipping visibly in CI; the founder runs it on hardware per `SMOKE_CHECKLIST.md` step 18. The seam-level harness (`ASRFixtureHarnessTests`) stays the CI-measured half either way. |

## The first real run (recorded)

On founder hardware (M4 Max, macOS 26.5.2), with the model provisioned by the script:

- `ParakeetEngineWERTests` **passed on the first attempt** — 15.4 s for `prepare()` + all six
  fixtures.
- All **provisional** tolerances met on the TTS stand-ins: clean/spike-clip word-perfect
  (WER 0), accented/60 s/noisy within their ceilings, the 200 ms clip's single word
  recovered.
- The provisional numbers stay provisional until the founder's real recordings (F2) land —
  TTS audio is unrepresentatively clean — but the machinery is now proven end-to-end:
  provision → store → engine → fixtures → scorer.

## The generated manifest

`Sources/VoccaASR/Models/Manifests/parakeet-tdt-0.6b-v3.json` is the real artifact's digest
registry (22 files, generated by the script from the model on this machine). It is immutable
per version (`PRODUCT_SPEC.md:273`); re-provisioning a different artifact must bump the
version, not rewrite the digests.
