# Implementation Plan: record-and-sync

**Date:** 2026-09-01 · **Feature:** p2-gate-measurement · **Spec:** `docs/planning/p2-gate-measurement/record-and-sync/spec.md`

## Context for the executing agent

Swift 6 package; suite via `Scripts/test-with-floor.sh` (floor 1731). This is the last
aspect: it records what aspects 1–5 measured into the single places the gates and the
next unit read. Docs-only work — no code change expected; the floor check still runs.

## Goal

The five tolerance files carry their measured-values rows; `STATUS.md` carries the
unit's entry with its honesty block; `SMOKE_CHECKLIST.md` tracked tables carry their
first real rows; `CLAUDE.md`'s front door is synced; the citation drift this unit
touched (S1) is corrected.

## Phase 1 — Measured-values rows (per-run, cited)

For each run that happened, add the measured row to **exactly** the file the procedure
names:

- `docs/planning/second-asr-engine/fixture-harness/tolerances_20260810.md` — WER both
  engines + whisper streamed rows (from aspect `real-engine-runs`).
- `docs/planning/warm-start-streaming/warm-start/tolerances_20260825.md` — warm-start
  ratio + re-warm rows (measured-values table).
- `docs/planning/speculative-asr/equivalence-measurement/tolerances_20260831.md` —
  equivalence table + key-up costs (measured-values table).
- `docs/planning/deterministic-cleanup/eval-harness/tolerances_20260815.md` — F2
  preference + margin (from aspect `cleanup-eval-f2`).
- `docs/planning/settings-live-controls/verification-smoke/tolerances_20260829.md` —
  artifact-hash table where step 19 produced one.

Every row cites its run. A run that did not happen gets no row — or a dated "not run +
reason" line where the file's convention allows.

## Phase 2 — STATUS.md entry

- Append the `p2-gate-measurement` entry (newest first): what the runs measured, what
  was fixed (each defect test-first), the founder-signed re-baselines, and the honesty
  block — what remains unmeasured (e.g. the external-users gate leg, the release, any
  step recorded "not performed").
- Follow the house voice: "**What this unit is NOT, and must not be claimed:**" naming
  every thing the unit does not prove.

## Phase 3 — SMOKE tracked tables

- Matrix tracked table first row (step 93: Release | Date | Rows run | Skipped | Voided
  | FMS | Notes — `SMOKE_CHECKLIST.md:1877-1879`).
- Equivalence verdict row (step 126), latency rows (71-72, 77, 128), F2 row (73).
- S1 sweep: correct the stale `AppBootstrap.swift` citations in the dictation-loop and
  matrix sections, and the stale step-67 preamble (the Settings → General
  activation-mode switch shipped 2026-08-26, `STATUS.md:534-542`) — **only the sections
  this unit executed**.

## Phase 4 — CLAUDE.md front door + final commit

- Update `CLAUDE.md`'s status paragraphs to describe the tree this branch ships in:
  the gate-measurement unit's outcome, the test floor (unchanged 1731 unless a defect
  fix changed it), and no gate passed.
- Commit: `sync the front door and the record with the p2-gate-measurement unit`

## Testing strategy

- `Scripts/test-with-floor.sh` after the final commit — the single-source scans must
  stay green (a re-baseline that duplicated a constant fails; a re-baseline that
  changed a value in a second file fails).
- No new tests expected; docs-only commits.

## Environment & privacy

- None. No env vars, no network, no founder data in the repo (the F2 corpus is cited by
  path, never committed).

## Edge cases

- A number the founder declined to re-baseline: stays provisional with a dated row
  saying so — "a number that moved with no row behind it is a number nobody can check"
  (`tolerances_20260829.md:67-69`).
- The 7-day gate log is outside the repo: the front door must not claim the P0 gate
  passed; day-1 existence is noted, nothing more.
- If a defect fix raised the floor, re-record the floor number in every doc that cites
  it (`CLAUDE.md`, `STATUS.md`, this plan's context) — single-source discipline applies
  to doc numbers too.

## Agent execution notes

- This aspect is a single coherent commit set; sequence: tolerances → STATUS → smoke
  tables → CLAUDE.md, one commit per file family, then the floor check.
- Checkpoint: present the STATUS.md entry draft to the founder before the final commit
  — the honesty block is the founder's record.