# Plan: record-and-sync (2026-09-09)

> Executes `record-and-sync/spec.md`. Runs after `matrix-run` produces its run log and tally.
> Pure documentation work on the feature branch; no code changes.

## Tasks (agents team)

1. **T1 — tracked-table row.** In `docs/SMOKE_CHECKLIST.md` (§12 tracked table,
   `:2000-2009`): append the v0.3.0 row. Columns per the table's existing format; FMS cell
   carries the memory-ordered number over 17 with the ceiling note. Do not touch the v0.2.1
   or v0.1.0 rows. Also confirm/flag the Slack seed line (`:1940`) per S1.
2. **T2 — STATUS.md entry.** Prepend one entry (newest-first) to `docs/STATUS.md`:
   - Header line naming the unit + date + build (v0.3.0) + floor (re-read from
     `Scripts/test-with-floor.sh`).
   - Per-row verdict table (row | verdict | rung | bytes) from the run log.
   - Step 92 four-condition record.
   - Notes/Mail re-probe observation (elapsed or not-elapsed with due date).
   - FMS tally line + verdict (ceiling 17/20, ≥19/20 structurally unreachable, NOT a gate
     pass) — verbatim from the run notes.
   - Harness changes shipped test-first (self-capture guard; memory-ordered FMS question)
     with RED→GREEN and the floor statement.
   - Dispositions: step 89 (not executed, reasons), step 90/91 (window observations),
     step 93 (this entry).
   - NOT block: no gate passes; no percentage outside the denominator discipline; voids are
     not passes; anything unmeasured named.
3. **T3 — CLAUDE.md sync.** Update the front-door status block's matrix sentence to the
   recorded state (one paragraph, matching the STATUS entry's facts; per the front-door
   rule this is the only CLAUDE.md edit this unit makes).
4. **T4 — S2 live-check offer + S3 notes.** If the founder consented, add the unified-log
   corroboration lines to the STATUS entry; record the skip rows' bundle IDs as guesses.

## Validation

- `git diff` review: STATUS entry is append-only (newest first, nothing rewritten);
  tracked table has exactly one new row; CLAUDE.md edit limited to the matrix paragraph.
- No test suite impact (docs only) — floor unchanged.
- Cross-check: every claim in STATUS matches the run-log JSONL (row names, verdicts,
  rung values, bytes flags).

## Commits

- `matrix-run: record the v0.3.0 tracked row, STATUS entry, and front-door sync`
- (any S1/S2/S3 follow-ups in the same commit, or a second doc commit if the founder's
  consent arrived later)

## Checkpoints

- Before T2: integrator confirms the run log path and the tally line from `matrix-run`.
- After T3: report the diff summary for the founder's review before pushing.