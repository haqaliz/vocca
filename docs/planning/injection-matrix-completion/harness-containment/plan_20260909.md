# Plan: harness-containment (2026-09-09)

> Implements `harness-containment/spec.md` (PRD R1, M3, M6, M8). Strict TDD: every change
> begins with a failing test. All work in the worktree
> `.claude/worktrees/feat-injection-matrix-completion` on branch
> `feat/injection-matrix-completion/aliz`.

## Task list (agents team — one agent per task, TDD inside each)

1. **T1 — RED pins for the self-capture guard.** Add to
   `Tests/HarnessTests/InjectionMatrixHarnessTests.swift`:
   - `testTheSelfCheckCatchesASelfCaptureCheckThatNeverFires` — planted violation swaps
     `is_self_capture`'s equality so it never fires; the copy's `--self-check` must fail
     with the self-capture pin's message.
   - `testTheSelfCheckCatchesAWiringThatDroppedTheGuard` — planted violation removes the
     guard call site + void message from `run_row`; `--self-check` must fail on the wiring
     grep.
   - `testTheSelfCheckRejectsAnEmptyHostAsNotSelfCapture` — a planted `is_self_capture` that
     fires on empty host must fail the self-check.
   All three RED against the shipped script (functions undefined / pins absent → self-check
   failure or anchor missing). Run: `Scripts/test-with-floor.sh` (or
   `swift test --filter InjectionMatrixHarnessTests`) — RED expected, floor not yet enforced
   on new tests.

2. **T2 — GREEN: the guard.** Edit `Scripts/injection-matrix.sh`:
   - Add `host_terminal_bundle_id()` (parent-chain walk from `$$`; first command path
     containing `.app/Contents/MacOS/`; derive app path; `plutil -extract CFBundleIdentifier
     raw "<app>/Contents/Info.plist"`; empty + no error when absent; guard every `ps`/`plutil`
     call with `|| true`; bounded walk depth 8).
   - Add `is_self_capture() { [ -n "$1" ] && [ -n "$2" ] && [ "$1" = "$2" ]; }`.
   - Extend `self_check` with the three behavioral pins (equal fires / differing ids do not /
     empty host does not) + the host-shape pin (non-empty ⇒ contains a dot) + the wiring grep
     (call site + `self-capture` message present in the script text).
   - Wire into `run_row`: after the frontmost-aim check passes and before the sentinel copy —
     `if is_self_capture "$activation_id" "$(host_terminal_bundle_id)"; then VOID … return 3`.
     Apply to terminal-class rows only (guard on class == `terminal`).
   - GREEN: the three planted tests + full harness suite pass.

3. **T3 — RED pin for the FMS question.** Add
   `testTheSelfCheckCatchesARowThatRecordsNoLandingRung` — planted violation restores the old
   y/N question text in `run_row`; the wiring grep for the landing-rung question must fail.
   RED against the current script.

4. **T4 — GREEN: the FMS question.** Edit `run_row`'s confirmation half (spec R2):
   - Landing-rung prompt (closed vocabulary; `none` refused for deliverable rows).
   - First-attempted y/N prompt.
   - Verdicts per spec (byte-mismatch / fallback / expected-landed PASS / memory-ordered
     PASS with note).
   - `log_run_row` records the observed landing rung in `rung` for every deliverable row.
   - The `full_run` tally prints both tallies: FMS (memory-ordered) and expected-rung
     calibration — the summary line names both numbers explicitly.
   - GREEN: planted test passes; self-check passes; full suite green.

5. **T5 — manual behavioral proof (founder, recorded).**
   - `bash -c 'host_terminal_bundle_id'`-equivalent check: `--row Terminal` run **from inside
     Terminal.app** → verdict `voided`, note `self-capture: harness runs inside the target
     terminal`, exit 3, one JSONL line. (This is itself the Terminal self-capture void
     artifact — reuse it in the run.)
   - Confirm the same for `--row Warp` from inside Warp (optional; one of the two suffices as
     the proof, the other's guard is identical code).
   - CI shape: `swift test --filter InjectionMatrixHarnessTests` passes (host empty).

6. **T6 — floor + full suite.** `Scripts/test-with-floor.sh` — executed count rises (new
   tests), floor 1930 holds. Commit per task on the feature branch.

## Validation

- `swift test --filter InjectionMatrixHarnessTests` — GREEN at T2/T4 end.
- `Scripts/test-with-floor.sh` — floor 1930 never drops (T6).
- Manual void proof (T5) recorded in the run log + noted in the STATUS record.

## Edge cases

- `set -euo pipefail`: every discovery command must be failure-tolerant (`|| true`), else a
  `ps` hiccup aborts the whole harness.
- `log show`-reading is NOT used by the harness (founder observes the log; file chain stays
  load-bearing — spec keeps the founder in the loop).
- Old run-log lines remain valid (schema unchanged).
- The refusal rows' flow untouched — step 92 semantics preserved exactly.

## Commits

- `matrix-harness: pin the self-capture guard — RED, then GREEN (containment pins)`
- `matrix-harness: record the landing rung and count memory-ordered FMS (RED pin first)`
- `matrix-harness: prove the self-capture void from a real terminal (manual, recorded)`

## Checkpoints

- After T2 and T4: run the harness suite, report counts.
- T5 needs the founder (one terminal row run).
- Flag to the integrator: the FMS-question change is the plan-level boundary extension of
  the `harness-containment` aspect (approved at plan gate 2026-09-09).