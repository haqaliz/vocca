# Plan: matrix-run (2026-09-09)

> Executes `matrix-run/spec.md`. Semi-automated by design: the founder dictates each row
> (~30 s/row) and answers the landing-rung question from the ladder's log. The harness
> records the machine half; the founder records the log half. Total founder time ~20-30 min
> plus precondition setup. All commands from the worktree
> `.claude/worktrees/feat-injection-matrix-completion`.

## Preconditions (record each before the first row)

1. `git -C /Users/aliz/dev/at/vocca worktree list` — branch `feat/injection-matrix-completion/aliz`.
2. Vocca v0.3.0 installed and running, armed (Accessibility grant, tap live), model prepared:
   `defaults read /Applications/Vocca.app/Contents/Info CFBundleShortVersionString` → expect
   `0.3.0`.
3. `Scripts/injection-matrix.sh --verify-bundle-ids` → expect `0 mismatched` (ChatGPT row:
   `com.openai.codex` CONFIRMED against `/Applications/ChatGPT.app` — verified 2026-09-09).
4. `Scripts/injection-matrix.sh --self-check` → passes (post `harness-containment`).
5. `strategies.json` snapshot: `cat ~/Library/Application\ Support/Vocca/strategies.json`
   → save to the run notes (memory active — NO reset).
6. Automation grants: Terminal hosting the harness must have Automation grants for the target
   apps; a denied grant VOIDs per-row by design.

## Run procedure (one `--row` per row, same run log via `--run-log`)

```
LOG=~/Library/Application\ Support/Vocca/matrix-runs/20260909-v03.jsonl
# Terminal's row from inside Warp; Warp's row from inside Terminal.app:
# (the shell above — wherever this is run from — is the non-target terminal for each)
```

1. **Notes/Mail re-probe** (if date ≥ 2026-09-10; else record not-elapsed, due 2026-09-10):
   - `--row Notes` — expect the memory to re-probe `.accessibility`; observe whether it
     lands or is re-demoted; record both the bytes result and the landing rung.
   - `--row Mail` — same.
2. **The 7 unrun rows:** `--row VSCode`, `--row Teams`, `--row Discord`, `--row ChatGPT`,
   `--row Obsidian`, `--row Safari`, `--row GoogleDocs` (dictate into the Google Docs body —
   hostile seed must force `.clipboardPaste` as first attempt).
3. **The 4 voided re-runs:** `--row Messages`, `--row Firefox` (aim-fixed harness),
   then `--row Terminal` **from Warp**, `--row Warp` **from Terminal.app**.
4. **Step 92 (refusal rows):** `--row Passwords`, `--row PasswordField` — all four
   conditions verified and named:
   (a) log records `attempted: []`; (b) failsafe shows the password-field copy;
   (c) transcript present and copyable; (d) `strategies.json` gained nothing for the bundle id.
5. **Per-row record discipline:** after each row, the founder answers the landing-rung
   question from the unified log (`log show --predicate 'subsystem == "dev.vocca.Vocca"'
   --last 30m | grep "delivery target="`), never from memory. Note the `attempted:` trace's
   first element — that is the memory-ordered first method.

## FMS tally (after the last row)

- FMS numerator = rows with bytes-matched delivery whose landing rung was the first
  attempted rung (memory-ordered). Denominator = 17 (deliverable rows minus the 3 permanent
  skips). Refusals excluded from both.
- Expected-rung landings = separate calibration tally.
- Verdict line (record verbatim):
  `FMS = <n>/17 (<pct>%) memory-ordered; expected-rung landings <m>/17; ≥19/20 bar
  structurally unreachable on this machine (3 permanent skips → ceiling 17/20); NOT a gate
  pass.`
- No percentage may be quoted outside this denominator discipline.

## Dispositions (record, do not execute)

- **Step 89:** Google Docs fresh-memory half NOT executed — a memory reset mid-unit would
  wipe the Notes/Mail demotions and promotion-candidate windows the run depends on; recorded
  with the reason. Slack half unrunnable while the row is Teams — recorded.
- **Step 90:** Notes/Mail re-probe observed (above); ≥5-clipboard-delivery accumulation
  continues via daily use (feeds the P0 gate too).
- **Step 91:** promotion candidates (Xcode, Telegram, Chrome) windows open ~2026-09-12 —
  recorded not-elapsed; the observation belongs to a follow-up.
- **Step 93:** tracked-table v0.3.0 row → record-and-sync.

## Edge cases (M8 rule)

- A harness defect surfaced by a row → fix test-first (harness-containment's task pattern),
  then continue; the affected row re-runs.
- An evidence-chain-invalidating defect (run-log corruption, byte-compare false positive) →
  stop the run, void the affected rows with the reason, escalate to the integrator.
- A row that VOIDs (frontmost mismatch, no Automation grant) → re-run it once after fixing
  the precondition; a second void is recorded with the reason.
- If the founder cannot run today: the plan holds; Notes/Mail re-probe becomes due
  2026-09-10 — run those rows first on the actual run date.

## Checkpoints

- After step 1 (Notes/Mail): report re-probe outcome to the integrator.
- After step 4: report step 92's four-condition results.
- After the tally: the FMS verdict line goes to the integrator for record-and-sync.