# Implementation plan: action-round-trip

> Date 2026-09-22 · Aspect of `intent-layer` (C13 slice 6). Strict TDD: RED → GREEN →
> REFACTOR, one commit per step. Work in `.claude/worktrees/feat-intent-layer` on
> `feat/intent-layer/aliz`. Suite: `Scripts/test-with-floor.sh`.

## Setup

- Depends on `intent-seam` (vocabulary) and `converse-step` (the handler slot).
- Reference the shipped recipe to mirror: `Sources/VoccaBootstrap/ActionWiring.swift`
  (arm/confirm/decline at lines 264-368, policy floor at 203, the generation mint at
  159-165) and the root slots at `AppBootstrap.swift:1520-1547`.

## Phase C1 — RED: the round-trip contract

- Files to create:
  - `Tests/HarnessTests/IntentRoundTripTests.swift` — failing tests for acceptance
    1-5 over probe doubles: a real temp-directory audit store, a stub provider with a
    scripted call log (the `ProbeActionProvider` shape), the real `ActionGate` path via
    the recipe-under-test (a `composeIntentWiring` preview on a probe root, or the
    recipe's closures driven directly).
  - `Tests/HarnessTests/EscapeValveTests.swift` — failing test for acceptance 6: the §8
    floor enumerated over approval values (`.withheld`/`.granted`), policy floors
    (`.none` and a raising floor), and modes (`.live`/`.dryRun`) — an outward-facing
    invocation never auto-runs.
- Step order: tests first; they do not compile (no recipe yet) — that is the RED.
- Commit: `action-round-trip: RED — the voice round trip and the §8 floor`

## Phase C2 — GREEN: the IntentWiring recipe

- Files to create/modify:
  - `Sources/VoccaBootstrap/IntentWiring.swift` — `public struct IntentWiring<Provider:
    ActionProvider>` (the `ActionWiring` shape) with the closure set: resolve (catalog
    from enablement → resolver), performAction (executor leg + card guard + spoken ack),
    plus the executor instance and the policy floor (`.none`, recorded).
  - The recipe `composeIntentWiring` static — construction-only, I/O-free, window-free,
    stores consulted per call (probe-safe by construction, the `ActionWiring.swift:183-189`
    doctrine).
- Validation: `IntentRoundTripTests` + `EscapeValveTests` green; the existing action
  suite untouched (no changes to gate/executor/store/config).
- Commit: `action-round-trip: GREEN — the IntentWiring recipe`

## Phase C3 — REFACTOR: pins and the handler slot

- Files:
  - `Tests/HarnessTests/ConverseIntentStepTests.swift` — the handler the driver calls is
    the wiring's performAction (the seam between this aspect and `converse-step`: one
    integration test drives a scripted driver with the real recipe over probe doubles —
    utterance → card → confirm → audit row, end to end).
  - `Tests/HarnessTests/IntentSeamBoundaryTests.swift` — the wiring joins the permitted
    set for the intent families.
- Validation: full suite green.
- Commit: `action-round-trip: REFACTOR — the driver-handler integration and the table`

## Edge cases to handle

- `auditRecorded == false` → the bounded failure ack, never a success ack.
- The card-up guard reads the store's non-nil confirmation (`WidgetStateStore.swift:94-107`)
  — lazily, per call.
- `ActionInvocation` construction refusal (empty ids, >4 KB arguments) → the recipe
  turns it into `.ask`/silent, never a crash.
- The mismatch re-prompt (approvedSentence drift) is inherited from
  `ActionWiring.confirm` — no new code, asserted by the integration test.