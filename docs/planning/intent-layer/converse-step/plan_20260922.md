# Implementation plan: converse-step

> Date 2026-09-22 · Aspect of `intent-layer` (C13 slice 6). Strict TDD: RED → GREEN →
> REFACTOR, one commit per step. Work in `.claude/worktrees/feat-intent-layer` on
> `feat/intent-layer/aliz`. Suite: `Scripts/test-with-floor.sh`.

## Setup

- Depends on `intent-seam` having landed (the `IntentResolution` vocabulary).
- Enumerate every `ConverseLoopDriver(` call site first: `Sources/VoccaBootstrap/
  ConverseWiring.swift` (the recipe), `Sources/VoccaNetworkProbe/ConverseLoopDrive.swift`,
  `Tests/HarnessTests/ConverseLoopDriverTests.swift` (the pin), and any test doubles.
  The widening commit touches all of them.

## Phase B1 — RED: the intent step's behavior

- Files to create:
  - `Tests/HarnessTests/ConverseIntentStepTests.swift` — failing tests for acceptance
    1-4, driving a scripted driver over stub providers: an `.ask` resolution is spoken
    and no provider is touched (call-log assertion); a `.toolCall` routes through the
    stub handler whose reply is spoken; two consecutive `.ask`s fall through to the
    reply generator; `.none`/nil reproduces the echo path.
- Step order: tests first. They do not compile against today's 12-parameter init — that
  is the RED.
- Commit: `converse-step: RED — the intent step's contract`

## Phase B2 — GREEN: the widened driver

- Files to modify:
  - `Sources/VoccaBootstrap/ConverseLoopDriver.swift` — add the two lazy closures
    (defaulted so existing call sites compile unchanged where the defaults are
    nil-safe no-ops: `intentProvider: @Sendable (String) async -> IntentResolution? = {
    _ in nil }`-shaped, `intentActionHandler: … = { _ in nil }`), the pipeline branch
    between `clean` and `replyGenerator.reply` (`ConverseLoopDriver.swift:326`), the
    bounded re-ask counter (per-session, reset on any non-`.ask` outcome).
  - `Sources/VoccaBootstrap/ConverseWiring.swift` — pass the wiring-supplied closures
    (the recipe receives them as parameters or composes defaults).
  - `Sources/VoccaNetworkProbe/ConverseLoopDrive.swift` — the probe's doubles gain the
    defaulted closures (behavior unchanged: the fallback default work still echoes).
  - `Tests/HarnessTests/ConverseLoopDriverTests.swift` — the pin updated deliberately
    with the two new parameters.
- Validation: `ConverseIntentStepTests` green; the pre-existing driver tests green with
  only the pin's parameter changes (any behavior diff is a violation — the unwired case
  must be byte-identical, asserted by the untouched test bodies).
- Commit: `converse-step: GREEN — the intent step between cleanup and reply`

## Phase B3 — REFACTOR: the prohibition leg

- Files:
  - `Tests/HarnessTests/ConverseWiringSeamBoundaryTests.swift` — extend the permitted
    tables: the intent families may be named in the driver, the recipe, and the probe
    drive; the `TextInjector` prohibition leg re-checked (the driver must not name it).
- Validation: suite green.
- Commit: `converse-step: REFACTOR — the seam table and the TextInjector leg`

## Edge cases to handle

- Handler returns nil (silent) or a bounded error string — never a throw (the driver's
  silent-return discipline; `ConverseLoopDriver.swift:294-306`).
- The re-ask bound resets correctly across a resolved action and a barge-in.
- The `.ask` question is spoken via the same reply path (the window stays open for the
  user's next turn — the natural re-resolution loop).