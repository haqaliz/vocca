# Implementation plan: record

> Date 2026-09-22 · Aspect of `intent-layer` (C13 slice 6) · Final aspect — written from
> the code aspects' true outcomes, not from the PRD's intentions.

## Setup

- Depends on all code aspects having landed and the suite being green at the raised
  floor. Work in `.claude/worktrees/feat-intent-layer` on `feat/intent-layer/aliz`.

## Phase E1 — The unit record and docs sync

- Files to modify:
  - `docs/STATUS.md` — head entry in the slice-record shape: per-aspect shipped summary,
    the decisions (all of the PRD's "Decisions made in this unit", verbatim where they
    bind), the inherited-caveat honesty block (R8/N2 stated, the D3-shaped seam claim
    stated as whichever is true, D2 unchanged, the §8 deferrals with blockers, the
    seeds-code-level tuning path), the no-gate-passes posture, the floor.
  - `CLAUDE.md` — the top paragraph describing the tree state at the tip.
  - `docs/technical/CAPABILITY_ROADMAP.md` — the C13 amendment naming what remains
    unbuilt (ShellProvider, coding-agent handoff, reply rendering, S1 phrase table,
    time-boxed trust).
  - `docs/technical/ARCHITECTURE.md` — the intent seam row (seam, implementations, the
    §8 decision in the policy row).
- Commit: `record: the intent-layer unit record and the docs sync`

## Phase E2 — SMOKE rows and the floor ratchet

- Files to modify:
  - `docs/SMOKE_CHECKLIST.md` — section "## 23. Intent layer — `intent-layer`" with rows
    148-150, each in the established shape (Gesture / Verify-the-state-was-entered /
    Pass / Void-not-fail / Failure), all marked recorded-never-gated, no percentage
    claims; the spoken-ack copy marked provisional pending the founder's real run.
  - `Scripts/test-with-floor.sh` — `MINIMUM_EXECUTED_TESTS` raised to the executed
    count **in the same commit as the test-growth commits** (the ratchet rule; if the
    growth commits already raised it, this phase only verifies).
- Validation: `Scripts/test-with-floor.sh` green; `git status` clean.
- Commit: `record: SMOKE 148-150 and the floor ratchet`

## Phase E3 — Planning artifacts committed

- Commit the planning trail (`docs/planning/_card/issue.md`, `_card/understanding.md`,
  `docs/planning/intent-layer/**` — PRD, specs, plans) — the `_card` commit first (the
  unit's source), then the PRD/plans with the code aspects, or a single planning commit
  at the start of the branch; the repo's precedent is "card+plan" in one commit
  (`673c912`). Decide at the tip against the branch's actual history.

## Edge cases to handle

- If an aspect shipped less than the PRD planned (e.g. S1 skipped), the record says so
  and the C13 amendment names it — never a silent drift.
- If the floor's executed count disagrees with the raised constant, trust the floor
  script's own parse (the ratchet commit convention) and fix in the same commit.