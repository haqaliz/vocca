# Implementation plan: probe

> Date 2026-09-22 · Aspect of `intent-layer` (C13 slice 6). Strict TDD: RED → GREEN →
> REFACTOR, one commit per step. Work in `.claude/worktrees/feat-intent-layer` on
> `feat/intent-layer/aliz`. Suite: `Scripts/test-with-floor.sh`.

## Setup

- Depends on `intent-seam`, `converse-step`, `action-round-trip` having landed.
- Reference: the PROBE-ACTIONS quartet in `Tests/HarnessTests/ZeroNetworkTests.swift`
  (constant at 474-499, verbatim assertion at 990-1031, accessor at 2059-2070,
  guard-the-guard at 1801-1899) and the drive at `Sources/VoccaNetworkProbe/
  ActionAuditDrive.swift`; the default-configuration driver at
  `VoccaNetworkProbe.swift:241-374`; the root slots at `AppBootstrap.swift:1520-1547`;
  the G5 pin at `TurnTakingComposedAcceptanceTests.swift:311-347`.

## Phase D1 — RED: the PROBE-INTENT quartet

- Files to create/modify:
  - `Tests/HarnessTests/ZeroNetworkTests.swift` — `expectedIntentLifecycle` constant,
    the verbatim assertion, `intentPayload(of:)`, and the guard-the-guard test (fails
    closed on empty/malformed/repeated fields; refuses a constant the drive could fake:
    `intentResolved=0` must be an effect of the composed default, `spawnsSubprocess`
    must be asserted false). Tests fail: no drive emits the line yet.
- Commit: `probe: RED — the PROBE-INTENT quartet`

## Phase D2 — GREEN: the drive and the composition

- Files to create/modify:
  - `Sources/VoccaNetworkProbe/IntentDrive.swift` — `exerciseIntent() -> IntentDrive`
    composing the recipe over probe doubles: real temp-directory stores, a stub provider
    with a call log, the real gate path; full round trip (resolve → confirmationRequired →
    present → confirm via the wiring's closures → audit row reconstructs); report fields
    read off the store's own answer and the provider's call log; the module witness;
    `print("PROBE-INTENT\t\(report)")` added in `exerciseDefaultConfiguration()` next to
    the PROBE-ACTIONS line (`VoccaNetworkProbe.swift:362`).
  - `Sources/VoccaBootstrap/AppBootstrap.swift` — the additive composition block:
    `composeIntentWiring` over `root.actionExecutor`, the enablement catalog, and the
    composed default's `NullIntentResolver`; new nullable root slots
    (`intentWiring: IntentWiring<AuditActionProvider>?`, `intentResolved` fact carrier);
    the default-configuration facts folded into the probe report (`intentResolved=0`,
    `spawnsSubprocess=false`).
  - `Sources/VoccaBootstrap/IntentWiring.swift` — any composition-only seams the drive
    needs (store injections, resolver selection).
- Validation: the quartet green; the full suite green; `ModuleBoundaryTests` still green
  (only `VoccaNetworkProbe` imports `VoccaBootstrap`).
- Commit: `probe: GREEN — PROBE-INTENT drives the composed intent wiring`

## Phase D3 — REFACTOR: pins and lints

- Files:
  - `Tests/HarnessTests/TurnTakingComposedAcceptanceTests.swift` — the G5 re-anchor:
    `AppBootstrap.swift`'s digest recomputed in this commit (deliberate, never
    edit-to-match); the two dictation digests unchanged.
  - `Tests/HarnessTests/ActionWiringSeamBoundaryTests.swift` (or a sibling intent lint
    leg) — the wiring-family table gains `IntentWiring.swift` + `AppBootstrap.swift` +
    `IntentDrive.swift`; the pinned-dictation-files never-name-intent guard; planted +
    comment-strip controls.
- Validation: suite green at the raised floor.
- Commit: `probe: REFACTOR — the G5 re-anchor and the wiring-family lint`

## Edge cases to handle

- The drive's report must not fake `intentResolved=0` for the full-round-trip run (the
  round trip is the probe's *other* composition; the default-configuration run is the
  null-resolver one — two distinct report lines or one composed report with both facts,
  decided by the aspect implementer against the guard-the-guard's per-field reading).
- The settle window holds the process open; the drive must not deadlock the probe child
  (the semaphore + run-loop pattern, `ConverseLoopDrive.swift:66-77`).