# Plan: claims-and-record

> `docs/planning/release-distribution/claims-and-record/spec.md` · 2026-09-03 · Phase 1 MUST
> merge before the v0.2.0 tag (it ships in the release notes); Phases 2-4 land after the
> release exists.

## Phase 1 — Release-notes heredoc (before the tag)

- **Goal:** the v0.2.0 release notes carry the measured truth.
- **Files:** `.github/workflows/release.yml` — the notes heredoc (`release.yml:244-246`):
  replace "**This build has not been proven to dictate.** … unmeasured" with the corrected
  paragraph: the loop has delivered on a real machine (SMOKE 62-68, founder-reported,
  `p2-gate-measurement`); the injection matrix, the latency gate, and notarization are not
  yet published — "treat this as a look at what's possible, with those numbers still
  pending". Keep the `xattr` + `LSUIElement` instructions.
- **Validation:** `git diff` review; no test change needed (the floor stays 1755 here).
- **Commit:** `release-distribution: correct the release notes' stale claims`
- ⛔ **Ordering:** this PR merges before version-bump's tag push. If both are in one PR,
  that PR contains: CaskVersionTests (RED) → bump (GREEN) → notes fix — committed in that
  order.

## Phase 2 — README callouts

- **Goal:** the front door says what is true.
- **Files:** `README.md` — status block (`:56-58`): "the loop has delivered on a real
  machine; the matrix and latency numbers are not yet published; notarization is the next
  distribution milestone". Install callout (`:74-76`): same correction. The quarantine
  instructions (`:98-111`) stay — still required until notarization.
- **Validation:** read-through; no test change.
- **Commit:** `release-distribution: correct the README's stale claims`

## Phase 3 — Runbook disposition

- **Goal:** the runbook's status section reflects v0.2.0.
- **Files:** `docs/planning/notarization/runbook.md` — replace "Status as of v0.1.0"
  (`runbook.md:12-46`) with "Status as of v0.2.0": what executed (DMG packaging gate on the
  real bundle, cask shipped, install proven, notes/README corrected), and the blocked list —
  Steps 0 (program not purchased), 1 (Developer ID cert), 2 (identity switch), 3 (notarize
  + staple), 5 (TCC reset tests), 7 (quarantine removal), 8 (Sparkle) — each named
  **blocked — not purchased**.
- **Validation:** every step 0-8 has an explicit state line.
- **Commit:** `release-distribution: record the runbook disposition at v0.2.0`

## Phase 4 — STATUS + CLAUDE + floor

- **Goal:** the house record-and-sync.
- **Files:**
  - `docs/STATUS.md` (new entry, top, with "What this unit is NOT, and must not be
    claimed:"): first real DMG release + cask + install proof; claims corrected; runbook
    steps recorded blocked-not-purchased; no gate passed; matrix/latency-gate numbers still
    unmeasured; notarization remains unproven; floor 1756.
  - `CLAUDE.md` — front-door sync: one sentence describing the release state (installable
    DMG + cask, notarization pending the $99 purchase).
  - Floor re-record if the count moved.
- **Validation:** `./Scripts/test-with-floor.sh` green; STATUS entry cites the run URL, the
  SHA256SUMS line, and the install transcript.
- **Commit:** `release-distribution: record the unit`

## Edge cases

- **The tag run fails after all fixes:** claims still land (they are true of the tree); the
  STATUS entry records the failure and the retry — never a silent pass.
- **No second Mac:** S2's record says so (cask-ship Phase 5), no assumption.
- **Floor moves:** if a defect fix adds a test, the floor is re-recorded with its count —
  never edited in place without the STATUS entry naming it.

## Agent execution notes

- Phase 1 is the only one with ordering pressure (before tag). Phases 2-4 are sequential
  doc edits; one agent can own all four, or two agents (1, then 2-4) — Phase 1 is a
  separate PR from 2-4 if the tag is imminent.
- All records must be cite-able: run URL, artifact names, command transcripts.