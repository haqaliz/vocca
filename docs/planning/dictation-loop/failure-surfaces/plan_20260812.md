# Plan — `failure-surfaces` (2026-08-12)

> Source: `docs/planning/dictation-loop/failure-surfaces/spec.md`; PRD R5. Strict TDD:
> RED test → GREEN implementation → REFACTOR, per task. Commit per task on
> `feat/dictation-loop/aliz`.

## Setup

Swift package, no env needed. Test command (worktree root):
`./Scripts/test-with-floor.sh` (full suite; runs `swift test` with the floor check).
Focused during RED/GREEN: `swift test --filter FailsafeStateReducerTests`,
`--filter FailsafeCopyTests`, `--filter JournalTranscriptHolderTests`.

## Task 1 — The two reason cases (RED first)

- **Files to modify**: `Sources/VoccaCore/FailsafeReason.swift` (add `.modelUnavailable`,
  `.transcriptionFailed` raw cases — journal-safe spellings, placed before the reserved
  `.accessibilityRevoked`).
- **Tests (written first)**: `Tests/HarnessTests/FailsafeReasonTests.swift` (new) —
  round-trip the new raw values through a `RecoveryJournal` (the journal's persisted
  schema is the FailsafeReason consumer): hold a `HeldTranscript` with each new reason,
  reload, assert `reason` round-trips and `FailsafeCopy.reasonText(for:targetAppName:)`
  renders non-empty copy for both, without the ladder affordances text.
- **Validation**: focused filters green; full suite green.
- **Commit**: `Add the two voice-processing FailsafeReason cases with journal-safe spellings`

## Task 2 — Reason-only panel state (RED first)

- **Files to modify**: `Sources/VoccaUI/FailsafeState.swift` (new state case
  `.reasonOnly(FailsafeReason)` + new action `.reasonShown(FailsafeReason)`; reducer
  rule: `.reasonShown` → `.reasonOnly` from any non-hidden state; from `.reasonOnly` the
  **only** exit is `.dismissRequested` → `.hidden`; no time-based transition exists),
  `Sources/VoccaUI/FailsafeCopy.swift` (the two copy entries; affordances line empty for
  reason-only), `Sources/VoccaUI/FailsafeView.swift` (reason-only rendering: reason text
  where the transcript would sit, no copy/retry affordances), `Sources/VoccaUI/
  FailsafePanel.swift` (new `@MainActor` entry: `presentReasonOnly(_ reason:
  FailsafeReason)` — dispatch reducer action + showWindow, mirroring
  `presentHeldTranscript`).
- **Tests (written first)**: extend `Tests/HarnessTests/FailsafeStateReducerTests.swift`
  — decision table over the closed action set: `.reasonShown` from `.hidden` and from
  `.presenting`; `.copyRequested`/`.retryRequested` in `.reasonOnly` are no-ops;
  `.dismissRequested` from `.reasonOnly` → `.hidden`; no action reaches a time-based
  transition (fold every action from `.reasonOnly`, assert state set is
  `{reasonOnly, hidden}`).
- **Validation**: focused filters green; full suite green; `swift build` clean.
- **Commit**: `Add the reason-only failsafe state: dismiss-only, time-free`

## Edge cases

- A reason-only notice must never show ⌘C/⏎ affordances (no text exists to copy).
- The two new cases must not be produced speculatively by other code — only the
  `loop-wiring` aspect produces them (this plan adds no producer).
- Journal capacity/order behavior unchanged; only the schema vocabulary grows.

## Agent notes

- Keep the reducer's closed action set exhaustive — no `default:` (the repo's switch
  discipline).
- `FailsafePanel` is `@MainActor` (NSPanel); the new entry point must be too.
- Verify with `swift test --filter ModuleBoundaryTests` that no module boundary moved.
