# Plan — `smoke-checklist` (2026-08-12)

> Source: `docs/planning/dictation-loop/smoke-checklist/spec.md`; PRD metric 1 + R8-4.
> Docs-only (no code). Lands last, against the shipped behavior of `loop-wiring` +
> `widget-live-states`. Single task.

## Task 1 — The loop's first-execution entries

- **File to modify**: `docs/SMOKE_CHECKLIST.md` — add a new section (following the
  existing numbered-step + gesture/expected-result discipline of steps 22-35) covering:
  1. **First dictation** — Notes and TextEdit; a 10-second utterance lands **verbatim**;
     the full state sequence OPENING → RECORDING (waveform moves with voice, the "it
     heard me" signal, `PRODUCT_SPEC.md:84`) → TRANSCRIBING → DELIVERED → IDLE; both
     apps.
  2. **Secure Input through the loop** — a password field shows the reason-only notice
     (`.secureInput` copy); no text ever lands there.
  3. **Esc during RECORDING and during TRANSCRIBING** — aborts; nothing injected
     (`PRODUCT_SPEC.md:129`).
  4. **Short press** (~80 ms) — returns to IDLE; no injector call; no failsafe.
  5. **Engine-not-ready refusal** — with the model directory blocked/removed, dictation
     is refused with the `.modelUnavailable` copy; the mic never opens (system mic
     indicator never lights).
  6. **Toggle session** — a toggle-mode session runs and ends via its triggers
     (ceiling / tap-disabled / system), per the session-lifecycle spec.
  7. **20-cycle stability** — no crash, no stuck session, zero transcript loss
     (PRD metric 3).
- Each entry: named gesture, expected result, failure report line, and the PRD metric
  it verifies.
- **Validation**: markdown reads clean; no entry invents behavior the shipped code or
  `PRODUCT_SPEC.md` does not have (cross-check each against the machine's
  `EndReason`/`SessionEffect` vocabulary).
- **Commit**: `Record the dictation loop's first-execution smoke steps`

## Agent notes

- Do not edit the existing steps 18-19 (real-engine WER) or 22-35 (ladder adapters) —
  add alongside.
- Cross-check each gesture against the shipped machine: reasons, states, and copy must
  match what the code can actually produce (the checklist is the code's only honest
  executor).
