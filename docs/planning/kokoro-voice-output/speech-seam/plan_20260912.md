# Plan: speech-seam (2026-09-12)

> Implements `speech-seam/spec.md` (PRD G1/G3/G4/G5/G8, R1/R2/R4, S2). Strict TDD. Worktree
> `.claude/worktrees/feat-kokoro-voice-output`, branch `feat/kokoro-voice-output/aliz`.
> Floor: 1930 → ratchet to 1949 (the executed count from the last full runs).

## Task list (agents team — one agent per task, TDD inside each)

1. **T1 — RED: the contract pins.** In `Tests/HarnessTests/` add `SpeechSynthesizerSeamTests.swift`:
   - `StubSynthesizer` (deterministic chunks, injected yield delay, cancel recorder).
   - Contract pins: identity attribution; empty text → empty stream; sentence order; cancel
     mid-stream → prompt termination, no chunks after; cancel-then-reinvoke → second speak
     completes.
   - RED: compile failures — no `SpeechSynthesizer`/`AudioChunk`/`VoiceIdentity` types exist.
2. **T2 — GREEN: the vocabulary.** In `VoccaCore/` add `Sources/VoccaCore/Speech/`:
   `AudioChunk.swift`, `VoiceIdentity.swift`, `SpeechSynthesizer.swift` (protocol + the
   documented cancel contract). GREEN: the pins pass. (Core imports nothing — a `CoreBoundaryTests`-visible fact.)
3. **T3 — RED/GREEN: the chunker.** `SentenceChunkerTests.swift` first (the acceptance
   table: multi-sentence, no-boundary run stays whole, punctuation kept, whitespace-only → [],
   empty → [], trimming, the `space-after` abbreviation rule) — RED — then
   `Sources/VoccaCore/Speech/SentenceChunker.swift` — GREEN.
4. **T4 — RED/GREEN: the parameterized suite body + floor ratchet.** `SpeechFixtureSuite.swift`
   (the `evaluate(_ synth:fixtures:)` body: pinned duration floor, ordering, cancel ≤50 ms,
   re-invoke) + `SpeechSynthesizerSuiteTests.swift` running it over the stub. RED first (the
   body doesn't exist), GREEN after. In the same commit, ratchet
   `Scripts/test-with-floor.sh:1569` `MINIMUM_EXECUTED_TESTS` 1930 → 1949 (the deliberate
   reviewed edit; the commit message names the raise and the drift it fixes).
5. **T5 — full suite + floor.** `Scripts/test-with-floor.sh` — executed ≥1949, floor holds.
   Commit per task.

## Validation

- `swift test --filter SpeechSynthesizerSeamTests` — RED at T1, GREEN at T2.
- `swift test --filter SentenceChunkerTests` — RED→GREEN at T3.
- `swift test --filter SpeechSynthesizerSuiteTests` — RED→GREEN at T4.
- `Scripts/test-with-floor.sh` — floor 1949 holds (T5).
- `git diff --stat`: no AVFoundation names in VoccaCore (the Core import lint stays green).

## Edge cases

- The stub's injected delay makes the cancel-timing leg deterministic; the real-engine timing
  is the second aspect's env-gated leg.
- The `space-after` abbreviation rule: "Mr. Smith went." → one chunk (the `.` after `Mr` is
  not followed by whitespace), "Mr. Smith went. He left." → two chunks — pinned in T3's table.
- Floor ratchet rides in T4's commit WITH the tests (the script's doctrine: no bare raise).

## Commits

- `speech: pin the SpeechSynthesizer seam contract — RED stubs, then GREEN vocabulary`
- `speech: the sentence chunker — RED table, then GREEN`
- `speech: the parameterized suite body + floor ratchet 1930 -> 1949`

## Checkpoints

- After T2: the seam's public surface is final (the second aspect compiles against it).
- After T4: report the floor raise + executed count.