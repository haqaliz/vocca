# Plan: system-synthesizer (2026-09-12)

> Implements `system-synthesizer/spec.md` (PRD G2/G5/G6/G7, R3/R5/R6/R7, S1, N1). Strict TDD.
> Worktree `.claude/worktrees/feat-kokoro-voice-output`, branch `feat/kokoro-voice-output/aliz`.

## Task list (agents team — one agent per task, TDD inside each)

1. **T1 — RED: the adapter's headless pins.** In `Tests/HarnessTests/` add
   `SystemSynthesizerTests.swift`:
   - Buffer conversion: an `AudioChunk` built from a constructed `AVAudioPCMBuffer`
     (known frames/rate/channels → duration = frames ÷ rate; bytes = the buffer's frames)
     — the conversion function must be visible to tests (a static/builder on the adapter or
     a small internal type the tests import via `@testable` — follow the repo's precedent).
   - Chunker→utterance mapping: the adapter feeds the chunker's sentences as utterances
     (assert the mapping over the pure half).
   - Identity: `engineID == "system"`, `voiceName` = the configured voice identifier.
   - Cancel semantics over a minimal real invocation where the environment allows.
   RED: no `SystemSynthesizer` type exists (compile RED).
2. **T2 — GREEN: the adapter.** `Sources/VoccaSpeech/System/SystemSynthesizer.swift` +
   the `VoccaSpeech/` seam imports (`VoccaSpeech` gains the `VoccaCore` dependency in
   `Package.swift`; the placeholder file is retired — the module's product is the adapter).
   The `write(_:toBufferCallback:)` → `AsyncThrowingStream` bridge; one utterance per
   sentence; cancel = `stopSpeaking(at: .immediate)` + stream termination; re-invoke safe;
   voice/rate as plain data inputs. GREEN: T1's pins pass.
3. **T3 — RED/GREEN: the lint amendments.** First RED (the reviewed edits fail the lints):
   - `ModuleBoundaryTests.swift`: `VoccaSpeech` leaf → adapter move (+ its positive control).
   - `AudioFormatConverterTests.swift`: the AVFoundation expected-set gains
     `VoccaSpeech/System/SystemSynthesizer.swift`.
   - The seam-family row: AVSpeechSynthesizer/AVFAudio confined to that one file (planted
     violation in a copy → the scan must fail).
   Then GREEN (the edits themselves, minimal).
4. **T4 — RED/GREEN: the env-gated real suite + TTFA benchmark + probe wiring.**
   - `SpeechSystemSuiteTests.swift` (env-gated `VOCCA_RUN_REAL_SPEECH=1`): the parameterized
     body from `speech-seam` over `SystemSynthesizer` (duration floor, ordering, cancel
     ≤50 ms wall-clock, re-invoke).
   - The TTFA harness (`TimeToFirstAudioBenchmark.swift` or the suite's measured row): time
     to first chunk, printed/recorded — RED first (harness absent), GREEN.
   - `VoccaNetworkProbe`: the `VoccaSpeech` placeholder witness replaced with real driven
     work (construct + empty-speak + cancel) — the module-coverage cross-check must pass.
5. **T5 — the record.** The follow-on card for the Kokoro binding (S1: runtime options ranked
   per `ARCHITECTURE.md:706`, phonemizer named) goes in the STATUS record, not the code; the
   TTFA measured row lands in `SMOKE_CHECKLIST.md` as **step 126** (recorded, never gated).
   Commit per task.

## Validation

- `swift test --filter SystemSynthesizerTests` — RED at T1, GREEN at T2.
- `swift test --filter ModuleBoundaryTests` + `AudioFormatConverterTests` + the seam family —
  RED→GREEN at T3.
- `VOCCA_RUN_REAL_SPEECH=1 swift test --filter SpeechSystemSuiteTests` — GREEN at T4 on the
  founder's machine (CI skips the gate).
- `swift test --filter ZeroNetworkTests` — the module coverage cross-check passes (T4).
- `Scripts/test-with-floor.sh` — floor 1949 holds throughout.
- SMOKE_CHECKLIST step 126 exists with the measured TTFA + the never-gated note (T5).

## Edge cases

- CI cannot render speech reliably → every real-speech test is env-gated; CI runs only the
  headless half. The founder's machine runs the gated half once and records the output.
- If TTFA measures >300 ms: record the number verbatim; the ≤300 ms budget is never claimed;
  the warm-the-renderer mitigation is a recorded option for the Kokoro unit.
- The `write` callback may deliver zero-length buffers at boundaries — the conversion drops
  empty frames (pinned).

## Commits

- `speech: the system synthesizer — RED headless pins, then GREEN adapter`
- `speech: lint amendments for the AVFAudio surface (module move, import set, seam family)`
- `speech: env-gated real suite + TTFA benchmark + zero-network probe wiring`
- `speech: record the TTFA row (SMOKE 126) and the Kokoro follow-on card`

## Checkpoints

- After T2: the adapter's public surface is final.
- After T4: report the env-gated suite results + the TTFA number.
- After T5: the record is ready for the review-before-merge.