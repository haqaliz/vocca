# Card: injection-strategy-memory

> Source: inline brief (no GitHub issue). `vbf feat injection-strategy-memory`, generated by
> `vocca-next` on 2026-08-27. Owner: `aliz`.

## Brief

Build C8 (P2) — the per-app injection strategy memory plus the expanded matrix, per
`docs/technical/CAPABILITY_ROADMAP.md:178-193`. The `InjectionStrategyStore` seam is new;
persist which ladder rung succeeded per bundle ID, start there next time, demote on failure,
and re-probe occasionally so an app update that fixes AX support is eventually rediscovered;
seed known-hostile apps (Docs, Slack) so first-timers skip the discovery cost; add a
user-visible per-app override. The memory logic is headless and CI-tested (learn → tried
first → demote → rediscover); the ≥95% matrix number over the expanded 20+ app set is
produced by the founder running the harness, since it needs real Accessibility/Automation
grants no hosted runner has. Acceptance tests come first: a learned strategy is tried first,
a subsequent failure demotes it, re-probing rediscovers a working rung, pasteboard survives
100 dictations with a clipboard manager, and the zero-network default is untouched. Caveat:
dictation has never delivered end-to-end on a real machine (smoke 62–68), so the matrix rows
are the ladder's first real execution — a row that names `.accessibility` with nothing in the
field is a bug, not a pass.
